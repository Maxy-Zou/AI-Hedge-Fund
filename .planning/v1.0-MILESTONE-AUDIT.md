---
milestone: v1.0
audited: 2026-03-29
status: tech_debt
scores:
  requirements: 27/27
  phases: 8/8
  integration: 16/18
  flows: 1/2
gaps:
  requirements: []
  integration:
    - id: "INTG-01"
      description: "TearsheetBuilder not called from `backtest run --export-all` — only wired in demo-only `backtest export` subcommand"
      affected_requirements: ["RPT-04"]
      severity: "low"
      fix: "Add TearsheetBuilder.build() call to run command's export_all branch"
    - id: "INTG-02"
      description: "Dashboard sector chart uses DEMO_TICKER_SECTORS instead of querying UniverseTicker.gics_sector from DB"
      affected_requirements: ["RPT-03"]
      severity: "low"
      fix: "Query UniverseTicker for active tickers' gics_sector when not in demo mode"
  flows:
    - id: "FLOW-01"
      description: "`backtest run` → tearsheet PDF path is broken (TearsheetBuilder not called)"
      break_point: "cli.py run command export_all branch"
      severity: "low"
tech_debt:
  - phase: 06-streamlit-dashboard
    items:
      - "Visual smoke test deferred (human_needed verification)"
      - "Dashboard hardcodes DEMO_TICKER_SECTORS — needs DB query for live mode"
  - phase: 08-ai-washing-detector-integration
    items:
      - "CLI run --export-all skips TearsheetBuilder (only CSV+JSON produced)"
      - "Benchmark returns (SPY, IWM) not fetched in CLI run path — alpha/beta always 0.0 in exports"
  - phase: 05-risk-metrics-engine
    items:
      - "Benchmark data loader exists in dashboard but not wired to CLI run pipeline"
nyquist:
  compliant_phases: ["01"]
  partial_phases: []
  missing_phases: ["02", "03", "04", "05", "06", "07", "08"]
  overall: "partial"
---

# Milestone v1.0 Audit Report

## Summary

**Status:** tech_debt (no blockers, accumulated debt)
**Requirements:** 27/27 satisfied
**Phases:** 8/8 complete and verified
**Integration:** 16/18 connections wired (2 minor gaps)
**E2E Flows:** 1 complete, 1 partial (tearsheet in live run path)

## Requirements Coverage (3-Source Cross-Reference)

All 27 requirements marked `[x]` in REQUIREMENTS.md, verified in VERIFICATION.md, and listed in SUMMARY.md frontmatter.

| Category | Count | Status |
|----------|-------|--------|
| DATA (01-07) | 7 | All satisfied |
| BT (01-07) | 7 | All satisfied |
| RISK (01-04) | 4 | All satisfied |
| RPT (01-06) | 6 | All satisfied (RPT-03, RPT-04 have integration wiring debt) |
| INT (01-03) | 3 | All satisfied |

## Integration Gaps

### INTG-01: Tearsheet missing from live run path
- `backtest run --export-all` produces CSV + JSON but not PDF tearsheet
- Fix: Add `TearsheetBuilder.build()` call in cli.py run command
- Severity: Low — `backtest export --tearsheet` works with demo data; live tearsheet is a 5-line wiring fix

### INTG-02: Dashboard sector chart hardcoded to demo data
- `DEMO_TICKER_SECTORS` used instead of DB query
- Fix: Query `UniverseTicker.gics_sector` when not in demo mode
- Severity: Low — Phase 1 data is in the DB, just not queried by dashboard

## Tech Debt by Phase

### Phase 5: Risk Metrics Engine
- Benchmark data fetched in dashboard (yfinance) but not in CLI run path

### Phase 6: Streamlit Dashboard
- Visual smoke test deferred (human_needed)
- DEMO_TICKER_SECTORS hardcoded

### Phase 8: AI Washing Detector Integration
- `run --export-all` skips TearsheetBuilder
- alpha/beta always 0.0 in CLI exports (no benchmark fetch)

## Double-Shift Safety
Confirmed safe. `shift(1)` applied exactly once in `SignalAdapter.adapt()`. Both `PortfolioSimulator` and `AiWashingLoader` have explicit guard comments.

## Package Isolation
Confirmed. Zero `from ai_washer import` statements in backtest package. Only coupling is shared PostgreSQL tables.

## Test Suite
- 185 total tests (unit + integration)
- 90.27% coverage (requirement: 80%)
- All passing

---
*Audited: 2026-03-29*

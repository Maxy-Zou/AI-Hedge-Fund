---
phase: 04-cost-model-and-portfolio-simulator
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backtest/src/fund_backtest/simulator/__init__.py
  - backtest/src/fund_backtest/simulator/types.py
  - backtest/src/fund_backtest/config.py
  - backtest/tests/unit/test_simulator_types.py
  - backtest/tests/unit/test_simulator_engine.py
autonomous: true
requirements:
  - BT-02
  - BT-03
  - BT-04
  - BT-06
  - BT-07

must_haves:
  truths:
    - "CostConfig is a frozen Pydantic BaseModel with slippage_bps, commission_bps, borrow_cost_bps_annual fields (all non-negative float)"
    - "PortfolioResult is a Pydantic BaseModel holding gross_returns Series, net_returns Series, positions DataFrame, and trade_log DataFrame"
    - "load_cost_config() factory in config.py returns a CostConfig with defaults matching the phase success criteria"
    - "Test scaffold for engine (test_simulator_engine.py) contains failing test stubs for BT-02 through BT-07 behaviors"
    - "Test scaffold for types (test_simulator_types.py) contains failing test stubs for CostConfig and PortfolioResult validation"
  artifacts:
    - path: "backtest/src/fund_backtest/simulator/__init__.py"
      provides: "Package marker (empty)"
    - path: "backtest/src/fund_backtest/simulator/types.py"
      provides: "CostConfig, PortfolioResult, PriceFrame type alias"
      exports: ["CostConfig", "PortfolioResult", "PriceFrame"]
    - path: "backtest/src/fund_backtest/config.py"
      provides: "load_cost_config() factory added"
      contains: "load_cost_config"
    - path: "backtest/tests/unit/test_simulator_types.py"
      provides: "TDD stubs for CostConfig and PortfolioResult (RED state)"
    - path: "backtest/tests/unit/test_simulator_engine.py"
      provides: "TDD stubs for PortfolioSimulator behaviors (RED state)"
  key_links:
    - from: "backtest/src/fund_backtest/simulator/types.py"
      to: "backtest/src/fund_backtest/config.py"
      via: "CostConfig imported into load_cost_config()"
      pattern: "from fund_backtest.simulator.types import CostConfig"
    - from: "backtest/tests/unit/test_simulator_engine.py"
      to: "backtest/src/fund_backtest/simulator/engine.py"
      via: "import PortfolioSimulator (will exist in plan 02)"
      pattern: "from fund_backtest.simulator.engine import PortfolioSimulator"
---

<objective>
Create the type contracts and failing TDD scaffolds for the Portfolio Simulator.

Purpose: Establish the CostConfig and PortfolioResult types that downstream plans implement against, and write failing tests (RED state) that define the exact behavior expected from the simulator engine. Tests are the specification.

Output: simulator/types.py with frozen CostConfig and PortfolioResult, load_cost_config() in config.py, and two failing test files that become green in plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cost-model-and-portfolio-simulator/04-RESEARCH.md

@backtest/src/fund_backtest/config.py
@backtest/src/fund_backtest/signal/types.py
@backtest/src/fund_backtest/signal/adapter.py
</context>

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From backtest/src/fund_backtest/signal/types.py:
```python
WeightFrame = pd.DataFrame
# Schema: DatetimeIndex rows x ticker columns, float values in [-1.0, +1.0]
# CRITICAL: First row is always NaN (shift(1) artifact from SignalAdapter)
```

From backtest/src/fund_backtest/config.py (pattern to follow):
```python
class SignalAdapterConfig(BaseModel):
    """Signal adapter configuration."""
    min_coverage: int = 5
    gross_exposure_limit: float = 1.0

def load_signal_adapter_config(config_path: Path | None = None) -> SignalAdapterConfig:
    """Load signal adapter config from YAML config or return defaults."""
    ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create simulator/types.py — CostConfig and PortfolioResult</name>
  <read_first>
    - backtest/src/fund_backtest/config.py (SignalAdapterConfig pattern to follow exactly)
    - backtest/src/fund_backtest/signal/types.py (WeightFrame type alias pattern)
    - .planning/phases/04-cost-model-and-portfolio-simulator/04-RESEARCH.md (Pattern 1 and Pattern 2)
  </read_first>
  <files>
    backtest/src/fund_backtest/simulator/__init__.py,
    backtest/src/fund_backtest/simulator/types.py,
    backtest/src/fund_backtest/config.py
  </files>
  <behavior>
    - CostConfig.slippage_bps defaults to 10.0 (float, not int)
    - CostConfig.commission_bps defaults to 5.0 (float)
    - CostConfig.borrow_cost_bps_annual defaults to 50.0 (float)
    - All three CostConfig fields have Pydantic Field(ge=0.0) validators
    - CostConfig is frozen: model_config = {"frozen": True} — immutable after construction
    - PortfolioResult has model_config = {"arbitrary_types_allowed": True} to accept pd.Series/pd.DataFrame
    - PortfolioResult fields: gross_returns (pd.Series), net_returns (pd.Series), positions (pd.DataFrame), trade_log (pd.DataFrame)
    - PriceFrame is a pd.DataFrame type alias (same pattern as WeightFrame)
    - load_cost_config() added to config.py following load_signal_adapter_config() pattern exactly
    - CostConfig mutation raises a Pydantic ValidationError (frozen enforcement)
  </behavior>
  <action>
    Create backtest/src/fund_backtest/simulator/__init__.py as an empty file with only the module docstring.

    Create backtest/src/fund_backtest/simulator/types.py with:
    - Module docstring explaining the module, what it provides, and usage example
    - `from __future__ import annotations`
    - PriceFrame type alias (pd.DataFrame) with docstring: "date x ticker float close prices in dollars (not cents)"
    - CostConfig class:
      - Inherits from pydantic.BaseModel
      - model_config = {"frozen": True}
      - Triple-quoted Google-style docstring explaining all fields with units
      - slippage_bps: float = Field(default=10.0, ge=0.0) with docstring "One-way slippage per trade in bps (10 bps = 0.10%)"
      - commission_bps: float = Field(default=5.0, ge=0.0) with docstring "One-way commission per trade in bps (5 bps = 0.05%)"
      - borrow_cost_bps_annual: float = Field(default=50.0, ge=0.0) with docstring "Annualized short borrow cost in bps (50 bps = 0.50%/yr). Flat rate — tiered borrow is a future enhancement."
    - PortfolioResult class:
      - Inherits from pydantic.BaseModel
      - model_config = {"arbitrary_types_allowed": True}
      - Triple-quoted Google-style docstring
      - gross_returns: pd.Series with docstring "Daily gross returns before cost deduction. DatetimeIndex. Leading NaN row dropped."
      - net_returns: pd.Series with docstring "Daily net returns after all costs. DatetimeIndex. Same length as gross_returns."
      - positions: pd.DataFrame with docstring "Date x ticker weight held each day. Same shape as input WeightFrame (minus NaN row)."
      - trade_log: pd.DataFrame with docstring "Columns: date, ticker, direction (long/short), weight_before, weight_after, weight_change, cost_fraction, cost_bps."

    Append to backtest/src/fund_backtest/config.py (after load_signal_adapter_config):
    - Import: `from fund_backtest.simulator.types import CostConfig` at top of file (add to existing imports)
    - load_cost_config() function following the exact pattern of load_signal_adapter_config():
      - Same signature: `def load_cost_config(config_path: Path | None = None) -> CostConfig:`
      - Same YAML key access: `data.get("cost", {})` (reads from a "cost" YAML section)
      - Same fallback: return CostConfig() on any error or when config_path is None
      - Google-style docstring with Args, Returns sections

    IMPORTANT: Do NOT add a circular import. The import of CostConfig in config.py comes AFTER
    all existing imports — place it inside the function body if needed to avoid circular imports
    (the simulator/types.py imports nothing from config.py, so top-level import is fine).
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -c "from fund_backtest.simulator.types import CostConfig, PortfolioResult, PriceFrame; from fund_backtest.config import load_cost_config; c = load_cost_config(); print(c.slippage_bps, c.commission_bps, c.borrow_cost_bps_annual)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "class CostConfig" backtest/src/fund_backtest/simulator/types.py` returns a match
    - `grep -r "frozen.*True" backtest/src/fund_backtest/simulator/types.py` returns a match
    - `grep -r "class PortfolioResult" backtest/src/fund_backtest/simulator/types.py` returns a match
    - `grep -r "arbitrary_types_allowed" backtest/src/fund_backtest/simulator/types.py` returns a match
    - `grep -r "PriceFrame" backtest/src/fund_backtest/simulator/types.py` returns a match
    - `grep -r "load_cost_config" backtest/src/fund_backtest/config.py` returns a match
    - Python import command above prints "10.0 5.0 50.0"
    - `ls backtest/src/fund_backtest/simulator/__init__.py` exists
  </acceptance_criteria>
  <done>simulator/types.py exists with CostConfig (frozen, 3 float fields with ge=0 validators), PortfolioResult (arbitrary_types_allowed, 4 fields), PriceFrame alias. config.py has load_cost_config(). Package __init__.py created. Import succeeds and default values are 10.0 / 5.0 / 50.0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test scaffolds (RED state)</name>
  <read_first>
    - backtest/tests/unit/test_signal_adapter.py (factory function + class-organized test pattern to follow)
    - backtest/tests/unit/test_signal_types.py (types test pattern)
    - backtest/src/fund_backtest/simulator/types.py (just created — know the contracts)
    - .planning/phases/04-cost-model-and-portfolio-simulator/04-RESEARCH.md (Validation Architecture section — exact test class names)
  </read_first>
  <files>
    backtest/tests/unit/test_simulator_types.py,
    backtest/tests/unit/test_simulator_engine.py
  </files>
  <behavior>
    test_simulator_types.py:
    - TestCostConfigDefaults: asserts slippage_bps=10.0, commission_bps=5.0, borrow_cost_bps_annual=50.0
    - TestCostConfigValidation: asserts negative bps raises ValidationError; fractional bps (7.5) is accepted
    - TestCostConfigFrozen: asserts that assigning config.slippage_bps = 99.0 raises a Pydantic exception
    - TestPortfolioResultStructure: asserts PortfolioResult can be constructed with pd.Series and pd.DataFrame fields

    test_simulator_engine.py (RED — imports PortfolioSimulator which does not exist yet):
    - _make_weight_frame() factory: 5-day date range (business days), 3 tickers (AAPL/NVDA/MSFT), row 0 all-NaN (shift artifact), rows 1-4 with values in [-1, +1]
    - _make_price_frame() factory: matching date range, synthetic prices that yield known hand-calculated returns
    - TestShortPositions: test that negative weights appear in trade_log with direction="short" and borrow_cost applied
    - TestTransactionCosts: test that weight changes incur slippage+commission charges; days without changes incur zero transaction cost
    - TestBorrowCosts: test that borrow cost = abs(short_weight) * borrow_cost_bps_annual / 10000 / 252 per day
    - TestEqualWeight: test that simulator preserves the WeightFrame values unchanged in PortfolioResult.positions
    - TestHandCalculatedReference: test that a 2-ticker, 3-day scenario produces net_return matching a hand-calculated value within 1e-9
    - TestNoDoubleShift: test that result.positions does NOT have an additional shift vs the input WeightFrame
    - TestImmutableInputs: test that input WeightFrame and PriceFrame are not mutated by simulate()
    - TestNaNFirstRowDropped: test that net_returns does not contain the leading NaN row from WeightFrame

    All test_simulator_engine.py tests must FAIL at this stage because PortfolioSimulator does not exist.
    Use pytest.importorskip or a try/except import at module level is NOT acceptable — let tests fail with ImportError to confirm RED state.
  </behavior>
  <action>
    Create backtest/tests/unit/test_simulator_types.py:
    - Module docstring: "TDD tests for CostConfig and PortfolioResult type contracts."
    - `from __future__ import annotations`
    - Imports: pytest, pydantic.ValidationError, pd, CostConfig, PortfolioResult from fund_backtest.simulator.types
    - Class TestCostConfigDefaults with one test method: instantiate CostConfig() and assert all three default values
    - Class TestCostConfigValidation:
      - test_negative_slippage_raises: CostConfig(slippage_bps=-1.0) raises ValidationError
      - test_negative_commission_raises: CostConfig(commission_bps=-0.01) raises ValidationError
      - test_negative_borrow_raises: CostConfig(borrow_cost_bps_annual=-50.0) raises ValidationError
      - test_fractional_bps_accepted: CostConfig(slippage_bps=7.5) does not raise
      - test_zero_bps_accepted: CostConfig(slippage_bps=0.0, commission_bps=0.0, borrow_cost_bps_annual=0.0) does not raise
    - Class TestCostConfigFrozen:
      - test_mutation_raises: `cfg = CostConfig(); cfg.slippage_bps = 99.0` raises ValidationError or TypeError
    - Class TestPortfolioResultStructure:
      - test_construction: Build a PortfolioResult with two-element pd.Series for gross/net_returns and empty pd.DataFrame for positions/trade_log; assert isinstance checks pass

    Create backtest/tests/unit/test_simulator_engine.py:
    - Module docstring: "TDD tests for PortfolioSimulator engine behaviors."
    - `from __future__ import annotations`
    - Standard imports: pytest, pandas as pd, numpy as np
    - Import that will fail RED: `from fund_backtest.simulator.engine import PortfolioSimulator`
    - Import CostConfig, PortfolioResult: `from fund_backtest.simulator.types import CostConfig, PortfolioResult`

    Factory functions (module-level):
    ```python
    def _make_price_frame() -> pd.DataFrame:
        """Synthetic price frame: 5 business days, 3 tickers, known returns.

        Prices designed for hand-calculation:
          Day 0: AAPL=100, NVDA=200, MSFT=50
          Day 1: AAPL=102, NVDA=198, MSFT=51  (AAPL +2%, NVDA -1%, MSFT +2%)
          Day 2: AAPL=103, NVDA=197, MSFT=52  (AAPL +0.98%, NVDA -0.51%, MSFT +1.96%)
          Day 3: AAPL=104, NVDA=198, MSFT=51  (AAPL +0.97%, NVDA +0.51%, MSFT -1.92%)
          Day 4: AAPL=105, NVDA=199, MSFT=52  (AAPL +0.96%, NVDA +0.51%, MSFT +1.96%)
        """
        dates = pd.date_range("2024-01-02", periods=5, freq="B")
        return pd.DataFrame(
            {"AAPL": [100.0, 102.0, 103.0, 104.0, 105.0],
             "NVDA": [200.0, 198.0, 197.0, 198.0, 199.0],
             "MSFT": [50.0, 51.0, 52.0, 51.0, 52.0]},
            index=dates,
        )

    def _make_weight_frame(cost_config: CostConfig | None = None) -> pd.DataFrame:
        """Synthetic WeightFrame: 5 days, 3 tickers, row 0 NaN (shift artifact).

        AAPL long (+0.5), NVDA short (-0.5), MSFT flat (0.0) from day 1 onward.
        """
        dates = pd.date_range("2024-01-02", periods=5, freq="B")
        data = {
            "AAPL": [float("nan"), 0.5, 0.5, 0.5, 0.5],
            "NVDA": [float("nan"), -0.5, -0.5, -0.5, -0.5],
            "MSFT": [float("nan"), 0.0, 0.0, 0.0, 0.0],
        }
        return pd.DataFrame(data, index=dates)
    ```

    Test classes (each is a stub that will be implemented properly in this task):

    class TestShortPositions:
        def test_short_tickers_appear_in_trade_log_with_short_direction: ...
        def test_borrow_cost_deducted_from_net_returns_on_short_days: ...

    class TestTransactionCosts:
        def test_no_cost_when_weights_unchanged: ...
        def test_cost_charged_proportional_to_weight_change: ...

    class TestBorrowCosts:
        def test_daily_borrow_rate_formula: ...
        def test_zero_borrow_cost_when_no_shorts: ...

    class TestEqualWeight:
        def test_positions_preserve_input_weight_values: ...

    class TestHandCalculatedReference:
        def test_net_return_matches_hand_calc_day1: ...

    class TestNoDoubleShift:
        def test_positions_not_double_shifted: ...

    class TestImmutableInputs:
        def test_weight_frame_not_mutated: ...
        def test_price_frame_not_mutated: ...

    class TestNaNFirstRowDropped:
        def test_net_returns_no_leading_nan: ...

    IMPORTANT: Write REAL test implementations, not just `pass`. Each test method must contain
    actual assertions. Use `pytest.raises(ImportError)` is NOT acceptable — the tests must
    genuinely fail because PortfolioSimulator does not exist. Write them as if the class exists
    with a `simulate(weights, prices)` method returning a PortfolioResult.

    Hand-calculated reference for TestHandCalculatedReference.test_net_return_matches_hand_calc_day1:
    Day 1 (index 1) weights: AAPL=+0.5, NVDA=-0.5, MSFT=0.0
    Day 1 is the FIRST day with positions (day 0 is NaN — no trade on day 0, trade happens as
    day 0 -> day 1 transition).
    Day 2 prices: AAPL=103, NVDA=197, MSFT=52
    Day 1 prices: AAPL=102, NVDA=198, MSFT=51
    gross_return_day2 = 0.5*(103/102 - 1) + (-0.5)*(197/198 - 1)
                      = 0.5*0.009804 + (-0.5)*(-0.005051)
                      = 0.004902 + 0.002525 = 0.007427...
    transaction cost day2 = 0 (no weight change from day 1 to day 2)
    borrow cost day2 = 0.5 * (50/10000/252) = 0.5 * 0.00019841... = 0.00009920...
    net_return_day2 ≈ 0.007427 - 0.000099 ≈ 0.007328...

    The test asserts:
    ```python
    result = sim.simulate(_make_weight_frame(), _make_price_frame())
    day2 = result.net_returns.iloc[1]  # index 1 = second non-NaN day
    assert abs(day2 - expected) < 1e-6
    ```
    (exact value computed in test using the formula, not hardcoded)
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/test_simulator_types.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "class TestCostConfigDefaults" backtest/tests/unit/test_simulator_types.py` returns a match
    - `grep -r "class TestShortPositions" backtest/tests/unit/test_simulator_engine.py` returns a match
    - `grep -r "class TestHandCalculatedReference" backtest/tests/unit/test_simulator_engine.py` returns a match
    - `grep -r "class TestNaNFirstRowDropped" backtest/tests/unit/test_simulator_engine.py` returns a match
    - `cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/test_simulator_types.py -q` exits 0 (all type tests pass — types exist)
    - `cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/test_simulator_engine.py -q 2>&1 | grep -E "ERROR|error"` contains an import error (RED state confirmed — engine not yet built)
  </acceptance_criteria>
  <done>test_simulator_types.py passes (types exist). test_simulator_engine.py fails with ImportError for PortfolioSimulator (RED state confirmed). Both files have real test implementations with assertions, not stubs with `pass`.</done>
</task>

</tasks>

<verification>
cd /Users/maxzou/Documents/projects/AI Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/test_simulator_types.py -v
</verification>

<success_criteria>
1. simulator/ package exists with __init__.py and types.py
2. CostConfig is frozen Pydantic with slippage_bps=10.0, commission_bps=5.0, borrow_cost_bps_annual=50.0 defaults
3. PortfolioResult has arbitrary_types_allowed with 4 pandas fields
4. load_cost_config() is in config.py and returns CostConfig defaults
5. test_simulator_types.py: all tests GREEN
6. test_simulator_engine.py: all tests RED (ImportError — PortfolioSimulator not yet built)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cost-model-and-portfolio-simulator/04-01-SUMMARY.md`
</output>

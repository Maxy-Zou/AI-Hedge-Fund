---
phase: 03-signal-adapter-and-integration-contract
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backtest/src/fund_backtest/signal/__init__.py
  - backtest/src/fund_backtest/signal/types.py
  - backtest/src/fund_backtest/signal/validator.py
  - backtest/src/fund_backtest/config.py
  - backtest/tests/unit/test_signal_types.py
autonomous: true
requirements:
  - INT-01

must_haves:
  truths:
    - "SignalFrame and WeightFrame type aliases exist with full docstrings documenting their schema contract"
    - "SignalValidationError is a ValueError subclass with descriptive messages"
    - "validate_signal_frame() raises SignalValidationError for wrong index type, empty columns, non-string columns, and NaN-only columns"
    - "SignalAdapterConfig Pydantic BaseModel exists in config.py with min_coverage=5 and gross_exposure_limit=1.0 defaults"
    - "All validation tests pass (test_signal_types.py green)"
  artifacts:
    - path: "backtest/src/fund_backtest/signal/__init__.py"
      provides: "Package marker (empty)"
    - path: "backtest/src/fund_backtest/signal/types.py"
      provides: "SignalFrame alias, WeightFrame alias, documented contracts"
    - path: "backtest/src/fund_backtest/signal/validator.py"
      provides: "SignalValidationError, validate_signal_frame()"
    - path: "backtest/src/fund_backtest/config.py"
      provides: "SignalAdapterConfig model, load_signal_adapter_config() factory"
    - path: "backtest/tests/unit/test_signal_types.py"
      provides: "Validation tests covering all SignalValidationError cases"
  key_links:
    - from: "signal/validator.py"
      to: "signal/types.py"
      via: "import SignalValidationError (defined together for cohesion)"
      pattern: "SignalValidationError"
    - from: "config.py"
      to: "SignalAdapterConfig"
      via: "Pydantic BaseModel following PriceSettings pattern"
      pattern: "class SignalAdapterConfig"
---

<objective>
Define the typed signal contract for Phase 3: the SignalFrame/WeightFrame schemas, the validation function that enforces the contract, and the SignalAdapterConfig that controls normalization behavior. Write tests first (TDD) then implement to pass them.

Purpose: Any strategy module that produces signals must conform to the SignalFrame contract. This plan establishes that contract with runtime enforcement, so the adapter in Plan 02 has a validated input.

Output: signal/types.py, signal/validator.py, SignalAdapterConfig in config.py, and passing test_signal_types.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/maxzou/Documents/projects/AI Hedgefund/.planning/ROADMAP.md
@/Users/maxzou/Documents/projects/AI Hedgefund/.planning/phases/03-signal-adapter-and-integration-contract/03-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor MUST follow. No codebase exploration needed. -->

From backtest/src/fund_backtest/config.py:
```python
class PriceSettings(BaseModel):
    """Price data configuration."""
    lookback_years: int = 5
    batch_size: int = 80
    ...

def load_price_settings(config_path: Path | None = None) -> PriceSettings: ...
```

From backtest/src/fund_backtest/price/types.py — module structure pattern:
```python
"""Module docstring explaining purpose and what it provides."""
from __future__ import annotations
from pydantic import BaseModel, field_validator

class PriceAnomalyRecord(BaseModel):
    anomaly_type: Literal["return_spike_plus", "return_spike_minus"]
    ...
```

Test file pattern (from backtest/tests/unit/test_price_types.py):
```python
import pytest
from fund_backtest.price.types import ...

def test_something():
    ...

def test_raises_on_invalid():
    with pytest.raises(SomeError, match="descriptive pattern"):
        ...
```

Working directory for test runs: backtest/
Run command: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_types.py -x -q
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for signal contracts and validation</name>
  <files>backtest/tests/unit/test_signal_types.py</files>
  <read_first>
    - backtest/src/fund_backtest/config.py (PriceSettings pattern to follow for SignalAdapterConfig)
    - backtest/tests/unit/test_price_types.py (test structure pattern)
    - backtest/src/fund_backtest/price/types.py (PriceAnomalyRecord Literal pattern)
  </read_first>
  <behavior>
    TDD: Write tests BEFORE implementation. All tests must FAIL at this point.

    Tests to write in backtest/tests/unit/test_signal_types.py:

    test_signal_validation_error_is_value_error:
      - Instantiate SignalValidationError("msg")
      - Assert isinstance(err, ValueError)

    test_validate_signal_frame_rejects_non_datetime_index:
      - df = pd.DataFrame({"AAPL": [90.0]}, index=[0])  # integer index
      - pytest.raises(SignalValidationError, match="DatetimeIndex")

    test_validate_signal_frame_rejects_empty_columns:
      - df = pd.DataFrame(index=pd.DatetimeIndex(["2024-01-02"]))
      - pytest.raises(SignalValidationError, match="at least one")

    test_validate_signal_frame_rejects_non_string_column_names:
      - df = pd.DataFrame({1: [90.0]}, index=pd.DatetimeIndex(["2024-01-02"]))
      - pytest.raises(SignalValidationError, match="non-empty strings")

    test_validate_signal_frame_rejects_empty_string_column:
      - df = pd.DataFrame({"": [90.0]}, index=pd.DatetimeIndex(["2024-01-02"]))
      - pytest.raises(SignalValidationError, match="non-empty strings")

    test_validate_signal_frame_accepts_valid_frame:
      - df with DatetimeIndex, string columns, float values — no error raised

    test_validate_signal_frame_accepts_partial_nan:
      - df with some NaN values (not all-NaN column) — no error raised

    test_validate_signal_frame_warns_and_accepts_all_nan_column:
      - df where one column is all NaN — no error raised (warning only)

    test_signal_adapter_config_defaults:
      - config = SignalAdapterConfig()
      - assert config.min_coverage == 5
      - assert config.gross_exposure_limit == 1.0

    test_signal_adapter_config_custom:
      - config = SignalAdapterConfig(min_coverage=3, gross_exposure_limit=2.0)
      - assert config.min_coverage == 3

    Import targets (will not exist yet, causing ImportError = RED):
      from fund_backtest.signal.validator import SignalValidationError, validate_signal_frame
      from fund_backtest.config import SignalAdapterConfig
  </behavior>
  <action>
    Create backtest/tests/unit/test_signal_types.py with all tests above.
    Run: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_types.py -x -q
    Confirm: All tests FAIL (ImportError or assertion errors — RED phase complete).
    Do NOT implement anything yet.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/test_signal_types.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backtest/tests/unit/test_signal_types.py exists
    - Running pytest shows collection errors or test failures (not "passed")
    - At minimum, ImportError for fund_backtest.signal.validator proves RED state
  </acceptance_criteria>
  <done>All test_signal_types.py tests collected and failing (RED state confirmed)</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — Implement signal contracts to pass all tests</name>
  <files>
    backtest/src/fund_backtest/signal/__init__.py
    backtest/src/fund_backtest/signal/types.py
    backtest/src/fund_backtest/signal/validator.py
    backtest/src/fund_backtest/config.py
  </files>
  <read_first>
    - backtest/tests/unit/test_signal_types.py (tests just written — implement to these specs)
    - backtest/src/fund_backtest/config.py (where to add SignalAdapterConfig)
    - backtest/src/fund_backtest/price/types.py (module structure to mirror)
  </read_first>
  <behavior>
    Implement minimal code to pass all tests in test_signal_types.py.

    signal/__init__.py:
      Empty file (package marker only).

    signal/types.py:
      - from __future__ import annotations
      - Module docstring explaining the file provides SignalFrame and WeightFrame type aliases
      - SignalFrame = pd.DataFrame with full docstring documenting schema contract:
        index = DatetimeIndex, columns = ticker symbols, values = float scores, NaN allowed
        Invariants enforced by validate_signal_frame()
      - WeightFrame = pd.DataFrame with full docstring documenting output contract:
        values in [-1.0, +1.0], already shifted(1), first row always NaN

    signal/validator.py:
      - from __future__ import annotations
      - Module docstring
      - import structlog; logger = structlog.get_logger(__name__)
      - class SignalValidationError(ValueError):
          """Raised when a SignalFrame fails contract validation. Always includes
          a descriptive message naming the exact violation."""
      - def validate_signal_frame(signal: pd.DataFrame) -> None:
          Checks (in order, raise on first violation):
          1. isinstance(signal.index, pd.DatetimeIndex) — raise "SignalFrame.index must be DatetimeIndex, got {type}"
          2. not signal.columns.empty — raise "SignalFrame must have at least one ticker column"
          3. all(isinstance(c, str) and c for c in signal.columns) — raise "All SignalFrame column names must be non-empty strings"
          4. For all-NaN columns: log warning, do NOT raise (warn-and-drop, not error)
          Function must be < 50 lines. Returns None on success.

    config.py (append, do not remove existing content):
      - class SignalAdapterConfig(BaseModel):
          """Signal adapter configuration — controls normalization and coverage rules."""
          min_coverage: int = 5
          gross_exposure_limit: float = 1.0
          With docstrings on each field explaining what they control.
      - def load_signal_adapter_config(config_path: Path | None = None) -> SignalAdapterConfig:
          Follow load_price_settings() pattern exactly.

    Run: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_types.py -x -q
    Confirm: ALL tests pass (GREEN state).
  </behavior>
  <action>
    Create the four files listed above with minimal correct implementations.
    Run ruff: cd backtest && .venv/bin/ruff check src/fund_backtest/signal/ src/fund_backtest/config.py --fix
    Run ruff format: cd backtest && .venv/bin/ruff format src/fund_backtest/signal/ src/fund_backtest/config.py
    Run pytest: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_types.py -v
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/test_signal_types.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "class SignalValidationError" backtest/src/fund_backtest/signal/validator.py
    - grep -r "class SignalAdapterConfig" backtest/src/fund_backtest/config.py
    - grep -r "SignalFrame = pd.DataFrame" backtest/src/fund_backtest/signal/types.py
    - grep -r "WeightFrame = pd.DataFrame" backtest/src/fund_backtest/signal/types.py
    - grep -r "min_coverage: int = 5" backtest/src/fund_backtest/config.py
    - pytest tests/unit/test_signal_types.py shows "passed" with 0 failures
  </acceptance_criteria>
  <done>All test_signal_types.py tests pass (GREEN state confirmed), ruff clean</done>
</task>

</tasks>

<verification>
Full unit suite must remain green after this plan:
cd /Users/maxzou/Documents/projects/AI Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/ -q 2>&1 | tail -5
</verification>

<success_criteria>
- signal/__init__.py, signal/types.py, signal/validator.py exist
- SignalAdapterConfig and load_signal_adapter_config() in config.py
- All 10 tests in test_signal_types.py pass
- Existing unit tests remain green (no regressions)
- ruff check passes on all new files
</success_criteria>

<output>
After completion, create `/Users/maxzou/Documents/projects/AI Hedgefund/.planning/phases/03-signal-adapter-and-integration-contract/03-01-SUMMARY.md`
</output>

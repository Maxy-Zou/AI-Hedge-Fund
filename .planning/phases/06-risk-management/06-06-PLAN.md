---
phase: 06-risk-management
plan: 06
type: execute
wave: 5
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - tests/integration/test_phase6_e2e.py
  - tests/integration/test_policy_sha_audit.py
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
must_haves:
  truths:
    - "Five end-to-end integration scenarios pass: no-violation, position-size veto, sector veto, correlation veto, drawdown veto"
    - "Every VETOED output contains the constraint name, observed value, and limit value in the rationale context (human-readable auditable output)"
    - "policy_sha differs when risk_policy.yaml fields change (audit trail integrity)"
    - "Insufficient-history scenario produces the 'insufficient_price_history' constraint name (Pitfall 4)"
    - "No real LLM calls — all tests use TestModel stubs for reproducibility"
  artifacts:
    - path: "tests/integration/test_phase6_e2e.py"
      provides: "5 end-to-end scenarios validating RISK-01/02/03 success criteria"
      contains: "def test_approved_path\\|def test_position_size_veto\\|def test_sector_concentration_veto\\|def test_correlation_veto\\|def test_drawdown_veto"
    - path: "tests/integration/test_policy_sha_audit.py"
      provides: "policy_sha is deterministic, change-sensitive, and persisted on every RiskAssessment"
  key_links:
    - from: "tests/integration/test_phase6_e2e.py"
      to: "ai_hedge_fund.graph.pipeline.build_debate_pipeline"
      via: "full pipeline invocation with 10-agent TestModel stubs"
---

<objective>
End-to-end integration validation of Phase 6. Runs the full `build_debate_pipeline(with_risk=True)` with all 10 LLM agents stubbed via TestModel against seeded fixtures covering five scenarios — one approved path and four distinct veto paths. Plus a dedicated policy_sha audit test suite.

Purpose: RISK-01/02/03 success criteria — "Risk Manager can veto a high-conviction recommendation", "position sizing constraints enforced as hard limits", "sector concentration / correlation / drawdown each trigger a named constraint violation" — are proven at the integration layer, not just the unit layer.

Output: Two new integration test files (no production code). The phase ships green after this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-risk-management/06-RESEARCH.md
@.planning/phases/06-risk-management/06-VALIDATION.md
@.planning/phases/06-risk-management/06-01-PLAN.md
@.planning/phases/06-risk-management/06-02-PLAN.md
@.planning/phases/06-risk-management/06-03-PLAN.md
@.planning/phases/06-risk-management/06-04-PLAN.md
@.planning/phases/06-risk-management/06-05-PLAN.md
@CLAUDE.md

# Prior summaries (this plan depends on the compiled pipeline from 06-05)
@.planning/phases/06-risk-management/06-01-SUMMARY.md
@.planning/phases/06-risk-management/06-02-SUMMARY.md
@.planning/phases/06-risk-management/06-03-SUMMARY.md
@.planning/phases/06-risk-management/06-04-SUMMARY.md
@.planning/phases/06-risk-management/06-05-SUMMARY.md

# Analog: Phase-5 end-to-end TestModel integration style
@tests/integration/test_debate_pipeline.py

<interfaces>
<!-- Pipeline construction — exact signature from 06-05 -->
```python
from ai_hedge_fund.graph.pipeline import build_debate_pipeline
from ai_hedge_fund.graph.nodes import RiskDeps

pipeline = build_debate_pipeline(with_risk=True, risk_deps=deps)
# Initial state: {"ticker": "MSFT", "as_of_date": "2026-03-01", "candidate_metadata": {"sector": "Technology", "instrument_type": "equity"}}
# candidate_metadata is REQUIRED for scenarios that exercise sector/exclusions checks (BL-03):
#   sector and instrument_type are injected via this state key — NOT extracted from ThesisOutput.
from ai_hedge_fund.schemas.state import CandidateMetadata
initial_state = {
    "ticker": "MSFT",
    "as_of_date": "2026-03-01",
    "candidate_metadata": CandidateMetadata(sector="Technology").model_dump(),
}
final_state = await pipeline.ainvoke(initial_state)
# Final state contains: analyst_reports, thesis, bull_case, bear_case, rebuttal,
#   final_arguments, debate_synthesis, risk_assessment, signal|None
```

<!-- TestModel stubbing — 10 agents to override simultaneously -->
```python
from pydantic_ai.models.test import TestModel
from ai_hedge_fund.agents import (
    fundamental_agent, sentiment_agent, technical_agent, manager_agent,
    bull_agent, bear_agent, rebuttal_agent, final_arguments_agent,
    debate_synthesis_agent, signal_agent,
)
from ai_hedge_fund.agents.risk_manager import risk_manager_agent
# Idiomatic pattern from tests/integration/test_debate_pipeline.py — use `.override(model=TestModel(...))`
# nested `with` blocks OR a contextlib.ExitStack to avoid 11-deep nesting.
```

<!-- Test fixtures -->
<!--   tests/risk/fixtures/portfolio_sample.csv   (5 positions, 3+ sectors, from 06-02) -->
<!--   tests/risk/fixtures/returns_golden.csv     (252-day returns, 6 tickers including correlated pair, from 06-03) -->
<!--   tests/risk/fixtures/risk_policy_sample.yaml (tighter thresholds than production, from 06-01) -->

<!-- Custom policies per-scenario -->
<!--   For position-size veto: set max_single_position_pct to a low value that the high-conviction candidate exceeds -->
<!--   For sector-concentration veto: seed portfolio with heavy Technology + make candidate Technology with a positive size -->
<!--   For correlation veto: candidate ticker is one that correlates ~0.95 with a portfolio ticker in the golden returns -->
<!--   For drawdown veto: synthesise a returns DataFrame (test-local) with a known >25% drawdown signature -->
<!--   For insufficient-history veto: candidate ticker has <60 rows in returns -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five end-to-end scenarios against the wired pipeline</name>
  <files>tests/integration/test_phase6_e2e.py</files>
  <read_first>
    - tests/integration/test_debate_pipeline.py (Phase-5 TestModel-driven pattern — mirror for 11-agent override)
    - src/ai_hedge_fund/graph/pipeline.py (post-06-05 — the target of these tests)
    - tests/graph/test_pipeline_with_risk.py (06-05 task 2 — similar shape, but this file tests deeper semantic scenarios)
    - tests/risk/fixtures/portfolio_sample.csv + returns_golden.csv + risk_policy_sample.yaml (the fixtures being exercised)
    - Cross-reference 06-03-PLAN.md Task 2 fixture generation: confirm MSFT is in the golden returns CSV with ≥60 rows. If not, 06-03 must be amended — Scenario 1 relies on MSFT being present.
    - .planning/phases/06-risk-management/06-VALIDATION.md row 6-07-01 (acceptance contract)
    - .planning/phases/06-risk-management/06-RESEARCH.md Pitfalls 3, 4, 6, 8
  </read_first>
  <behavior>
    Each scenario runs the full 10-agent stubbed pipeline with seeded fixtures and asserts specific final-state properties.

    - Scenario 1 — test_approved_path_produces_signal:
      - Fixture: default sample_policy (max_single_position_pct=8.0), seeded 5-position portfolio across 3 sectors, golden returns (includes AAPL, MSFT, JNJ, JPM, PG, NEW per 06-03 Task 2)
      - Candidate ticker: **MSFT** — chosen because (a) MSFT is in `returns_golden.csv` with ≥60 rows per 06-03 Task 2 and (b) MSFT is NOT in `portfolio_sample.csv` (which seeds AAPL/JNJ/JPM-style positions) so no double-counting. Cross-reference: 06-03 Task 2 fixture generation must include MSFT with ≥60 rows; if not, the executor MUST update the golden fixture generator.
      - Stub thesis output: confidence=40 (→ conviction=low → candidate size ≈ 2.0% via 0.25× multiplier on 8.0% max), ticker=MSFT
      - Inject `state["candidate_metadata"] = CandidateMetadata(sector="Consumer Staples").model_dump()` (not heavily held in fixture portfolio — no sector-concentration conflict)
      - Assert: `final_state["risk_assessment"]["status"] == "APPROVED"`, `final_state["signal"] is not None`, `final_state["signal"]["ticker"] == "MSFT"`

    - Scenario 2 — test_position_size_veto_blocks_signal (RISK-02 success criterion 2):
      - Custom policy (spelled out explicitly for executor): `RiskPolicy(max_single_position_pct=0.01, size_high_conviction_multiplier=1.5, max_sector_pct=30.0, max_total_exposure_pct=100.0, max_correlation_with_portfolio=0.80, max_projected_drawdown_pct=25.0, correlation_window_days=60, drawdown_window_days=252, min_history_days=60, excluded_instrument_types=[], excluded_sectors=[])`
      - Derived size for confidence=90 (high conviction): `0.01 * 1.5 = 0.015` (1.5%), which EXCEEDS the `max_single_position_pct=0.01` (1.0%) limit → position-size violation
      - Stub thesis: confidence=90, ticker=MSFT
      - Inject `state["candidate_metadata"] = CandidateMetadata(sector="Technology").model_dump()` — sector is irrelevant because position-size check fires FIRST per 06-05 Task 1's ordering
      - Assert: `final_state["risk_assessment"]["status"] == "VETOED"`
      - Assert: `final_state["risk_assessment"]["constraint_violated"] == "max_single_position_pct"`
      - Assert: `final_state["risk_assessment"]["observed"] == 0.015`
      - Assert: `final_state["risk_assessment"]["limit"] == 0.01`
      - Assert: `final_state["signal"] is None` (rejection, not silent cap — Pitfall 8)
      - Assert: rationale string is non-empty and references observed + limit values (substring check)

    - Scenario 3 — test_sector_concentration_veto (RISK-03):
      - Fixture: portfolio CSV has 25% Technology (AAPL+MSFT positions sized such that combined = 25% of portfolio value). Note: if Scenario 1 relies on MSFT being absent from portfolio_sample.csv, use a different portfolio fixture here (e.g., `portfolio_tech_heavy.csv`) that IS Technology-heavy; keep fixtures scenario-scoped so they don't conflict.
      - Custom policy: `RiskPolicy(..., max_sector_pct=30.0, max_single_position_pct=10.0, excluded_sectors=[], ...)` (so sector check fires but position-size and exclusions pass)
      - Stub thesis: confidence=90 (high conviction → 10.0 * 1.0 = 10% size), ticker=NVDA (or another Technology ticker not already in portfolio)
      - **Explicitly inject `state["candidate_metadata"] = CandidateMetadata(sector="Technology", instrument_type="equity").model_dump()` into the initial state** — BL-03 fix: sector is sourced from state, NOT from thesis. Post-trade Technology = 25% + 10% = 35% > 30% → VETOED.
      - Assert: `risk_assessment.status == "VETOED"`, `constraint_violated == "max_sector_pct"`, `observed ≈ 35.0`, `limit == 30.0`
      - Assert: `signal is None`

    - Scenario 4 — test_correlation_veto (RISK-03):
      - Portfolio contains AAPL (from sample CSV). Golden returns has MSFT correlating ~0.95 with AAPL.
      - Stub thesis: candidate ticker=MSFT, conviction=low (so position size doesn't trigger veto first)
      - Policy: max_correlation_with_portfolio=0.80
      - Assert: `status == "VETOED"`, `constraint_violated == "max_correlation_with_portfolio"`, `observed >= 0.90`, `limit == 0.80`

    - Scenario 5 — test_drawdown_veto (RISK-03):
      - Use either a synthesised DataFrame with a >25% drawdown OR the golden returns if any ticker sequence crosses 25% (if the golden generator produced such a ticker; if not, construct an inline 252-day series in the test with a 30% peak-to-trough drop)
      - Policy: max_projected_drawdown_pct=20.0
      - Stub thesis: candidate with enough history, low conviction (so sizing/sector/correlation all pass)
      - Assert: `status == "VETOED"`, `constraint_violated == "max_projected_drawdown_pct"`, `observed > limit`

    - Scenario 6 — test_insufficient_price_history_veto (Pitfall 4):
      - Candidate ticker has <60 rows in golden returns (fixture NEW has 30 rows per plan 06-03 task 2)
      - Assert: `status == "VETOED"`, `constraint_violated == "insufficient_price_history"`

    - Scenario 7 — test_vetoed_output_is_structured (RISK-01 success criterion 1 explicit):
      - Re-use Scenario 2's fixtures
      - Assert: vetoed output is "blocked by risk" with specific named constraint — NOT a modified recommendation. Verify by confirming: (a) signal is None, (b) risk_assessment contains a constraint_violated string in CONSTRAINT_NAMES, (c) no alternate position_size_pct was suggested anywhere in state (signal absent means no recommendation was modified downstream).
  </behavior>
  <action>
    Create `tests/integration/test_phase6_e2e.py`.

    Structure:
    - `from __future__ import annotations`
    - pytest + pytest-asyncio imports; TestModel imports; RiskDeps + build_debate_pipeline imports; fixture loaders for portfolio CSV + golden returns CSV + policies.
    - Top-level helpers:
      - `def _build_stub_thesis(ticker, confidence) -> dict` returning a ThesisOutput.model_dump() dict — note ThesisOutput has NO sector/instrument_type fields (06-05 BL-03 rationale); those are injected separately via candidate_metadata.
      - `def _build_candidate_metadata(sector, instrument_type="equity") -> dict` returning `CandidateMetadata(sector=sector, instrument_type=instrument_type).model_dump()` — the explicit state-key injection point.
      - `def _stub_all_agents(stack, thesis_dict, ...)` that uses a `contextlib.ExitStack` to call `.override(model=TestModel(custom_output_args=...))` on all 11 agents (fundamental, sentiment, technical, manager, bull, bear, rebuttal, final_arguments, debate_synthesis, signal, risk_manager). Each stub returns minimally-valid output for its schema. For the manager agent the stub returns a full ThesisOutput with the desired confidence + ticker. The signal agent returns a valid SignalOutput (only consumed on APPROVED path).
      - `def _build_deps(db_session, policy, returns)` returning RiskDeps
      - `def _initial_state(ticker, as_of_date, sector, instrument_type="equity") -> dict` helper that composes the initial state including `candidate_metadata`.
    - Seven async test functions, one per scenario above.

    For Scenario 4 (correlation veto), the candidate ticker must exist in the golden returns AND in no portfolio position to avoid double-counting; pick MSFT if AAPL is the portfolio position or vice versa. For Scenario 5 (drawdown), if the golden fixture does not produce a 25%+ drawdown scenario cleanly, construct a test-local returns DataFrame inline with a known signature (e.g., 252 days where price starts at 1.0, climbs to 1.5 over 100 days, drops to 1.0 over 100 days = 33% drawdown).

    Each test:
    1. Build a db_session fixture (from root conftest) and seed portfolio via seed_portfolio_from_csv
    2. Build the appropriate RiskPolicy (either via load_policy(sample_policy_path) or RiskPolicy(...) directly)
    3. Build RiskDeps
    4. Enter the agent-override ExitStack
    5. `pipeline = build_debate_pipeline(with_risk=True, risk_deps=deps)`
    6. `final_state = await pipeline.ainvoke({"ticker": ..., "as_of_date": "2026-03-01"})`
    7. Assert on `final_state["risk_assessment"]` and `final_state["signal"]`

    File size target: 350-500 lines. If approaching 800, split helper utilities into `tests/integration/_phase6_helpers.py` (not named as a test file). Functions <50 lines each.

    Run `uv run ruff format tests/integration/test_phase6_e2e.py && uv run ruff check --fix tests/integration/test_phase6_e2e.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_phase6_e2e.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_phase6_e2e.py` exists with ≥7 async test functions (verify: `grep -c "^async def test_" tests/integration/test_phase6_e2e.py` ≥7)
    - Each test invokes `build_debate_pipeline(with_risk=True, ...)` (verify: `grep -c "with_risk=True" tests/integration/test_phase6_e2e.py` ≥5)
    - BL-03 candidate_metadata injection — `grep -c "candidate_metadata" tests/integration/test_phase6_e2e.py` ≥3 (Scenarios 1, 2, 3 all inject the state key)
    - Scenario 1 uses MSFT as candidate ticker (verify: `grep -q "MSFT" tests/integration/test_phase6_e2e.py`)
    - Scenario 2 uses the exact policy `max_single_position_pct=0.01, size_high_conviction_multiplier=1.5` (WN-02 arithmetic) — verify: `grep -q "size_high_conviction_multiplier=1.5" tests/integration/test_phase6_e2e.py && grep -q "max_single_position_pct=0.01" tests/integration/test_phase6_e2e.py`
    - Each test asserts on `risk_assessment` AND `signal` keys (verify: both substrings appear in file)
    - No real LLM model strings used outside TestModel context (verify: `grep -E "ModelTier\.|anthropic:" tests/integration/test_phase6_e2e.py` returns 0 — ModelTier is only imported for type hints or fixture config, never invoked for a real API call)
    - `uv run pytest tests/integration/test_phase6_e2e.py -q` exits 0 with ≥7 tests passing
    - Ruff clean
    - File <= 600 lines
  </acceptance_criteria>
  <done>Five scenarios (RISK-01/02/03 success criteria) + insufficient-history + structured-veto-output all proven end-to-end against the wired pipeline. Phase 6 success criteria structurally verified.</done>
</task>

<task type="auto">
  <name>Task 2: policy_sha audit trail integration tests</name>
  <files>tests/integration/test_policy_sha_audit.py</files>
  <read_first>
    - src/ai_hedge_fund/risk/policy.py (compute_policy_sha from 06-01)
    - src/ai_hedge_fund/graph/nodes.py (risk_manager_node from 06-05 — policy_sha insertion point)
    - .planning/phases/06-risk-management/06-VALIDATION.md row 6-07-02
    - tests/integration/test_phase6_e2e.py (task 1 above — reuse fixture/stub helpers)
  </read_first>
  <behavior>
    - Test 1: Two successive pipeline runs with the SAME policy produce the SAME policy_sha on the emitted RiskAssessment
    - Test 2: Two successive runs with DIFFERENT policies produce DIFFERENT policy_sha values
    - Test 3: Mutating `max_single_position_pct` from 10.0 → 10.1 produces a different policy_sha (change-sensitivity)
    - Test 4: policy_sha on an APPROVED assessment is exactly 64 lowercase hex chars
    - Test 5: policy_sha on a VETOED assessment is exactly 64 lowercase hex chars
    - Test 6: Changing one field and reverting back produces the original policy_sha (idempotent determinism)
  </behavior>
  <action>
    Create `tests/integration/test_policy_sha_audit.py`. Reuse helpers from `tests/integration/test_phase6_e2e.py` (or from `_phase6_helpers.py` if it was split).

    Each test:
    1. Construct two `RiskPolicy` instances (same or different fields)
    2. Build two RiskDeps, one per policy
    3. Run the full pipeline twice with stubbed agents (approved path — safe defaults so all 4 checks pass)
    4. Capture `final_state["risk_assessment"]["policy_sha"]` from each run
    5. Assert equality/inequality/format

    For test 5 (VETOED policy_sha), use the position-size-veto scenario from Task 1 and assert the policy_sha field is still populated with a valid hex.

    File <200 lines, functions <50 lines.

    Run ruff format + check.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_policy_sha_audit.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_policy_sha_audit.py` exists with ≥6 tests
    - Each test asserts on `final_state["risk_assessment"]["policy_sha"]` (verify: `grep -c "policy_sha" tests/integration/test_policy_sha_audit.py` ≥6)
    - All 6 tests pass
    - Ruff clean
  </acceptance_criteria>
  <done>policy_sha is deterministic, stable across same-policy runs, and different for different policies — audit trail integrity proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full pipeline invocation -> final state | Integration layer validates end-to-end that all trust-boundary mitigations from 06-01 through 06-05 compose correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Veto bypass (integration-layer) | end-to-end pipeline | mitigate (validation) | Scenarios 2-6 all stub the LLM to return approving language. The pipeline's deterministic path still produces VETOED. Proves the schema + node composition prevents veto bypass at the integration layer. |
| T-06-04 | Policy drift undetectable | policy_sha audit tests | mitigate (validation) | Task 2 all six tests prove policy_sha is deterministic + change-sensitive + persisted on every assessment. |
| T-06-08 | Veto + signal coexistence (Pitfall 8) | Scenario 7 | mitigate (validation) | Scenario 7 explicitly asserts `signal is None AND risk_assessment.status == VETOED` simultaneously — downstream Phase-8 consumers that check risk_assessment first will correctly interpret the outcome. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_phase6_e2e.py tests/integration/test_policy_sha_audit.py -q` passes with ≥13 tests
- `uv run pytest -q` (full suite) passes — no regressions in any prior phase
- All five success-criteria scenarios are represented: APPROVED path, position-size veto, sector veto, correlation veto, drawdown veto
- No real LLM calls — all tests use TestModel (reproducible, fast, no API key needed)
- Phase 6 UAT (if triggered) will have human-verified scenarios cross-referenced to these automated scenarios
</verification>

<success_criteria>
- Five end-to-end scenarios proving RISK-01, RISK-02, RISK-03 success criteria green
- policy_sha audit (T-06-04) verified deterministic + persisted
- No regressions in Phase 1-5 test suites
- Phase 6 integration coverage complete — ready for UAT / verify-work
</success_criteria>

<output>
Create `.planning/phases/06-risk-management/06-06-SUMMARY.md`. Key fields:
- provides: `tests/integration/test_phase6_e2e.py`, `tests/integration/test_policy_sha_audit.py` (test surfaces, no production code)
- requirements-completed: RISK-01 (integration-verified), RISK-02 (integration-verified), RISK-03 (integration-verified)
- affects: Phase 7 memory (can reference persisted RiskAssessment + policy_sha); Phase 8 signal (must gate on risk_assessment before signal — Pitfall 8)
- Validation sign-off checklist: all `<automated>` commands green, all Wave 0 fixtures present, sampling continuity met
</output>

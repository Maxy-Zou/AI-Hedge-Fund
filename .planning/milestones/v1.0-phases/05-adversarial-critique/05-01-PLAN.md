---
phase: 05-adversarial-critique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_hedge_fund/schemas/debate.py
  - src/ai_hedge_fund/schemas/__init__.py
  - src/ai_hedge_fund/agents/bull.py
  - src/ai_hedge_fund/agents/bear.py
  - src/ai_hedge_fund/agents/__init__.py
  - tests/unit/test_debate_schemas.py
  - tests/unit/test_bull_agent.py
  - tests/unit/test_bear_agent.py
autonomous: true
requirements:
  - DEBATE-01
  - DEBATE-02
  - DEBATE-03
must_haves:
  truths:
    - "BullCase schema rejects outputs with fewer than 3 BullClaims (ValidationError)"
    - "BearCase schema rejects outputs with fewer than 2 addressed_bull_claims (ValidationError) — this IS the DEBATE-02 enforcement"
    - "Every BullClaim / BearClaim carries a source_analyst Literal restricted to fundamental|sentiment|technical|manager"
    - "bull_agent and bear_agent are Agent[None, BullCase|BearCase] at REASONING tier with zero tools and retries=2"
    - "Bear system prompt literally instructs the agent to quote >=2 bull claims verbatim into addressed_bull_claims"
  artifacts:
    - path: "src/ai_hedge_fund/schemas/debate.py"
      provides: "All 5 top-level act schemas (BullCase, BearCase, RebuttalAct, FinalArguments, DebateSynthesis) plus their nested claim types (BullClaim, BearClaim, RebuttalPoint). Plan 05-02 consumes RebuttalAct/FinalArguments/DebateSynthesis; Plan 05-03 does not redefine."
      contains: "class BullCase, class BearCase, class RebuttalAct, class FinalArguments, class DebateSynthesis"
    - path: "src/ai_hedge_fund/agents/bull.py"
      provides: "bull_agent instance, BULL_SYSTEM_PROMPT, get_bull_limits(), format_analyst_evidence()"
      exports: ["bull_agent", "BULL_SYSTEM_PROMPT", "get_bull_limits", "format_analyst_evidence"]
    - path: "src/ai_hedge_fund/agents/bear.py"
      provides: "bear_agent instance, BEAR_SYSTEM_PROMPT, get_bear_limits(), format_bull_case_for_bear()"
      exports: ["bear_agent", "BEAR_SYSTEM_PROMPT", "get_bear_limits", "format_bull_case_for_bear"]
    - path: "tests/unit/test_debate_schemas.py"
      provides: "Per-schema validation tests; includes TestEmptyActRaisesValidationError for DEBATE-03"
    - path: "tests/unit/test_bull_agent.py"
      provides: "Wiring tests (tier=REASONING, output_type=BullCase, zero tools, retries=2, system prompt content)"
    - path: "tests/unit/test_bear_agent.py"
      provides: "Wiring tests + BEAR_SYSTEM_PROMPT asserts 'addressed_bull_claims' / 'at least 2' requirement appears"
  key_links:
    - from: "src/ai_hedge_fund/agents/bull.py"
      to: "src/ai_hedge_fund/schemas/debate.py (BullCase)"
      via: "Agent[None, BullCase] output_type binding"
      pattern: "output_type=BullCase"
    - from: "src/ai_hedge_fund/agents/bear.py"
      to: "src/ai_hedge_fund/schemas/debate.py (BearCase)"
      via: "Agent[None, BearCase] output_type binding"
      pattern: "output_type=BearCase"
    - from: "src/ai_hedge_fund/schemas/debate.py (DebateSynthesis.revised_thesis)"
      to: "src/ai_hedge_fund/schemas/agents.py (ThesisOutput)"
      via: "direct import and reuse — do NOT redefine"
      pattern: "from ai_hedge_fund.schemas.agents import ThesisOutput"
---

<objective>
Land the debate schema module and the first two debate agents (Bull Advocate, Bear Advocate) with full unit-test coverage.

Purpose: Phase 5 enforces "structured debate only" by putting all debate contracts into Pydantic. Bull and Bear are the first two debate agents; they set the template every subsequent debate agent follows (REASONING tier, zero tools, retries=2). DEBATE-01 and DEBATE-02 success criteria (specific data citations, ≥2 rebutted claims) are enforced at the schema boundary — no custom validator code is needed.

Output: `src/ai_hedge_fund/schemas/debate.py` (NEW), `src/ai_hedge_fund/agents/bull.py` (NEW), `src/ai_hedge_fund/agents/bear.py` (NEW), corresponding unit tests, and updated package re-exports. After this plan, Plan 05-02 can compose these schemas into Rebuttal/Final/Synthesis agents and Plan 05-03 can wire everything into the graph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-adversarial-critique/05-CONTEXT.md
@.planning/phases/05-adversarial-critique/05-RESEARCH.md
@.planning/phases/05-adversarial-critique/05-PATTERNS.md
@.planning/phases/05-adversarial-critique/05-VALIDATION.md
@CLAUDE.md

@src/ai_hedge_fund/schemas/agents.py
@src/ai_hedge_fund/agents/manager.py
@src/ai_hedge_fund/agents/__init__.py
@src/ai_hedge_fund/schemas/__init__.py
@tests/unit/test_manager_agent.py
@tests/unit/test_specialist_schemas.py

<interfaces>
<!-- Key contracts the executor needs — extracted so no codebase spelunking is required. -->

From src/ai_hedge_fund/schemas/agents.py:
```python
from typing import Literal
from pydantic import BaseModel, Field

class ThesisPoint(BaseModel):
    claim: str = Field(min_length=1)
    evidence: str = Field(min_length=1)
    source_tool: str = Field(min_length=1)

class ThesisOutput(BaseModel):
    ticker: str
    bull_case: list[ThesisPoint] = Field(min_length=3)
    bear_case: list[ThesisPoint] = Field(min_length=3)
    confidence: int = Field(ge=0, le=100)
    risk_factors: list[str] = Field(min_length=2)

class AnalystReport(BaseModel):
    analyst: Literal["fundamental", "sentiment", "technical"] = Field(...)
    analysis: dict
    tokens_used: int = Field(ge=0)
    error: str | None = None
```
DebateSynthesis MUST reuse ThesisOutput for its `revised_thesis` field (imported, not redefined).

From src/ai_hedge_fund/agents/manager.py (canonical zero-tools agent pattern):
```python
from pydantic_ai import Agent
from pydantic_ai.usage import UsageLimits
from ai_hedge_fund.agents.base import get_usage_limits
from ai_hedge_fund.models import ModelTier

MANAGER_SYSTEM_PROMPT = "..."

manager_agent: Agent[None, ThesisOutput] = Agent(
    ModelTier.REASONING.value,          # Opus
    output_type=ThesisOutput,
    system_prompt=MANAGER_SYSTEM_PROMPT,
    retries=2,
)

def get_manager_limits() -> UsageLimits:
    return get_usage_limits(ModelTier.REASONING)

def format_analyst_reports(reports: list[dict]) -> str:
    # pure module-level helper; no side effects
    ...
```

From src/ai_hedge_fund/agents/base.py:
```python
def get_usage_limits(
    model_tier: ModelTier,
    *,
    input_override: int | None = None,
    output_override: int | None = None,
    total_override: int | None = None,
) -> UsageLimits: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/ai_hedge_fund/schemas/debate.py with all 5 debate-act schemas + nested claim types</name>
  <files>src/ai_hedge_fund/schemas/debate.py, tests/unit/test_debate_schemas.py</files>
  <read_first>
    - src/ai_hedge_fund/schemas/debate.py (must be read even if absent — confirms starting state)
    - src/ai_hedge_fund/schemas/agents.py (ThesisPoint lines 59-79, ThesisOutput lines 82-110, AnalystReport lines 339-359 — the canonical schema template)
    - tests/unit/test_specialist_schemas.py (canonical schema-test template; your test file mirrors this exactly)
    - .planning/phases/05-adversarial-critique/05-RESEARCH.md sections "Pattern 2: Debate Schema Template" (lines 260-332) and "Validation Architecture" → Phase Requirements → Test Map (lines 766-779)
    - .planning/phases/05-adversarial-critique/05-PATTERNS.md section "src/ai_hedge_fund/schemas/debate.py" (lines 116-203) — exact deltas from ThesisPoint/ThesisOutput
  </read_first>
  <behavior>
    - BullCase with 2 claims raises ValidationError (min_length=3 on claims list)
    - BearCase with 1 addressed_bull_claim raises ValidationError (min_length=2 on addressed_bull_claims) — this IS DEBATE-02
    - BullClaim with source_analyst="marketing" raises ValidationError (Literal constraint)
    - RebuttalAct with empty bull_rebuttals raises ValidationError (min_length=2) and empty bear_rebuttals raises ValidationError (min_length=2)
    - FinalArguments with empty bull_closing raises ValidationError (min_length=1)
    - DebateSynthesis with evidence_strength=150 raises ValidationError (le=100)
    - DebateSynthesis with quality_score=-1 raises ValidationError (ge=0)
    - Every substantive string field (claim, evidence, synthesis_notes, headline) rejects empty string (min_length=1)
    - DebateSynthesis.revised_thesis is typed as ThesisOutput (imported, NOT redefined) and accepts a valid ThesisOutput instance
  </behavior>
  <action>
Create `src/ai_hedge_fund/schemas/debate.py` with module header docstring (following the `agents.py` lines 1-11 convention) that includes a `Threat mitigations:` block naming T-05-01 (Tampering: schema-enforced min_length prevents short/empty act outputs) and T-05-02 (Information disclosure N/A — debate uses only already-validated analyst evidence).

Imports (exactly, in this order):
```python
from __future__ import annotations
from typing import Literal
from pydantic import BaseModel, Field
from ai_hedge_fund.schemas.agents import ThesisOutput
```

Define an `AnalystName` type alias at module scope:
```python
AnalystName = Literal["fundamental", "sentiment", "technical", "manager"]
```
Note: includes "manager" because bull/bear may cite the manager's synthesis, not just the three analysts (per 05-RESEARCH.md Pitfall 2 mitigation).

Define the following Pydantic models in this order — each with a one-paragraph class docstring mirroring the ThesisPoint/ThesisOutput tone:

1. `BullClaim(BaseModel)`:
   - `claim: str = Field(min_length=1, description="The investment argument")`
   - `evidence: str = Field(min_length=1, description="Specific data point supporting the claim (number, date, filing section)")`
   - `source_analyst: AnalystName = Field(description="Which analyst produced the evidence")`

2. `BullCase(BaseModel)`:
   - `ticker: str = Field(description="Stock ticker symbol")`
   - `claims: list[BullClaim] = Field(min_length=3, description="Bull claims with evidence citations (minimum 3)")`
   - `headline: str = Field(min_length=1, description="One-sentence bull headline")`

3. `BearClaim(BaseModel)`:
   - `claim: str = Field(min_length=1)`
   - `evidence: str = Field(min_length=1)`
   - `source_analyst: AnalystName`
   - `addresses_bull_claim: str | None = Field(default=None, description="Verbatim or near-verbatim text of the bull claim this bear claim rebuts; None if independent counter-evidence")`

4. `BearCase(BaseModel)`:
   - `ticker: str`
   - `claims: list[BearClaim] = Field(min_length=3)`
   - `addressed_bull_claims: list[str] = Field(min_length=2, description="Verbatim text of at least 2 bull claims that this bear case directly rebuts — per DEBATE-02")`
     - Add constr-like non-empty enforcement by using `list[str]` with `min_length=2` on the list AND relying on `str` default (Pydantic rejects non-str). Per RESEARCH.md A3, verbatim enforcement is system-prompt only; the schema only enforces `len >= 2`.
   - `headline: str = Field(min_length=1)`

5. `RebuttalPoint(BaseModel)`:
   - `point: str = Field(min_length=1, description="The rebuttal argument")`
   - `targets_claim: str = Field(min_length=1, description="Verbatim text of the opposing claim this rebuts")`
   - `source_analyst: AnalystName`

6. `RebuttalAct(BaseModel)`:
   - `ticker: str`
   - `bull_rebuttals: list[RebuttalPoint] = Field(min_length=2, description="Bull-side rebuttals of bear points (minimum 2)")`
   - `bear_rebuttals: list[RebuttalPoint] = Field(min_length=2, description="Bear-side rebuttals of bull points (minimum 2)")`

7. `FinalArguments(BaseModel)`:
   - `ticker: str`
   - `bull_closing: str = Field(min_length=1, description="Bull side's closing argument citing final evidence")`
   - `bear_closing: str = Field(min_length=1, description="Bear side's closing argument citing final evidence")`
   - `bull_citation_count: int = Field(ge=0, description="Number of distinct evidence citations in bull closing")`
   - `bear_citation_count: int = Field(ge=0, description="Number of distinct evidence citations in bear closing")`

8. `DebateSynthesis(BaseModel)`:
   - `ticker: str`
   - `revised_thesis: ThesisOutput = Field(description="Post-debate thesis; may be identical to pre-debate or revised")`
   - `pre_debate_confidence: int = Field(ge=0, le=100, description="Manager's pre-debate confidence; sourced from state, not LLM")`
   - `post_debate_confidence: int = Field(ge=0, le=100, description="Confidence after debate — MUST be re-evaluated from scratch")`
   - `evidence_strength: int = Field(ge=0, le=100, description="LLM judgment: how strong is the evidence backing the thesis?")`
   - `logical_consistency: int = Field(ge=0, le=100, description="LLM judgment: how internally consistent is the debate?")`
   - `risk_coverage: int = Field(ge=0, le=100, description="LLM judgment: how well are risks identified and covered?")`
   - `quality_score: int = Field(ge=0, le=100, description="Computed (not LLM-generated) weighted mean of the three sub-scores. Populated by compute_quality_score() in debate_synthesis_node before writing to state.")`
   - `synthesis_notes: str = Field(min_length=1, description="Rationale for revised_thesis changes and confidence delta")`

Update `src/ai_hedge_fund/schemas/__init__.py` to re-export every new public symbol. Add this block in alphabetical order inside the imports:
```python
from ai_hedge_fund.schemas.debate import (
    BearCase,
    BearClaim,
    BullCase,
    BullClaim,
    DebateSynthesis,
    FinalArguments,
    RebuttalAct,
    RebuttalPoint,
)
```
Add each name to `__all__` in alphabetical order.

Now create `tests/unit/test_debate_schemas.py` following `tests/unit/test_specialist_schemas.py` verbatim with symbol substitution. Classes to include:
- `TestBullClaim`: test_valid_claim, test_rejects_empty_claim, test_rejects_empty_evidence, test_rejects_invalid_source_analyst (Literal "marketing" → ValidationError)
- `TestBullCase`: test_valid_with_three_claims (happy), test_rejects_fewer_than_three_claims (2 claims → ValidationError), test_rejects_empty_headline
- `TestBearClaim`: test_valid_with_addresses_bull_claim_none (default), test_valid_with_addresses_bull_claim_set, test_rejects_empty_claim
- `TestBearCase`: test_valid_happy_path, **`test_rejects_fewer_than_two_addressed_bull_claims`** → assert both `addressed_bull_claims=[]` AND `addressed_bull_claims=["only one"]` raise ValidationError. This is the DEBATE-02 enforcement test.
- `TestRebuttalPoint`: test_valid, test_rejects_empty_point, test_rejects_empty_targets_claim
- `TestRebuttalAct`: test_valid, test_rejects_empty_bull_rebuttals, test_rejects_empty_bear_rebuttals, test_rejects_one_bull_rebuttal (min_length=2)
- `TestFinalArguments`: test_valid, test_rejects_empty_bull_closing, test_rejects_empty_bear_closing, test_rejects_negative_citation_count
- `TestDebateSynthesis`: test_valid_happy_path (build a full ThesisOutput and nest it in revised_thesis), test_rejects_out_of_range_sub_score (evidence_strength=101 → ValidationError), test_rejects_negative_quality_score, test_rejects_empty_synthesis_notes
- **`TestEmptyActRaisesValidationError`**: one test per schema showing that instantiating with empty substantive fields raises ValidationError. This class is the DEBATE-03 success-criterion-3 enforcement evidence — name it exactly `TestEmptyActRaisesValidationError`.

Every test that triggers a ValidationError MUST use `with pytest.raises(ValidationError):`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_debate_schemas.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class BullCase\|class BearCase\|class RebuttalAct\|class FinalArguments\|class DebateSynthesis\|class BullClaim\|class BearClaim\|class RebuttalPoint" src/ai_hedge_fund/schemas/debate.py` returns 8
    - `grep -c "from ai_hedge_fund.schemas.agents import ThesisOutput" src/ai_hedge_fund/schemas/debate.py` returns 1 (reuse, not redefine)
    - `grep -c "AnalystName = Literal\[\"fundamental\", \"sentiment\", \"technical\", \"manager\"\]" src/ai_hedge_fund/schemas/debate.py` returns 1
    - `grep "min_length=3" src/ai_hedge_fund/schemas/debate.py` finds at least one match (BullCase.claims AND BearCase.claims)
    - `grep "min_length=2" src/ai_hedge_fund/schemas/debate.py` finds at least three matches (BearCase.addressed_bull_claims, RebuttalAct.bull_rebuttals, RebuttalAct.bear_rebuttals)
    - `uv run pytest tests/unit/test_debate_schemas.py::TestBearCase::test_rejects_fewer_than_two_addressed_bull_claims -x` passes
    - `uv run pytest tests/unit/test_debate_schemas.py::TestEmptyActRaisesValidationError -x` passes
    - `uv run pytest tests/unit/test_debate_schemas.py -x` reports 0 failures
    - `uv run python -c "from ai_hedge_fund.schemas import BullCase, BearCase, RebuttalAct, FinalArguments, DebateSynthesis, BullClaim, BearClaim, RebuttalPoint"` exits 0
    - `grep -c "\"DebateSynthesis\"" src/ai_hedge_fund/schemas/__init__.py` returns 1 (present in __all__)
    - `uv run ruff check src/ai_hedge_fund/schemas/debate.py tests/unit/test_debate_schemas.py` passes
  </acceptance_criteria>
  <done>All 8 models defined with correct min_length / ge / le / Literal constraints; ThesisOutput reused via import; every min_length-rejecting path is covered by a ValidationError test; re-exports updated; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bull_agent + bear_agent with zero tools, REASONING tier, enforced system prompts, and full wiring tests</name>
  <files>src/ai_hedge_fund/agents/bull.py, src/ai_hedge_fund/agents/bear.py, src/ai_hedge_fund/agents/__init__.py, tests/unit/test_bull_agent.py, tests/unit/test_bear_agent.py</files>
  <read_first>
    - src/ai_hedge_fund/agents/manager.py (complete file — the canonical zero-tools REASONING agent with format helper)
    - src/ai_hedge_fund/agents/__init__.py (re-export pattern)
    - src/ai_hedge_fund/agents/base.py lines 70-115 (get_usage_limits signature + overrides)
    - src/ai_hedge_fund/schemas/debate.py (just created in Task 1 — BullCase and BearCase definitions)
    - tests/unit/test_manager_agent.py (template for per-agent wiring tests)
    - .planning/phases/05-adversarial-critique/05-RESEARCH.md section "Pattern 1: Debate Agent Template" (lines 216-258) and "Example 1: Bear agent system prompt" (lines 590-612)
    - .planning/phases/05-adversarial-critique/05-PATTERNS.md sections "src/ai_hedge_fund/agents/bull.py" (lines 31-82) and "src/ai_hedge_fund/agents/bear.py" (lines 85-94) and "test_bull_agent.py" (lines 454-517)
  </read_first>
  <behavior>
    - `bull_agent` and `bear_agent` both exist as module-level `Agent[None, ...]` instances
    - `bull_agent.model.model_name` equals the REASONING-tier model name (same as `manager_agent.model.model_name` — do not hardcode; assert equality with `manager_agent.model.model_name`)
    - `bull_agent._output_type is BullCase` and `bear_agent._output_type is BearCase`
    - `bull_agent._max_result_retries == 2` and `bear_agent._max_result_retries == 2`
    - `len(bull_agent._function_toolset.tools) == 0` and `len(bear_agent._function_toolset.tools) == 0`
    - `BULL_SYSTEM_PROMPT` contains the substring "source_analyst" (literal) and either "at least 3" or "minimum 3"
    - `BEAR_SYSTEM_PROMPT` contains "addressed_bull_claims" AND "at least 2" (both literal) — this is how the DEBATE-02 contract is communicated to the LLM
    - `get_bull_limits()` and `get_bear_limits()` return UsageLimits whose total_tokens_limit equals REASONING-tier default (116_000)
    - `format_analyst_evidence(reports, thesis)` returns a non-empty string for non-empty reports and handles empty input gracefully (no IndexError)
    - `format_bull_case_for_bear(bull_case_dict)` emits bull claims as a numbered list so the bear agent can quote them verbatim
  </behavior>
  <action>
Create `src/ai_hedge_fund/agents/bull.py` — clone of `src/ai_hedge_fund/agents/manager.py` structure, with these exact deltas.

Module docstring: one-paragraph description of the Bull Advocate role, followed by:
```
Threat mitigations:
    T-05-03: Tampering (unsupported claim) -- BullCase.BullClaim.source_analyst is Literal,
             system prompt forbids claims without analyst evidence, retries=2 lets PydanticAI
             hand validation errors back to the model for self-correction.
    T-05-04: DoS -- UsageLimits via get_bull_limits() caps each run at REASONING tier.
```

Imports (exactly):
```python
from __future__ import annotations
import json
from pydantic_ai import Agent
from pydantic_ai.usage import UsageLimits
from ai_hedge_fund.agents.base import get_usage_limits
from ai_hedge_fund.models import ModelTier
from ai_hedge_fund.schemas.debate import BullCase
```

Define `BULL_SYSTEM_PROMPT` as a module-level constant (parenthesised multi-line string) containing VERBATIM (you may polish wording but keep the rule IDs and enumerated structure):
```
You are the Bull Advocate. Your task: argue for this company as a buy.

STRICT RULES:
1. You are given analyst reports AND a preliminary thesis. Build your case ONLY from evidence in those inputs. Do not invent facts or reference data outside what you are given.
2. Every claim you make MUST cite its source_analyst — one of 'fundamental', 'sentiment', 'technical', or 'manager'. Claims without source_analyst will be rejected by schema validation.
3. Cite specific data points (numbers, dates, filing sections). Vague claims like 'strong fundamentals' are forbidden.
4. Produce AT LEAST 3 bull claims. Outputs with fewer than 3 claims will be rejected.
5. You are advocating, not synthesizing — present the strongest possible buy case. The Bear Advocate will counter you.
6. Brevity is a feature. Cap your response at 3-5 high-quality claims; more is not better.
```

Define the agent and limits helper:
```python
bull_agent: Agent[None, BullCase] = Agent(
    ModelTier.REASONING.value,
    output_type=BullCase,
    system_prompt=BULL_SYSTEM_PROMPT,
    retries=2,
)

def get_bull_limits() -> UsageLimits:
    """Return UsageLimits for the Bull Advocate (REASONING tier, Opus)."""
    return get_usage_limits(ModelTier.REASONING)
```

Add `format_analyst_evidence(reports: list[dict], thesis: dict) -> str`:
- Accepts reports list (same shape as `format_analyst_reports` input) AND the manager's thesis dict.
- Returns a string with two sections separated by `"\n\n---\n\n"`: `"PRELIMINARY THESIS:\n{json.dumps(thesis, indent=2)}"` followed by `"ANALYST EVIDENCE:\n{format_analyst_reports-equivalent body}"`.
- Handle empty reports by returning `"No analyst evidence available."` as the second section.
- Handle `thesis=None` by returning `"No preliminary thesis available."` as the first section.
- Use `json.dumps(..., indent=2)` for the nested dicts (mirrors `format_analyst_reports`).
- Must be a pure function (no I/O, no state).

Now create `src/ai_hedge_fund/agents/bear.py` — same structure. Module docstring names T-05-05 (Tampering: addressed_bull_claims min_length=2 + Literal source_analyst) and T-05-06 (DoS: UsageLimits).

Imports identical except `from ai_hedge_fund.schemas.debate import BearCase`.

Define `BEAR_SYSTEM_PROMPT` with these rules (copy the text from 05-RESEARCH.md Example 1 lines 594-611 verbatim, then ensure both literal substrings "addressed_bull_claims" and "at least 2" appear). The prompt MUST tell the agent to quote bull-claim text verbatim into `addressed_bull_claims`.

Define `bear_agent: Agent[None, BearCase]` with `output_type=BearCase`, REASONING tier, retries=2, zero tools.

Define `get_bear_limits()` → `get_usage_limits(ModelTier.REASONING)`.

Add `format_bull_case_for_bear(bull_case: dict) -> str`:
- Accepts the bull_case dict (model_dump of BullCase).
- Returns a string that starts with `"BULL CASE TO REBUT:\n"` and emits each claim on a numbered line: `f"{i}. [{claim.source_analyst}] {claim.claim} — evidence: {claim.evidence}"`.
- Follows with `"HEADLINE: {bull_case['headline']}"`.
- Handles empty claims list by returning `"No bull case provided."`.
- Handles `bull_case=None` by returning `"No bull case provided."`.
- Pure function.

Update `src/ai_hedge_fund/agents/__init__.py`:
- Add imports (alphabetized within the existing block):
  ```python
  from ai_hedge_fund.agents.bear import bear_agent, format_bull_case_for_bear, get_bear_limits
  from ai_hedge_fund.agents.bull import bull_agent, format_analyst_evidence, get_bull_limits
  ```
- Add the 6 new names (`bear_agent`, `bull_agent`, `format_analyst_evidence`, `format_bull_case_for_bear`, `get_bear_limits`, `get_bull_limits`) to `__all__` in alphabetical order.

Now create `tests/unit/test_bull_agent.py` — clone of `tests/unit/test_manager_agent.py` with symbol substitution. Structure:

Imports (include the dummy API key BEFORE agent imports):
```python
from __future__ import annotations
import os
os.environ.setdefault("ANTHROPIC_API_KEY", "test-key-for-unit-tests")
from pydantic_ai import Agent
from pydantic_ai.usage import UsageLimits
from ai_hedge_fund.agents.bull import (
    BULL_SYSTEM_PROMPT, bull_agent, format_analyst_evidence, get_bull_limits,
)
from ai_hedge_fund.agents.manager import manager_agent  # for model-tier equality test
from ai_hedge_fund.schemas.debate import BullCase
```

Test classes:
1. `TestBullAgent`: test_agent_exists (isinstance Agent), test_agent_uses_reasoning_tier (assert `bull_agent.model.model_name == manager_agent.model.model_name` — robust to model version bumps), test_agent_output_type (`bull_agent._output_type is BullCase`), test_agent_has_retries (`bull_agent._max_result_retries == 2`), test_agent_no_tools (`len(bull_agent._function_toolset.tools) == 0`)
2. `TestBullSystemPrompt`: test_prompt_requires_source_analyst_citation (assert `"source_analyst" in BULL_SYSTEM_PROMPT`), test_prompt_forbids_vague_claims (assert `"vague" in BULL_SYSTEM_PROMPT.lower() or "specific" in BULL_SYSTEM_PROMPT.lower()`), test_prompt_requires_minimum_claims (assert `"at least 3" in BULL_SYSTEM_PROMPT.lower() or "minimum 3" in BULL_SYSTEM_PROMPT.lower()`), test_prompt_names_advocate_role (assert `"advocate" in BULL_SYSTEM_PROMPT.lower()` or `"advocating" in BULL_SYSTEM_PROMPT.lower()`)
3. `TestBullLimits`: test_returns_usage_limits_instance (isinstance UsageLimits), test_total_limit_matches_reasoning_tier (assert `get_bull_limits().total_tokens_limit == get_usage_limits(ModelTier.REASONING).total_tokens_limit` — import ModelTier + get_usage_limits from base)
4. `TestFormatAnalystEvidence`:
   - test_empty_reports_and_none_thesis (returns string that contains "No analyst evidence" AND "No preliminary thesis")
   - test_empty_reports_with_thesis (returns string containing thesis JSON)
   - test_single_report (contains analyst name and JSON of analysis)
   - test_multi_report_separator (divider `"---"` appears exactly once between the two sections when thesis is present)
   - test_error_report_branch (report with `error` key emits "UNAVAILABLE" — clone pattern from manager.format_analyst_reports tests if that test exists; otherwise assert the error string appears in output)

Now create `tests/unit/test_bear_agent.py` with the same structure. Additional Bear-specific test:
- `TestBearSystemPrompt::test_prompt_requires_addressing_two_bull_claims`: assert `"addressed_bull_claims" in BEAR_SYSTEM_PROMPT AND "at least 2" in BEAR_SYSTEM_PROMPT.lower()`. Use BOTH substring checks combined with AND so prompt wording is unambiguous.
- `TestFormatBullCaseForBear`: test_empty_claims (returns "No bull case provided."), test_none_input (returns "No bull case provided."), test_numbered_list_format (claims appear as `"1. "`, `"2. "`, etc.), test_includes_source_analyst (analyst label in bracketed prefix), test_includes_headline_line (string contains "HEADLINE:").

All tests use `asyncio.run(_invoke())` pattern if they call async functions; but these tests only inspect agent attributes and call pure helpers, so no async needed.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_bull_agent.py tests/unit/test_bear_agent.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bull_agent: Agent\[None, BullCase\]" src/ai_hedge_fund/agents/bull.py` returns 1
    - `grep -c "bear_agent: Agent\[None, BearCase\]" src/ai_hedge_fund/agents/bear.py` returns 1
    - `grep -c "ModelTier.REASONING.value" src/ai_hedge_fund/agents/bull.py src/ai_hedge_fund/agents/bear.py` returns 2 (one per file)
    - `grep -c "retries=2" src/ai_hedge_fund/agents/bull.py src/ai_hedge_fund/agents/bear.py` returns 2
    - `grep -c "source_analyst" src/ai_hedge_fund/agents/bull.py` returns at least 1 (in the system prompt)
    - `grep -c "addressed_bull_claims" src/ai_hedge_fund/agents/bear.py` returns at least 1 (in the system prompt)
    - `grep -c "at least 2" src/ai_hedge_fund/agents/bear.py` returns at least 1 (verbatim phrase in the system prompt)
    - `grep -c "tools=" src/ai_hedge_fund/agents/bull.py src/ai_hedge_fund/agents/bear.py` returns 0 (zero-tools — the kwarg must NOT be passed to Agent)
    - `uv run pytest tests/unit/test_bull_agent.py -x` reports 0 failures
    - `uv run pytest tests/unit/test_bear_agent.py -x` reports 0 failures
    - `uv run python -c "from ai_hedge_fund.agents import bull_agent, bear_agent, get_bull_limits, get_bear_limits, format_analyst_evidence, format_bull_case_for_bear"` exits 0
    - `grep -c "\"bear_agent\"\|\"bull_agent\"" src/ai_hedge_fund/agents/__init__.py` returns 2 (both in __all__)
    - `uv run ruff check src/ai_hedge_fund/agents/bull.py src/ai_hedge_fund/agents/bear.py tests/unit/test_bull_agent.py tests/unit/test_bear_agent.py` passes
    - `uv run pytest tests/unit/ -x --ff` passes end-to-end (no regressions in Phase 1-4 tests)
  </acceptance_criteria>
  <done>Bull and Bear agents wired identically to manager_agent (REASONING tier, retries=2, zero tools, typed output), system prompts contain the DEBATE-01/DEBATE-02 enforcement language verbatim, format helpers are pure and tested, re-exports alphabetized, full unit-test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyst report → Debate agent | Structured analyst outputs (validated by Phase-4 schemas) flow into bull/bear agents. No untrusted user input crosses here; LLM outputs are schema-validated on emit. |
| LLM output → Pipeline state | BullCase / BearCase dicts from the LLM are validated by Pydantic before being written to LangGraph state. Invalid outputs trigger retries and (after 2 retries) raise `UnexpectedModelBehavior`. |

Phase 5 adds **no new external network endpoints, no new user-input parsing, no new secret surface**. The only relevant attack vector is LLM-output tampering / prompt-injection via analyst reports; this is bounded by schema-enforced outputs (no free-form text escapes the schema).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `src/ai_hedge_fund/schemas/debate.py` (act outputs) | mitigate | Pydantic `min_length` on every substantive list; Literal type on `source_analyst` prevents fabricated analyst names; `ge/le` on all score fields rejects out-of-range integers. |
| T-05-02 | Tampering (short-form) | Debate schemas | mitigate | `addressed_bull_claims: list[str] = Field(min_length=2)` on BearCase — schema enforces DEBATE-02 count. Verbatim-string enforcement is system-prompt only (see A3 in RESEARCH.md); semantic alignment verified at UAT. |
| T-05-03 | Tampering (unsupported claim) | `bull_agent` | mitigate | System prompt forbids invented facts; retries=2 lets PydanticAI hand validation errors back for self-correction; `BullClaim.source_analyst` Literal restricts to 4 valid analyst names. |
| T-05-04 | DoS | `bull_agent` | mitigate | `get_bull_limits()` caps per-run tokens at REASONING tier (116k total); LangGraph node wraps `agent.run(usage_limits=limits)` in try/except UsageLimitExceeded (covered in Plan 05-03). |
| T-05-05 | Tampering (sycophancy / agreement) | `bear_agent` | mitigate | System prompt explicitly requires verbatim quotation of bull claims into `addressed_bull_claims`; BearCase schema rejects <2 claims. Pitfall 1 mitigation — role-priming in prompt makes the adversarial stance explicit. |
| T-05-06 | DoS | `bear_agent` | mitigate | `get_bear_limits()` caps per-run tokens at REASONING tier (116k total). |
| T-05-07 | Prompt injection via analyst reports | bull/bear input formatting | accept | Analyst reports are already validated by Phase-4 Pydantic schemas before reaching state. Bull/bear read state dicts (not raw web content), so injection vector is bounded to what an analyst LLM could craft — mitigated by the analyst's own schema constraints. No PII, no secrets involved. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_debate_schemas.py tests/unit/test_bull_agent.py tests/unit/test_bear_agent.py -x` passes
- `uv run pytest tests/unit/ -x --ff` passes (no regression to Phase 1-4)
- `uv run python -c "from ai_hedge_fund.schemas import BullCase, BearCase, RebuttalAct, FinalArguments, DebateSynthesis; from ai_hedge_fund.agents import bull_agent, bear_agent"` exits 0
- `uv run ruff check src/ai_hedge_fund/schemas/debate.py src/ai_hedge_fund/agents/bull.py src/ai_hedge_fund/agents/bear.py tests/unit/test_debate_schemas.py tests/unit/test_bull_agent.py tests/unit/test_bear_agent.py` passes
- `uv run ruff format --check src/ai_hedge_fund/schemas/debate.py src/ai_hedge_fund/agents/bull.py src/ai_hedge_fund/agents/bear.py` passes
</verification>

<success_criteria>
- All 8 debate schemas exist in `src/ai_hedge_fund/schemas/debate.py` with correct constraints
- `DebateSynthesis.revised_thesis` reuses `ThesisOutput` via import (not redefined)
- `BearCase.addressed_bull_claims` has `min_length=2` — DEBATE-02 enforcement mechanism
- `BULL_SYSTEM_PROMPT` contains "source_analyst" (DEBATE-01 enforcement)
- `BEAR_SYSTEM_PROMPT` contains both "addressed_bull_claims" and "at least 2" (DEBATE-02 enforcement)
- Both agents are REASONING tier, retries=2, zero tools (mirrors manager_agent)
- All new symbols re-exported from `schemas/__init__.py` and `agents/__init__.py` in alphabetical order
- Every new file has `from __future__ import annotations` as the first non-docstring line
- `TestEmptyActRaisesValidationError` class in `test_debate_schemas.py` explicitly documents DEBATE-03 success criterion 3 ("skipping an act or producing an empty act causes a validation error")
- Full unit suite green, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-adversarial-critique/05-01-SUMMARY.md` documenting:
- What was built (schemas + 2 agents + tests)
- Files created/modified (per frontmatter)
- Key decisions locked in (e.g., quality_score weights deferred to Plan 05-02, AnalystName Literal includes "manager")
- Any deviations from RESEARCH.md (expect none)
- Test counts added
- Explicit note that DEBATE-01 and DEBATE-02 schema-level enforcement are now in place; DEBATE-03 (5-act protocol) remains partially covered — Rebuttal/Final/Synthesis schemas exist but are not yet agent-backed (comes in Plan 05-02)
</output>

---
phase: 06-risk-management
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/ai_hedge_fund/risk/checks.py
  - src/ai_hedge_fund/risk/correlation.py
  - src/ai_hedge_fund/risk/drawdown.py
  - src/ai_hedge_fund/risk/sizing.py
  - src/ai_hedge_fund/risk/__init__.py
  - tests/risk/fixtures/returns_golden.csv
  - tests/risk/test_position_size.py
  - tests/risk/test_sector_check.py
  - tests/risk/test_correlation.py
  - tests/risk/test_drawdown.py
  - tests/risk/test_sizing.py
autonomous: true
requirements:
  - RISK-02
  - RISK-03
must_haves:
  truths:
    - "check_position_size rejects signals whose candidate position_size_pct exceeds RiskPolicy.max_single_position_pct (RISK-02 hard limit — not silent cap)"
    - "check_sector_concentration rejects when post-trade sector weight exceeds RiskPolicy.max_sector_pct"
    - "compute_max_correlation_with_portfolio returns (top_ticker, corr) using pandas Pearson on daily log returns"
    - "project_max_drawdown_pct returns (drawdown_pct, error_name_or_None) using historical simulation over candidate+portfolio weighted returns"
    - "derive_candidate_size_pct(thesis, policy) maps high/medium/low conviction to max_single_position_pct * {1.0, 0.5, 0.25} (A4)"
    - "Each check function returns Violation | None; first violation wins (caller short-circuits)"
    - "Insufficient price history (<min_history_days) returns insufficient_price_history Violation on drawdown (Pitfall 4)"
    - "Division-by-zero and NaN paths are explicitly guarded (T-06-05)"
  artifacts:
    - path: "src/ai_hedge_fund/risk/checks.py"
      provides: "check_position_size, check_sector_concentration, check_exclusions"
      contains: "def check_position_size"
    - path: "src/ai_hedge_fund/risk/correlation.py"
      provides: "compute_max_correlation_with_portfolio + check_correlation"
      contains: "def compute_max_correlation_with_portfolio"
    - path: "src/ai_hedge_fund/risk/drawdown.py"
      provides: "project_max_drawdown_pct + check_drawdown"
      contains: "def project_max_drawdown_pct"
    - path: "src/ai_hedge_fund/risk/sizing.py"
      provides: "derive_candidate_size_pct"
    - path: "tests/risk/fixtures/returns_golden.csv"
      provides: "Deterministic 60+ day return series for 6 tickers"
  key_links:
    - from: "src/ai_hedge_fund/risk/correlation.py"
      to: "pandas.DataFrame.corr"
      via: "Pearson on daily log returns"
      pattern: "\\.corr\\(\\)"
    - from: "src/ai_hedge_fund/risk/drawdown.py"
      to: "numpy.maximum.accumulate"
      via: "cumulative-max drawdown"
      pattern: "np\\.maximum\\.accumulate"
---

<objective>
Implement the four deterministic risk checks as pure Python functions (tool-first per CLAUDE.md). These are the heart of Phase 6 — the LLM never computes any of these values. The node (plan 06-05) will call them in sequence and emit the first violation.

Purpose: RISK-02 (position-size hard limit) and RISK-03 (sector concentration, correlation, drawdown) are entirely satisfied at the math layer here. The node glues these together; the agent writes rationale. This plan is the computational core.

Output: Four new modules (checks, correlation, drawdown, sizing), one golden-return fixture, five test files. Every function is pure (no I/O, no DB session, no LLM).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-risk-management/06-RESEARCH.md
@.planning/phases/06-risk-management/06-VALIDATION.md
@.planning/phases/06-risk-management/06-01-PLAN.md
@.planning/phases/06-risk-management/06-02-PLAN.md
@CLAUDE.md

# Prior plan summaries
@.planning/phases/06-risk-management/06-01-SUMMARY.md
@.planning/phases/06-risk-management/06-02-SUMMARY.md

# Codebase analogs
@src/ai_hedge_fund/data/tools/price_tools.py

<interfaces>
<!-- From plan 06-01 -->
```python
from ai_hedge_fund.risk.policy import RiskPolicy
from ai_hedge_fund.schemas.risk import Violation, CONSTRAINT_NAMES
```

<!-- From plan 06-02 -->
```python
from ai_hedge_fund.risk.portfolio import PortfolioSnapshot, PortfolioSnapshotPosition
# PortfolioSnapshot has: positions, tickers(), weights(), sector_weight_pct(), position_value_pct()
```

<!-- From src/ai_hedge_fund/schemas/agents.py -->
```python
class SignalOutput(BaseModel):
    ticker: str
    direction: Literal["long", "short", "neutral"]
    conviction: Literal["low", "medium", "high"]
    position_size_pct: float  # ge=0, le=100
    ...
# But for Phase 6 risk checks we use the upstream thesis's conviction + policy to DERIVE
# the candidate size (signal_node runs AFTER risk_manager_node — see research Data flow step 2).
# Inputs to check functions are: candidate_ticker, candidate_sector, candidate_size_pct, portfolio, policy.
```

<!-- From 06-RESEARCH.md Pattern 4 -->
```python
def project_max_drawdown_pct(weights: dict[str, float], returns: pd.DataFrame) -> float:
    aligned = returns[list(weights)].dropna()
    weight_vec = np.array([weights[t] for t in aligned.columns])
    port_daily_ret = (aligned.values @ weight_vec)
    equity_curve = np.cumprod(1.0 + port_daily_ret)
    running_max = np.maximum.accumulate(equity_curve)
    drawdown = (running_max - equity_curve) / running_max
    return float(np.max(drawdown) * 100.0)
```

<!-- From 06-RESEARCH.md Pattern 5 -->
```python
def compute_max_correlation_with_portfolio(candidate_ticker, portfolio_tickers, returns) -> tuple[str, float]:
    aligned = returns[[candidate_ticker, *portfolio_tickers]].dropna()
    if len(aligned) < 20: return ("", 0.0)
    corrs = aligned.corr()[candidate_ticker].drop(candidate_ticker)
    top = corrs.abs().idxmax()
    return (top, float(corrs[top]))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Position-size, sector-concentration, exclusion checks + conviction→size</name>
  <files>src/ai_hedge_fund/risk/checks.py, src/ai_hedge_fund/risk/sizing.py, src/ai_hedge_fund/risk/__init__.py, tests/risk/test_position_size.py, tests/risk/test_sector_check.py, tests/risk/test_sizing.py</files>
  <read_first>
    - .planning/phases/06-risk-management/06-RESEARCH.md Architectural Responsibility Map + Pitfall 6 (pre-trade convention)
    - .planning/phases/06-risk-management/06-RESEARCH.md Assumption A4 (conviction→size fixed rule)
    - src/ai_hedge_fund/risk/policy.py (from 06-01 — fields: max_single_position_pct, max_sector_pct, excluded_instrument_types, excluded_sectors, size_*_multipliers)
    - src/ai_hedge_fund/risk/portfolio.py (from 06-02 — PortfolioSnapshot methods)
    - src/ai_hedge_fund/schemas/risk.py (from 06-01 — Violation, CONSTRAINT_NAMES)
  </read_first>
  <behavior>
    - Test 1: check_position_size with candidate 5% and policy 10% returns None (approved)
    - Test 2: check_position_size with candidate 15% and policy 10% returns Violation(name="max_single_position_pct", observed=15.0, limit=10.0) — NOT a silently-capped 10%
    - Test 3: check_position_size with candidate EXACTLY at the limit (10% vs 10%) returns None (inclusive)
    - Test 4: check_sector_concentration: portfolio already 20% Technology, candidate adds 7% more Technology, policy max 30% → None (27% < 30%)
    - Test 5: check_sector_concentration: portfolio 25% Technology, candidate adds 7% Technology, policy 30% → Violation(name="max_sector_pct", observed=32.0, limit=30.0). Pre-trade convention (Pitfall 6) asserted.
    - Test 6: check_sector_concentration on empty portfolio (new sector) with candidate 10% Healthcare → None (10% < 30%)
    - Test 7: check_exclusions: instrument_type "SPAC" when policy excluded_instrument_types contains "SPAC" → Violation(name="excluded_instrument_type", observed=1.0, limit=0.0)
    - Test 8: check_exclusions: sector "Energy" when policy excluded_sectors contains "Energy" → Violation(name="excluded_sector", ...)
    - Test 9: check_exclusions on non-excluded items → None
    - Test 10: derive_candidate_size_pct(conviction="high", policy) = policy.max_single_position_pct * 1.0 = 10.0 (default policy)
    - Test 11: derive_candidate_size_pct(conviction="medium", policy) = 5.0
    - Test 12: derive_candidate_size_pct(conviction="low", policy) = 2.5
    - Test 13: derive_candidate_size_pct with an unrecognised conviction raises ValueError
  </behavior>
  <action>
    Create `src/ai_hedge_fund/risk/sizing.py`:
    - `from __future__ import annotations`
    - `from typing import Literal`
    - `from ai_hedge_fund.risk.policy import RiskPolicy`
    - Function `derive_candidate_size_pct(conviction: Literal["low", "medium", "high"], policy: RiskPolicy) -> float`:
      - Map `"high"` → `policy.max_single_position_pct * policy.size_high_conviction_multiplier`
      - Map `"medium"` → `policy.max_single_position_pct * policy.size_medium_conviction_multiplier`
      - Map `"low"` → `policy.max_single_position_pct * policy.size_low_conviction_multiplier`
      - Any other value → `raise ValueError(f"Unknown conviction {conviction!r}; expected low/medium/high")`
      - Docstring cites A4 from research.
      - <50 lines.

    Create `src/ai_hedge_fund/risk/checks.py`:
    - `from __future__ import annotations`
    - Imports: `from ai_hedge_fund.risk.policy import RiskPolicy; from ai_hedge_fund.risk.portfolio import PortfolioSnapshot; from ai_hedge_fund.schemas.risk import Violation`
    - Function `check_position_size(candidate_size_pct: float, policy: RiskPolicy) -> Violation | None`:
      - If `candidate_size_pct > policy.max_single_position_pct`: return Violation(name="max_single_position_pct", observed=candidate_size_pct, limit=policy.max_single_position_pct)
      - Else: return None
      - Docstring notes: "Pitfall — never silently cap. RISK-02 requires REJECTION. If you're tempted to write `min(candidate, limit)`, you are violating the success criterion."
    - Function `check_sector_concentration(candidate_sector: str, candidate_size_pct: float, portfolio: PortfolioSnapshot, policy: RiskPolicy) -> Violation | None`:
      - Pre-trade convention (Pitfall 6): post_sector_pct = portfolio.sector_weight_pct(candidate_sector) + candidate_size_pct
      - If `post_sector_pct > policy.max_sector_pct`: return Violation(name="max_sector_pct", observed=post_sector_pct, limit=policy.max_sector_pct)
      - Else: return None
    - Function `check_exclusions(candidate_sector: str, candidate_instrument_type: str, policy: RiskPolicy) -> Violation | None`:
      - If candidate_instrument_type in policy.excluded_instrument_types: return Violation(name="excluded_instrument_type", observed=1.0, limit=0.0)
      - If candidate_sector in policy.excluded_sectors: return Violation(name="excluded_sector", observed=1.0, limit=0.0)
      - Else: return None
    - Every function <50 lines.

    Update `src/ai_hedge_fund/risk/__init__.py` to re-export: `check_position_size`, `check_sector_concentration`, `check_exclusions`, `derive_candidate_size_pct`. Keep `__all__` alphabetised.

    Create three test files — `test_position_size.py` (tests 1-3), `test_sector_check.py` (tests 4-6 + exclusions 7-9 since both live in checks.py), `test_sizing.py` (tests 10-13). Use the `sample_policy` fixture from conftest. For portfolio fixtures build `PortfolioSnapshot` directly in-test (no DB needed — check functions are pure).

    Run ruff format + check on all new files.
  </action>
  <verify>
    <automated>uv run pytest tests/risk/test_position_size.py tests/risk/test_sector_check.py tests/risk/test_sizing.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def check_position_size" src/ai_hedge_fund/risk/checks.py`
    - `grep -q "def check_sector_concentration" src/ai_hedge_fund/risk/checks.py`
    - `grep -q "def derive_candidate_size_pct" src/ai_hedge_fund/risk/sizing.py`
    - Files contain NO `min(` applied to position size (verify no silent cap: `! grep -E "min\(.*candidate_size_pct.*policy\.max" src/ai_hedge_fund/risk/checks.py`)
    - All 13 tests pass (4 position, 3 sector + 3 exclusion, 4 sizing) — verify ≥13 tests collected in the combined run
    - Ruff clean
    - Functions all <50 lines (verify each function body)
  </acceptance_criteria>
  <done>Position-size hard limit, sector concentration, exclusions, and conviction→size mapping all implemented and tested. RISK-02 math layer complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correlation + drawdown (numpy/pandas) + golden-returns fixture</name>
  <files>src/ai_hedge_fund/risk/correlation.py, src/ai_hedge_fund/risk/drawdown.py, src/ai_hedge_fund/risk/__init__.py, tests/risk/fixtures/returns_golden.csv, tests/risk/test_correlation.py, tests/risk/test_drawdown.py</files>
  <read_first>
    - .planning/phases/06-risk-management/06-RESEARCH.md Pattern 4 (drawdown idiom)
    - .planning/phases/06-risk-management/06-RESEARCH.md Pattern 5 (correlation idiom)
    - .planning/phases/06-risk-management/06-RESEARCH.md Pitfall 3 (empty portfolio edge case — drawdown evaluates candidate-only, NOT short-circuit to 0)
    - .planning/phases/06-risk-management/06-RESEARCH.md Pitfall 4 (insufficient history vetoes drawdown)
    - .planning/phases/06-risk-management/06-RESEARCH.md Example 3 (drawdown with error-name return)
    - src/ai_hedge_fund/data/tools/price_tools.py (`_compute_stats` for the numpy/pandas idiom used elsewhere in the codebase)
  </read_first>
  <behavior>
    Correlation tests (test_correlation.py):
    - Test 1: Golden fixture has a pre-computed correlation — e.g., seed perfectly-correlated series (ticker A = ticker B*2 + noise). Assert compute_max_correlation_with_portfolio returns (B, ~1.0) for candidate A with portfolio [B].
    - Test 2: Empty portfolio_tickers returns ("", 0.0)
    - Test 3: <20 aligned days returns ("", 0.0) AND emits a structlog warning (assert via caplog or a log-capturing fixture)
    - Test 4: check_correlation returns Violation when max corr > policy.max_correlation_with_portfolio
    - Test 5: check_correlation returns None when max corr <= limit

    Drawdown tests (test_drawdown.py):
    - Test 6: Golden fixture — known 252-day sequence with peak-to-trough 15.0% drawdown at known weights. Assert project_max_drawdown_pct returns (15.0 ± 0.5, None). Use exact fixture.
    - Test 7: If any ticker has < policy.min_history_days data, returns (0.0, "insufficient_price_history")
    - Test 8: Pitfall 3 — empty-portfolio + candidate 100% weight returns candidate's solo drawdown (NOT 0.0). Assert >0.
    - Test 9: Division-by-zero guard: a returns series of all zeros produces equity_curve of all 1.0, running_max / curve = 1.0, drawdown = 0.0 (no NaN, no DivisionError). T-06-05.
    - Test 10: check_drawdown returns Violation when projected drawdown > policy.max_projected_drawdown_pct
    - Test 11: check_drawdown short-circuits when drawdown error is "insufficient_price_history" → returns Violation(name="insufficient_price_history", observed=0.0, limit=float(policy.min_history_days))
  </behavior>
  <action>
    Create `tests/risk/fixtures/returns_golden.csv` — a deterministic CSV with 6 tickers (AAPL, MSFT, JNJ, JPM, PG, NEW) and 252 trading days of daily log returns. Build it programmatically in a small one-time generator script or hand-seeded:
    - Use numpy with a fixed seed (`np.random.default_rng(42)`) to generate deterministic Gaussian returns per ticker
    - For ticker MSFT, make it correlate ~0.95 with AAPL (compute MSFT = 0.95*AAPL + 0.05*independent_noise)
    - Ensure NEW has only 30 rows (to test insufficient history)
    - Format: header `date,AAPL,MSFT,JNJ,JPM,PG,NEW` and 252 data rows with log-return floats
    - The executor may choose to commit a small generator script `tests/risk/fixtures/_generate_returns_golden.py` that produces this CSV reproducibly; keep it under 50 lines.

    Create `src/ai_hedge_fund/risk/correlation.py`:
    - `from __future__ import annotations`
    - Imports: `structlog`, `numpy as np`, `pandas as pd`, `from ai_hedge_fund.risk.policy import RiskPolicy; from ai_hedge_fund.risk.portfolio import PortfolioSnapshot; from ai_hedge_fund.schemas.risk import Violation`
    - `logger = structlog.get_logger(__name__)`
    - Function `compute_max_correlation_with_portfolio(candidate_ticker: str, portfolio_tickers: list[str], returns: pd.DataFrame, min_window: int = 20) -> tuple[str, float]`:
      - If not portfolio_tickers: return ("", 0.0)
      - If candidate_ticker not in returns.columns or any pt not in returns.columns: logger.warning(...) and return ("", 0.0) (fail-safe per Pitfall 4 — correlation fails OPEN with log)
      - Filter `aligned = returns[[candidate_ticker, *portfolio_tickers]].dropna()`
      - If len(aligned) < min_window: logger.warning("correlation_insufficient_data", window=len(aligned), min=min_window); return ("", 0.0)
      - `corrs = aligned.corr()[candidate_ticker].drop(candidate_ticker)`
      - `top_ticker = corrs.abs().idxmax()`
      - Return (str(top_ticker), float(corrs[top_ticker]))
    - Function `check_correlation(candidate_ticker: str, portfolio: PortfolioSnapshot, returns: pd.DataFrame, policy: RiskPolicy) -> Violation | None`:
      - Call compute_max_correlation_with_portfolio(...) with policy.correlation_window_days as the min_window override? No — the min_window parameter caps at 20 per Pattern 5; use the default. Window length is controlled by the `returns` dataframe shape supplied by caller.
      - If abs(corr) > policy.max_correlation_with_portfolio: return Violation(name="max_correlation_with_portfolio", observed=abs(corr), limit=policy.max_correlation_with_portfolio)
      - Else: return None

    Create `src/ai_hedge_fund/risk/drawdown.py`:
    - `from __future__ import annotations`
    - Imports: `numpy as np`, `pandas as pd`, `from ai_hedge_fund.risk.policy import RiskPolicy; from ai_hedge_fund.schemas.risk import Violation`
    - Function `project_max_drawdown_pct(weights: dict[str, float], returns: pd.DataFrame, policy: RiskPolicy) -> tuple[float, str | None]` (exact signature from RESEARCH Example 3):
      - For each ticker in weights: if ticker not in returns.columns OR returns[ticker].count() < policy.min_history_days: return (0.0, "insufficient_price_history")
      - `aligned = returns[list(weights)].dropna()`
      - If len(aligned) < policy.min_history_days: return (0.0, "insufficient_price_history")
      - `weight_vec = np.array([weights[t] for t in aligned.columns])`
      - `port_daily_ret = aligned.values @ weight_vec`  # shape (n_days,)
      - `equity_curve = np.cumprod(1.0 + port_daily_ret)`
      - `running_max = np.maximum.accumulate(equity_curve)`
      - Guard against div-by-zero (T-06-05): `safe_max = np.where(running_max == 0, 1.0, running_max)`
      - `drawdown = (running_max - equity_curve) / safe_max`
      - Replace any NaN with 0: `drawdown = np.nan_to_num(drawdown, nan=0.0, posinf=0.0, neginf=0.0)`
      - Return (float(np.max(drawdown) * 100.0), None)
    - Function `check_drawdown(candidate_ticker: str, candidate_size_pct: float, portfolio: PortfolioSnapshot, returns: pd.DataFrame, policy: RiskPolicy) -> Violation | None`:
      - Build post-trade weights: for each position in portfolio.positions compute `(value_cents / total_value_cents) * (1 - candidate_size_pct/100)`; add `candidate_ticker: candidate_size_pct/100` (Pitfall 3 — empty portfolio + candidate = candidate-only)
      - Handle empty portfolio: if portfolio.total_value_cents == 0: weights = {candidate_ticker: 1.0} (full weight of candidate as the post-trade portfolio — Pitfall 3)
      - Call project_max_drawdown_pct(weights, returns, policy)
      - If err == "insufficient_price_history": return Violation(name="insufficient_price_history", observed=0.0, limit=float(policy.min_history_days))
      - If drawdown_pct > policy.max_projected_drawdown_pct: return Violation(name="max_projected_drawdown_pct", observed=drawdown_pct, limit=policy.max_projected_drawdown_pct)
      - Else: return None

    Update `src/ai_hedge_fund/risk/__init__.py` to re-export: `compute_max_correlation_with_portfolio`, `check_correlation`, `project_max_drawdown_pct`, `check_drawdown`. Alphabetise `__all__`.

    Create `tests/risk/test_correlation.py` (tests 1-5) and `tests/risk/test_drawdown.py` (tests 6-11). Load the golden CSV with `pd.read_csv(FIXTURES_DIR / "returns_golden.csv", index_col=0, parse_dates=True)`. Use the `sample_policy` fixture from conftest. For test 3 use `caplog` or a structlog test helper; if fiddly, capture via `monkeypatch`-replaced logger. For test 11 synthesize a returns DataFrame where the candidate ticker has only 5 rows.

    Run ruff format + check on all new files.
  </action>
  <verify>
    <automated>uv run pytest tests/risk/test_correlation.py tests/risk/test_drawdown.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def compute_max_correlation_with_portfolio" src/ai_hedge_fund/risk/correlation.py`
    - `grep -q "def project_max_drawdown_pct" src/ai_hedge_fund/risk/drawdown.py`
    - `grep -q "np.maximum.accumulate" src/ai_hedge_fund/risk/drawdown.py`
    - `grep -q "aligned.corr()" src/ai_hedge_fund/risk/correlation.py`
    - `grep -q "nan_to_num\|safe_max" src/ai_hedge_fund/risk/drawdown.py` (T-06-05 evidence)
    - `tests/risk/fixtures/returns_golden.csv` exists with 252+ rows and 6 columns plus header (verify: `head -1 returns_golden.csv` contains 7 comma-separated names; `wc -l` ≥253)
    - All 11 tests pass (5 correlation + 6 drawdown)
    - Ruff clean
  </acceptance_criteria>
  <done>Correlation + drawdown pure-Python math complete with NaN/div-zero guards, golden-value fixture committed, 11 tests passing. RISK-03 math layer complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inputs (signal + portfolio + returns) -> pure-math checks | No I/O, all data pre-loaded by caller |
| Numeric -> Violation schema | Checks emit typed Violations validated by pydantic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | DoS / wrong result (div-by-zero, NaN) | `drawdown.py::project_max_drawdown_pct` | mitigate | Explicit `np.where(running_max == 0, 1.0, ...)` guard + `np.nan_to_num(...)` sanitisation + unit test 9 seeds all-zero returns to prove the guard. |
| T-06-05b | Wrong result (correlation on empty data) | `correlation.py::compute_max_correlation_with_portfolio` | mitigate | Early return `("", 0.0)` when portfolio empty or aligned length < min_window, with structlog warning. Unit tests 2+3 prove behaviour. |
| T-06-02 | Veto bypass (silent capping instead of rejecting) | `check_position_size` | mitigate | Docstring explicitly forbids capping; grep-check in acceptance criteria asserts no `min(candidate, limit)` pattern. Test 2 asserts rejection (not cap). |
| T-06-07 | Sector double-counting (Pitfall 6) | `check_sector_concentration` | mitigate | Pre-trade convention documented; tests 4+5 assert the math uses current_sector_pct + candidate_pct (never candidate counted twice). |
</threat_model>

<verification>
`uv run pytest tests/risk -q` passes all tests from plans 06-01/02/03 together. `uv run ruff check src/ai_hedge_fund/risk/` clean. Every function under 50 lines (CLAUDE.md) — verify by inspection. No function imports `sqlalchemy` or any HTTP client — these are pure-Python helpers.
</verification>

<success_criteria>
- Four risk-check modules (checks, correlation, drawdown, sizing) each with pure functions
- Every check returns `Violation | None` — caller decides first-wins short-circuit
- Golden-returns fixture committed and used by deterministic tests
- ≥24 tests passing across all 5 new test files in this plan (13 in task 1 + 11 in task 2)
- RISK-02 math layer complete (position-size hard limit proven)
- RISK-03 math layer complete (sector, correlation, drawdown proven)
</success_criteria>

<output>
Create `.planning/phases/06-risk-management/06-03-SUMMARY.md`. Key fields:
- provides: `ai_hedge_fund.risk.checks.check_position_size`, `check_sector_concentration`, `check_exclusions`, `ai_hedge_fund.risk.correlation.compute_max_correlation_with_portfolio`, `check_correlation`, `ai_hedge_fund.risk.drawdown.project_max_drawdown_pct`, `check_drawdown`, `ai_hedge_fund.risk.sizing.derive_candidate_size_pct`, `tests/risk/fixtures/returns_golden.csv`
- requirements-completed: partial RISK-02 (math layer), partial RISK-03 (math layer). Full completion requires 06-05 + 06-06 wiring.
</output>

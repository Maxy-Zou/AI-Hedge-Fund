---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/ai_hedge_fund/__init__.py
  - src/ai_hedge_fund/config.py
  - src/ai_hedge_fund/logging.py
  - src/ai_hedge_fund/models.py
  - src/ai_hedge_fund/db/__init__.py
  - src/ai_hedge_fund/db/base.py
  - src/ai_hedge_fund/db/session.py
  - src/ai_hedge_fund/schemas/__init__.py
  - src/ai_hedge_fund/schemas/state.py
  - src/ai_hedge_fund/schemas/agents.py
  - .env.example
  - tests/__init__.py
  - tests/conftest.py
  - tests/unit/__init__.py
  - tests/unit/test_config.py
  - tests/unit/test_schemas.py
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "uv sync installs all dependencies without errors"
    - "Importing ai_hedge_fund works (package is installable)"
    - "AppSettings loads from .env with correct defaults"
    - "ModelTier enum contains three tiers with correct Claude model IDs"
    - "PipelineState TypedDict and agent output BaseModels validate correctly"
    - "structlog produces JSON-formatted log output"
    - "Database engine connects to PostgreSQL via docker-compose"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package definition with all dependencies"
      contains: "langgraph"
    - path: "src/ai_hedge_fund/config.py"
      provides: "Pydantic settings with env loading"
      contains: "class AppSettings"
    - path: "src/ai_hedge_fund/models.py"
      provides: "Claude model routing configuration"
      contains: "class ModelTier"
    - path: "src/ai_hedge_fund/schemas/state.py"
      provides: "LangGraph state TypedDict"
      contains: "class PipelineState"
    - path: "src/ai_hedge_fund/schemas/agents.py"
      provides: "PydanticAI output schemas"
      contains: "class ExtractionOutput"
    - path: "src/ai_hedge_fund/db/session.py"
      provides: "SQLAlchemy engine and session factory"
      contains: "def get_engine"
    - path: "tests/unit/test_config.py"
      provides: "Config and model tests"
      min_lines: 30
    - path: "tests/unit/test_schemas.py"
      provides: "Schema validation tests"
      min_lines: 40
  key_links:
    - from: "src/ai_hedge_fund/config.py"
      to: ".env"
      via: "pydantic-settings env loading"
      pattern: "class AppSettings.*BaseSettings"
    - from: "src/ai_hedge_fund/db/session.py"
      to: "src/ai_hedge_fund/config.py"
      via: "database_url from settings"
      pattern: "settings\\.database_url"
---

<objective>
Scaffold the AI Hedge Fund Python project with all dependencies, configuration, database connectivity, Claude model routing definitions, and typed schemas for the LangGraph pipeline.

Purpose: Every subsequent plan and phase depends on importable package structure, validated configuration, model routing, and typed schemas. This is the foundation layer.

Output: Installable Python package (src/ai_hedge_fund/) with config, logging, DB session factory, model tier enum, LangGraph state TypedDict, PydanticAI output BaseModels, and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, dependencies, config, models, logging, DB session</name>
  <files>
    pyproject.toml,
    src/ai_hedge_fund/__init__.py,
    src/ai_hedge_fund/config.py,
    src/ai_hedge_fund/logging.py,
    src/ai_hedge_fund/models.py,
    src/ai_hedge_fund/db/__init__.py,
    src/ai_hedge_fund/db/base.py,
    src/ai_hedge_fund/db/session.py,
    .env.example,
    tests/__init__.py,
    tests/conftest.py,
    tests/unit/__init__.py,
    tests/unit/test_config.py
  </files>
  <read_first>
    docker-compose.yml,
    CLAUDE.md,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: AppSettings loads defaults when no .env exists (database_url == "postgresql+psycopg://hedge:hedge@localhost:5432/ai_hedge_fund")
    - Test: AppSettings loads from env vars (set ANTHROPIC_API_KEY="test-key", assert settings.anthropic_api_key == "test-key")
    - Test: AppSettings.extraction_token_limit default is 22_000
    - Test: AppSettings.analysis_token_limit default is 58_000
    - Test: AppSettings.reasoning_token_limit default is 116_000
    - Test: AppSettings.pipeline_total_token_limit default is 500_000
    - Test: ModelTier.EXTRACTION.value == "anthropic:claude-haiku-4-5"
    - Test: ModelTier.ANALYSIS.value == "anthropic:claude-sonnet-4-6"
    - Test: ModelTier.REASONING.value == "anthropic:claude-opus-4-6"
    - Test: MODEL_BUDGETS[ModelTier.EXTRACTION].input_tokens_limit == 20_000
    - Test: MODEL_BUDGETS[ModelTier.ANALYSIS].input_tokens_limit == 50_000
    - Test: MODEL_BUDGETS[ModelTier.REASONING].input_tokens_limit == 100_000
    - Test: AgentBudget is frozen (immutable) -- assigning to a field raises FrozenInstanceError
    - Test: configure_logging() returns a structlog logger that produces JSON output
  </behavior>
  <action>
    1. Create pyproject.toml with:
       - name = "ai-hedge-fund", version = "0.1.0", requires-python = ">=3.11"
       - [build-system] requires = ["hatchling"], build-backend = "hatchling.build"
       - [tool.hatch.build.targets.wheel] packages = ["src/ai_hedge_fund"]
       - dependencies: langgraph>=1.1.0, langchain-core>=1.2.0, langchain-anthropic>=1.4.0, pydantic-ai>=1.0.0, anthropic>=0.94.0, pydantic>=2.12.0, pydantic-settings>=2.13.0, langgraph-checkpoint-postgres>=3.0.0, "psycopg[binary,pool]>=3.3.0", sqlalchemy>=2.0.49, alembic>=1.18.0, structlog>=25.0.0, httpx>=0.28.0, tenacity>=9.0.0, langfuse>=4.0.0
       - [project.optional-dependencies] dev = ["pytest>=9.0", "pytest-cov>=7.0", "pytest-asyncio>=1.0", "ruff>=0.15.0"]
       - [tool.ruff] line-length = 100, target-version = "py312"
       - [tool.ruff.lint] select = ["E", "F", "I", "UP", "B", "SIM"]
       - [tool.pytest.ini_options] testpaths = ["tests"], asyncio_mode = "auto"

    2. Run `uv sync` to install all dependencies.

    3. Create src/ai_hedge_fund/__init__.py:
       - `__version__ = "0.1.0"`

    4. Create src/ai_hedge_fund/config.py:
       - class AppSettings(BaseSettings) with model_config = SettingsConfigDict(env_file=".env", env_prefix="", extra="ignore")
       - Fields: database_url (str, default "postgresql+psycopg://hedge:hedge@localhost:5432/ai_hedge_fund"), anthropic_api_key (str, default ""), langfuse_public_key (str, default ""), langfuse_secret_key (str, default ""), langfuse_host (str, default "https://cloud.langfuse.com"), extraction_token_limit (int, default 22_000), analysis_token_limit (int, default 58_000), reasoning_token_limit (int, default 116_000), pipeline_total_token_limit (int, default 500_000), log_level (str, default "INFO"), edgar_identity (str, default "")
       - Module-level `def get_settings() -> AppSettings` factory that returns AppSettings() (fresh instance each call for testability)

    5. Create src/ai_hedge_fund/models.py:
       - class ModelTier(Enum): EXTRACTION = "anthropic:claude-haiku-4-5", ANALYSIS = "anthropic:claude-sonnet-4-6", REASONING = "anthropic:claude-opus-4-6"
       - @dataclass(frozen=True) class AgentBudget: input_tokens_limit (int), output_tokens_limit (int), total_tokens_limit (int)
       - MODEL_BUDGETS: dict[ModelTier, AgentBudget] mapping EXTRACTION -> (20_000, 2_000, 22_000), ANALYSIS -> (50_000, 8_000, 58_000), REASONING -> (100_000, 16_000, 116_000)

    6. Create src/ai_hedge_fund/logging.py:
       - def configure_logging(log_level: str = "INFO") -> structlog.stdlib.BoundLogger
       - Configure structlog with JSON renderer, timestamp in UTC (iso format), log level, caller info
       - Use structlog.configure() with processors: add_log_level, TimeStamper(fmt="iso", utc=True), JSONRenderer()

    7. Create src/ai_hedge_fund/db/__init__.py (empty).

    8. Create src/ai_hedge_fund/db/base.py:
       - from sqlalchemy.orm import DeclarativeBase
       - class Base(DeclarativeBase): pass
       - class AppendOnlyMixin: created_at = mapped_column(DateTime(timezone=True), server_default=func.now()), as_of_date = mapped_column(DateTime(timezone=True), nullable=True)
       - class DualTimestampMixin: as_of_date = mapped_column(DateTime(timezone=True), nullable=False), observed_date = mapped_column(DateTime(timezone=True), server_default=func.now())

    9. Create src/ai_hedge_fund/db/session.py:
       - def get_engine(database_url: str) -> Engine: returns create_engine(database_url, echo=False, pool_pre_ping=True)
       - def get_session_factory(engine: Engine) -> sessionmaker[Session]: returns sessionmaker(bind=engine, expire_on_commit=False)

    10. Create .env.example with all env vars from config.py (no real values, just placeholders with comments).

    11. Run `ruff format . && ruff check . --fix` to ensure code passes linting.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund && uv run pytest tests/unit/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "ai-hedge-fund"`
    - pyproject.toml contains `"langgraph>=1.1.0"` in dependencies
    - pyproject.toml contains `"pydantic-ai>=1.0.0"` in dependencies
    - pyproject.toml contains `"langfuse>=4.0.0"` in dependencies
    - pyproject.toml contains `line-length = 100`
    - src/ai_hedge_fund/__init__.py contains `__version__ = "0.1.0"`
    - src/ai_hedge_fund/config.py contains `class AppSettings(BaseSettings)`
    - src/ai_hedge_fund/config.py contains `database_url: str`
    - src/ai_hedge_fund/config.py contains `anthropic_api_key: str`
    - src/ai_hedge_fund/config.py contains `pipeline_total_token_limit: int`
    - src/ai_hedge_fund/models.py contains `class ModelTier(Enum)`
    - src/ai_hedge_fund/models.py contains `EXTRACTION = "anthropic:claude-haiku-4-5"`
    - src/ai_hedge_fund/models.py contains `ANALYSIS = "anthropic:claude-sonnet-4-6"`
    - src/ai_hedge_fund/models.py contains `REASONING = "anthropic:claude-opus-4-6"`
    - src/ai_hedge_fund/models.py contains `@dataclass(frozen=True)`
    - src/ai_hedge_fund/models.py contains `class AgentBudget`
    - src/ai_hedge_fund/models.py contains `MODEL_BUDGETS`
    - src/ai_hedge_fund/logging.py contains `def configure_logging`
    - src/ai_hedge_fund/logging.py contains `JSONRenderer`
    - src/ai_hedge_fund/db/base.py contains `class Base(DeclarativeBase)`
    - src/ai_hedge_fund/db/base.py contains `class AppendOnlyMixin`
    - src/ai_hedge_fund/db/base.py contains `class DualTimestampMixin`
    - src/ai_hedge_fund/db/session.py contains `def get_engine`
    - src/ai_hedge_fund/db/session.py contains `def get_session_factory`
    - .env.example contains `DATABASE_URL=`
    - .env.example contains `ANTHROPIC_API_KEY=`
    - .env.example contains `LANGFUSE_PUBLIC_KEY=`
    - `uv run pytest tests/unit/test_config.py -x` exits 0
    - `uv run ruff check src/` exits 0
  </acceptance_criteria>
  <done>
    All dependencies install via uv sync. AppSettings loads from env with correct defaults. ModelTier enum has three tiers with current Claude model IDs. AgentBudget is immutable. MODEL_BUDGETS maps tiers to budgets. structlog produces JSON. DB session factory connects. All unit tests pass. ruff clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LangGraph state schemas and PydanticAI output schemas with validation tests</name>
  <files>
    src/ai_hedge_fund/schemas/__init__.py,
    src/ai_hedge_fund/schemas/state.py,
    src/ai_hedge_fund/schemas/agents.py,
    tests/unit/test_schemas.py
  </files>
  <read_first>
    src/ai_hedge_fund/config.py,
    src/ai_hedge_fund/models.py,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: PipelineState TypedDict can be instantiated with all required fields (ticker, raw_text, extraction=None, analysis=None, error=None)
    - Test: ExtractionOutput validates with valid data (key_metrics=["revenue: $10B"], data_quality=0.85)
    - Test: ExtractionOutput rejects empty key_metrics list (min_length=1 violation)
    - Test: ExtractionOutput rejects data_quality > 1.0 (ge=0.0, le=1.0 constraint)
    - Test: ExtractionOutput rejects data_quality < 0.0
    - Test: AnalysisOutput validates with valid data (summary="test", confidence=0.7, key_findings=["finding1"])
    - Test: AnalysisOutput rejects confidence > 1.0
    - Test: AnalysisOutput rejects empty key_findings
    - Test: ThesisOutput validates with bull_case, bear_case, confidence, risk_factors (future-proofing schema)
    - Test: ThesisOutput rejects confidence outside 0-100 range
    - Test: SignalOutput validates with direction (long/short/neutral), conviction (low/medium/high), time_horizon, position_size_pct
    - Test: SignalOutput rejects invalid direction value (not in Literal["long","short","neutral"])
    - Test: SignalOutput rejects position_size_pct > 100 or < 0
    - Test: All output schemas produce clean model_dump() dicts (serialization works)
  </behavior>
  <action>
    1. Create src/ai_hedge_fund/schemas/__init__.py:
       - Re-export PipelineState, ExtractionOutput, AnalysisOutput, ThesisOutput, SignalOutput

    2. Create src/ai_hedge_fund/schemas/state.py:
       - from __future__ import annotations
       - from typing import TypedDict
       - class PipelineState(TypedDict, total=False): ticker (Required[str]), raw_text (Required[str]), extraction (dict | None), analysis (dict | None), error (str | None)
       - Use typing.Required for ticker and raw_text, rest optional with defaults

    3. Create src/ai_hedge_fund/schemas/agents.py:
       - from __future__ import annotations
       - from typing import Literal
       - from pydantic import BaseModel, Field

       - class ExtractionOutput(BaseModel):
           key_metrics: list[str] = Field(min_length=1, description="Key financial metrics extracted")
           data_quality: float = Field(ge=0.0, le=1.0, description="Quality score of extracted data")

       - class AnalysisOutput(BaseModel):
           summary: str = Field(description="Executive summary of analysis")
           confidence: float = Field(ge=0.0, le=1.0, description="Confidence in analysis (0-1)")
           key_findings: list[str] = Field(min_length=1, description="Key analytical findings")

       - class ThesisOutput(BaseModel):
           ticker: str = Field(description="Stock ticker symbol")
           bull_case: list[str] = Field(min_length=1, description="Bull case arguments with citations")
           bear_case: list[str] = Field(min_length=1, description="Bear case arguments with citations")
           confidence: int = Field(ge=0, le=100, description="Confidence score 0-100")
           risk_factors: list[str] = Field(min_length=1, description="Named risk factors")

       - class SignalOutput(BaseModel):
           ticker: str = Field(description="Stock ticker symbol")
           direction: Literal["long", "short", "neutral"] = Field(description="Trade direction")
           conviction: Literal["low", "medium", "high"] = Field(description="Conviction level")
           time_horizon: str = Field(description="Expected time horizon e.g. '3-6 months'")
           position_size_pct: float = Field(ge=0.0, le=100.0, description="Suggested allocation percentage")
           thesis_summary: str = Field(description="One-paragraph thesis summary")

    4. Create tests/unit/test_schemas.py with all behaviors listed above. Use pytest.raises(ValidationError) for rejection tests. Verify model_dump() produces dict for all valid instances.

    5. Run `ruff format . && ruff check . --fix`.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund && uv run pytest tests/unit/test_schemas.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/ai_hedge_fund/schemas/state.py contains `class PipelineState`
    - src/ai_hedge_fund/schemas/state.py contains `ticker`
    - src/ai_hedge_fund/schemas/state.py contains `raw_text`
    - src/ai_hedge_fund/schemas/state.py contains `extraction`
    - src/ai_hedge_fund/schemas/agents.py contains `class ExtractionOutput(BaseModel)`
    - src/ai_hedge_fund/schemas/agents.py contains `class AnalysisOutput(BaseModel)`
    - src/ai_hedge_fund/schemas/agents.py contains `class ThesisOutput(BaseModel)`
    - src/ai_hedge_fund/schemas/agents.py contains `class SignalOutput(BaseModel)`
    - src/ai_hedge_fund/schemas/agents.py contains `Literal["long", "short", "neutral"]`
    - src/ai_hedge_fund/schemas/agents.py contains `Literal["low", "medium", "high"]`
    - src/ai_hedge_fund/schemas/agents.py contains `ge=0, le=100` (ThesisOutput confidence)
    - src/ai_hedge_fund/schemas/agents.py contains `ge=0.0, le=100.0` (SignalOutput position_size_pct)
    - src/ai_hedge_fund/schemas/__init__.py contains `ExtractionOutput`
    - src/ai_hedge_fund/schemas/__init__.py contains `SignalOutput`
    - tests/unit/test_schemas.py contains at least 10 test functions
    - `uv run pytest tests/unit/test_schemas.py -x` exits 0
  </acceptance_criteria>
  <done>
    PipelineState TypedDict for LangGraph. ExtractionOutput and AnalysisOutput for Phase 1 agents. ThesisOutput and SignalOutput future-proofed for Phase 3+. All validation constraints enforced. All 14+ unit tests pass. ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env -> AppSettings | Secrets loaded from filesystem into application config |
| User input -> Pydantic schemas | External data validated at schema boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | config.py | mitigate | AppSettings never logs secret values; .env.example has placeholders only; .gitignore excludes .env |
| T-01-02 | Tampering | schemas/agents.py | mitigate | All output schemas use Pydantic Field constraints (ge, le, min_length, Literal) -- invalid data rejected at validation boundary |
| T-01-03 | Information Disclosure | logging.py | mitigate | structlog configured to never log raw API keys; log level configurable via env |
</threat_model>

<verification>
1. `uv sync` completes without errors
2. `uv run python -c "from ai_hedge_fund.config import get_settings; s = get_settings(); print(s.database_url)"` prints default DB URL
3. `uv run python -c "from ai_hedge_fund.models import ModelTier; print(ModelTier.EXTRACTION.value)"` prints "anthropic:claude-haiku-4-5"
4. `uv run python -c "from ai_hedge_fund.schemas import ExtractionOutput; print(ExtractionOutput(key_metrics=['test'], data_quality=0.9).model_dump())"` prints valid dict
5. `uv run pytest tests/unit/ -x -v` -- all tests pass
6. `uv run ruff check src/` -- no errors
</verification>

<success_criteria>
- Package installs and imports cleanly
- Config loads from environment with correct defaults and budget fields
- ModelTier enum contains 3 Claude model IDs matching current Anthropic naming
- AgentBudget is immutable with MODEL_BUDGETS mapping
- All 5 schema classes defined with proper Pydantic validation constraints
- Database session factory creates engine from config
- structlog produces JSON output
- 20+ unit tests pass covering config, models, and schemas
- ruff format and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

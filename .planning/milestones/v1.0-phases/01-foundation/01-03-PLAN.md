---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/ai_hedge_fund/graph/__init__.py
  - src/ai_hedge_fund/graph/pipeline.py
  - src/ai_hedge_fund/graph/nodes.py
  - src/ai_hedge_fund/graph/checkpointer.py
  - src/ai_hedge_fund/observability/__init__.py
  - src/ai_hedge_fund/observability/langfuse.py
  - src/ai_hedge_fund/observability/budget.py
  - tests/integration/__init__.py
  - tests/integration/test_graph.py
  - tests/integration/test_checkpointer.py
  - tests/integration/test_observability.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04
  - FOUND-05
user_setup:
  - service: langfuse
    why: "Observability tracing for agent runs -- free cloud tier for development"
    env_vars:
      - name: LANGFUSE_PUBLIC_KEY
        source: "Sign up at https://cloud.langfuse.com -> Settings -> API Keys -> Public Key"
      - name: LANGFUSE_SECRET_KEY
        source: "Sign up at https://cloud.langfuse.com -> Settings -> API Keys -> Secret Key"
    dashboard_config: []
  - service: anthropic
    why: "Claude model API access for agent LLM calls"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com -> API Keys -> Create Key"
    dashboard_config: []

must_haves:
  truths:
    - "LangGraph graph compiles with two nodes (extract + analyze) and runs to completion"
    - "Graph state checkpoints to PostgreSQL between steps and resumes from checkpoint"
    - "Different graph nodes route to different Claude models (Haiku for extract, Sonnet for analyze)"
    - "Langfuse trace shows each step with model, token count, and latency"
    - "When an agent exceeds its token budget, the pipeline halts with BudgetExceededError"
    - "Pipeline-level budget tracker enforces total token cap across all agent calls"
  artifacts:
    - path: "src/ai_hedge_fund/graph/pipeline.py"
      provides: "LangGraph StateGraph with extract and analyze nodes"
      contains: "def build_pipeline"
      exports: ["build_pipeline"]
    - path: "src/ai_hedge_fund/graph/nodes.py"
      provides: "Node functions wrapping PydanticAI agents"
      contains: "async def extract_node"
      exports: ["extract_node", "analyze_node"]
    - path: "src/ai_hedge_fund/graph/checkpointer.py"
      provides: "PostgresSaver setup and management"
      contains: "def create_checkpointer"
      exports: ["create_checkpointer"]
    - path: "src/ai_hedge_fund/observability/langfuse.py"
      provides: "Langfuse CallbackHandler factory"
      contains: "def create_langfuse_config"
      exports: ["create_langfuse_config"]
    - path: "src/ai_hedge_fund/observability/budget.py"
      provides: "Pipeline budget enforcement integration"
      contains: "def create_budget_enforced_config"
    - path: "tests/integration/test_graph.py"
      provides: "Graph compilation and execution tests"
      min_lines: 40
    - path: "tests/integration/test_checkpointer.py"
      provides: "PostgreSQL checkpoint persistence tests"
      min_lines: 30
    - path: "tests/integration/test_observability.py"
      provides: "Langfuse integration tests"
      min_lines: 20
  key_links:
    - from: "src/ai_hedge_fund/graph/nodes.py"
      to: "src/ai_hedge_fund/agents/extraction.py"
      via: "extraction_agent.run() wrapped in node function"
      pattern: "extraction_agent\\.run"
    - from: "src/ai_hedge_fund/graph/nodes.py"
      to: "src/ai_hedge_fund/agents/analysis.py"
      via: "analysis_agent.run() wrapped in node function"
      pattern: "analysis_agent\\.run"
    - from: "src/ai_hedge_fund/graph/pipeline.py"
      to: "src/ai_hedge_fund/graph/checkpointer.py"
      via: "PostgresSaver passed to builder.compile()"
      pattern: "builder\\.compile\\(checkpointer="
    - from: "src/ai_hedge_fund/graph/nodes.py"
      to: "src/ai_hedge_fund/agents/base.py"
      via: "get_usage_limits for budget enforcement on each agent.run()"
      pattern: "get_usage_limits"
    - from: "src/ai_hedge_fund/observability/langfuse.py"
      to: "langfuse.langchain"
      via: "CallbackHandler for tracing"
      pattern: "from langfuse\\.langchain import CallbackHandler"
---

<objective>
Wire the LangGraph pipeline: define a two-node graph (extract -> analyze) with PostgreSQL checkpointing, Langfuse observability, and budget enforcement. This completes the Phase 1 foundation -- a running, instrumented, budget-aware pipeline.

Purpose: This is the capstone plan that integrates all prior work into a functional pipeline matching all four ROADMAP success criteria: (1) graph with checkpointing, (2) typed agent schemas, (3) multi-model routing with traces, (4) token budget enforcement.

Output: Compilable LangGraph graph, PostgreSQL checkpointer, Langfuse tracing config, budget-enforced node functions, and integration tests proving all success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plans 01 and 02 that this plan depends on. -->

From src/ai_hedge_fund/schemas/state.py:
```python
class PipelineState(TypedDict, total=False):
    ticker: Required[str]
    raw_text: Required[str]
    extraction: dict | None
    analysis: dict | None
    error: str | None
```

From src/ai_hedge_fund/agents/extraction.py:
```python
extraction_agent: Agent  # PydanticAI Agent with ModelTier.EXTRACTION, output_type=ExtractionOutput
def get_extraction_limits() -> UsageLimits
```

From src/ai_hedge_fund/agents/analysis.py:
```python
analysis_agent: Agent  # PydanticAI Agent with ModelTier.ANALYSIS, output_type=AnalysisOutput
def get_analysis_limits() -> UsageLimits
```

From src/ai_hedge_fund/agents/base.py:
```python
def create_agent(model_tier: ModelTier, output_type: type[BaseModel], system_prompt: str, **kwargs) -> Agent
def get_usage_limits(model_tier: ModelTier, *, input_override: int | None = None, ...) -> UsageLimits
class PipelineBudgetTracker:  # frozen dataclass, total_limit: int, records: tuple
    def record_usage(self, agent_name, input_tokens, output_tokens, total_tokens) -> PipelineBudgetTracker
class BudgetExceededError(Exception): pass
```

From src/ai_hedge_fund/config.py:
```python
class AppSettings(BaseSettings):
    database_url: str = "postgresql+psycopg://hedge:hedge@localhost:5432/ai_hedge_fund"
    langfuse_public_key: str = ""
    langfuse_secret_key: str = ""
    langfuse_host: str = "https://cloud.langfuse.com"
    pipeline_total_token_limit: int = 500_000
```

From src/ai_hedge_fund/models.py:
```python
class ModelTier(Enum):
    EXTRACTION = "anthropic:claude-haiku-4-5"
    ANALYSIS = "anthropic:claude-sonnet-4-6"
    REASONING = "anthropic:claude-opus-4-6"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LangGraph graph definition, node functions, and checkpointer</name>
  <files>
    src/ai_hedge_fund/graph/__init__.py,
    src/ai_hedge_fund/graph/pipeline.py,
    src/ai_hedge_fund/graph/nodes.py,
    src/ai_hedge_fund/graph/checkpointer.py,
    tests/integration/__init__.py,
    tests/integration/test_graph.py,
    tests/integration/test_checkpointer.py
  </files>
  <read_first>
    src/ai_hedge_fund/schemas/state.py,
    src/ai_hedge_fund/agents/extraction.py,
    src/ai_hedge_fund/agents/analysis.py,
    src/ai_hedge_fund/agents/base.py,
    src/ai_hedge_fund/config.py,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    1. Create src/ai_hedge_fund/graph/__init__.py:
       - Re-export: build_pipeline, extract_node, analyze_node, create_checkpointer

    2. Create src/ai_hedge_fund/graph/nodes.py:
       - from __future__ import annotations
       - import structlog
       - from pydantic_ai.exceptions import UsageLimitExceeded
       - from ai_hedge_fund.agents.extraction import extraction_agent, get_extraction_limits
       - from ai_hedge_fund.agents.analysis import analysis_agent, get_analysis_limits
       - from ai_hedge_fund.agents.base import BudgetExceededError
       - from ai_hedge_fund.schemas.state import PipelineState

       - logger = structlog.get_logger(__name__)

       - async def extract_node(state: PipelineState) -> dict:
           """Wrap extraction agent as LangGraph node. Returns NEW dict (immutable pattern)."""
           try:
               limits = get_extraction_limits()
               result = await extraction_agent.run(
                   f"Extract key financial metrics from: {state['raw_text']}",
                   usage_limits=limits,
               )
               logger.info("extraction_complete", ticker=state["ticker"],
                           tokens=result.usage().total_tokens if hasattr(result, 'usage') else None)
               return {"extraction": result.output.model_dump()}
           except UsageLimitExceeded as e:
               logger.error("extraction_budget_exceeded", ticker=state["ticker"], error=str(e))
               return {"error": f"Extraction budget exceeded: {e}"}

       - async def analyze_node(state: PipelineState) -> dict:
           """Wrap analysis agent as LangGraph node. Returns NEW dict (immutable pattern)."""
           if state.get("error"):
               return {}  # Skip if prior node errored
           try:
               limits = get_analysis_limits()
               result = await analysis_agent.run(
                   f"Analyze financial data for {state['ticker']}: {state['extraction']}",
                   usage_limits=limits,
               )
               logger.info("analysis_complete", ticker=state["ticker"],
                           tokens=result.usage().total_tokens if hasattr(result, 'usage') else None)
               return {"analysis": result.output.model_dump()}
           except UsageLimitExceeded as e:
               logger.error("analysis_budget_exceeded", ticker=state["ticker"], error=str(e))
               return {"error": f"Analysis budget exceeded: {e}"}

    3. Create src/ai_hedge_fund/graph/checkpointer.py:
       - from __future__ import annotations
       - from contextlib import contextmanager
       - from langgraph.checkpoint.postgres import PostgresSaver
       - from ai_hedge_fund.config import get_settings

       - @contextmanager
         def create_checkpointer(database_url: str | None = None):
           """Create PostgresSaver checkpointer with setup. Use as context manager."""
           url = database_url or get_settings().database_url
           # PostgresSaver needs psycopg connection string format (not SQLAlchemy)
           # Convert "postgresql+psycopg://..." to "postgresql://..."
           pg_url = url.replace("postgresql+psycopg://", "postgresql://")
           with PostgresSaver.from_conn_string(pg_url) as checkpointer:
               checkpointer.setup()
               yield checkpointer

       - Also provide async version:
         from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver
         from contextlib import asynccontextmanager

         @asynccontextmanager
         async def create_async_checkpointer(database_url: str | None = None):
           url = database_url or get_settings().database_url
           pg_url = url.replace("postgresql+psycopg://", "postgresql://")
           async with AsyncPostgresSaver.from_conn_string(pg_url) as checkpointer:
               await checkpointer.setup()
               yield checkpointer

    4. Create src/ai_hedge_fund/graph/pipeline.py:
       - from __future__ import annotations
       - from langgraph.graph import StateGraph, END, START
       - from langgraph.checkpoint.postgres import PostgresSaver
       - from ai_hedge_fund.schemas.state import PipelineState
       - from ai_hedge_fund.graph.nodes import extract_node, analyze_node

       - def build_pipeline(checkpointer: PostgresSaver | None = None) -> StateGraph:
           """Build the foundation pipeline: extract -> analyze.
           Returns compiled graph. Pass checkpointer for PostgreSQL state persistence."""
           builder = StateGraph(PipelineState)
           builder.add_node("extract", extract_node)
           builder.add_node("analyze", analyze_node)
           builder.add_edge(START, "extract")
           builder.add_edge("extract", "analyze")
           builder.add_edge("analyze", END)
           return builder.compile(checkpointer=checkpointer)

    5. Create tests/integration/__init__.py (empty).

    6. Create tests/integration/test_graph.py:
       - Test: build_pipeline() without checkpointer compiles successfully (returns a compiled graph)
       - Test: compiled graph has nodes "extract" and "analyze"
       - Test: graph.get_graph().nodes contains expected node names
       - NOTE: Tests that invoke the graph with real LLM calls need ANTHROPIC_API_KEY.
         Mark these with @pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY"), reason="No API key").
       - Test (requires API key): graph.ainvoke({"ticker": "AAPL", "raw_text": "Revenue: $394B, Net Income: $97B, EPS: $6.42"}) returns state with "extraction" and "analysis" keys populated as dicts.
       - Test (requires API key): extraction result contains "key_metrics" list and "data_quality" float.
       - Test (requires API key): analysis result contains "summary" str, "confidence" float, "key_findings" list.

    7. Create tests/integration/test_checkpointer.py:
       - Mark all tests with @pytest.mark.skipif(not os.environ.get("DATABASE_URL") and not docker_available(), reason="PostgreSQL not available")
       - Helper: docker_available() -> bool: check if docker-compose postgres is running via subprocess
       - Test: create_checkpointer() context manager yields a PostgresSaver instance
       - Test: build_pipeline(checkpointer=checkpointer) compiles with checkpointer
       - Test (requires DB + API key): invoke graph with thread_id, then invoke again with same thread_id to verify checkpoint resume (second invoke starts from where first left off)

    8. Run `ruff format . && ruff check . --fix`.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund && uv run pytest tests/integration/test_graph.py -x -v -k "not requires_api_key and not requires_db"</automated>
  </verify>
  <acceptance_criteria>
    - src/ai_hedge_fund/graph/nodes.py contains `async def extract_node(state: PipelineState)`
    - src/ai_hedge_fund/graph/nodes.py contains `async def analyze_node(state: PipelineState)`
    - src/ai_hedge_fund/graph/nodes.py contains `extraction_agent.run(`
    - src/ai_hedge_fund/graph/nodes.py contains `analysis_agent.run(`
    - src/ai_hedge_fund/graph/nodes.py contains `usage_limits=limits`
    - src/ai_hedge_fund/graph/nodes.py contains `UsageLimitExceeded`
    - src/ai_hedge_fund/graph/nodes.py contains `return {"extraction":`
    - src/ai_hedge_fund/graph/nodes.py contains `return {"analysis":`
    - src/ai_hedge_fund/graph/pipeline.py contains `def build_pipeline(`
    - src/ai_hedge_fund/graph/pipeline.py contains `builder = StateGraph(PipelineState)`
    - src/ai_hedge_fund/graph/pipeline.py contains `builder.add_node("extract", extract_node)`
    - src/ai_hedge_fund/graph/pipeline.py contains `builder.add_node("analyze", analyze_node)`
    - src/ai_hedge_fund/graph/pipeline.py contains `builder.add_edge(START, "extract")`
    - src/ai_hedge_fund/graph/pipeline.py contains `builder.compile(checkpointer=checkpointer)`
    - src/ai_hedge_fund/graph/checkpointer.py contains `def create_checkpointer(`
    - src/ai_hedge_fund/graph/checkpointer.py contains `PostgresSaver.from_conn_string`
    - src/ai_hedge_fund/graph/checkpointer.py contains `checkpointer.setup()`
    - src/ai_hedge_fund/graph/__init__.py contains `build_pipeline`
    - tests/integration/test_graph.py contains at least 3 test functions
    - tests/integration/test_checkpointer.py contains at least 2 test functions
    - `uv run pytest tests/integration/test_graph.py -x -k "not requires"` exits 0
    - `uv run ruff check src/ai_hedge_fund/graph/` exits 0
  </acceptance_criteria>
  <done>
    LangGraph graph compiles with extract and analyze nodes. Node functions wrap PydanticAI agents with UsageLimits enforcement. Checkpointer context manager creates PostgresSaver. Graph compilation tests pass without API keys. Integration tests with API keys marked as skippable. ruff clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Langfuse observability integration and budget-enforced invocation config</name>
  <files>
    src/ai_hedge_fund/observability/__init__.py,
    src/ai_hedge_fund/observability/langfuse.py,
    src/ai_hedge_fund/observability/budget.py,
    tests/integration/test_observability.py
  </files>
  <read_first>
    src/ai_hedge_fund/graph/pipeline.py,
    src/ai_hedge_fund/graph/nodes.py,
    src/ai_hedge_fund/config.py,
    src/ai_hedge_fund/agents/base.py,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    1. Create src/ai_hedge_fund/observability/__init__.py:
       - Re-export: create_langfuse_config, create_invocation_config, shutdown_langfuse

    2. Create src/ai_hedge_fund/observability/langfuse.py:
       - from __future__ import annotations
       - import structlog
       - from langfuse import get_client
       - from langfuse.langchain import CallbackHandler
       - from ai_hedge_fund.config import get_settings

       - logger = structlog.get_logger(__name__)

       - def create_langfuse_handler() -> CallbackHandler | None:
           """Create Langfuse CallbackHandler if credentials configured. Returns None if not configured."""
           settings = get_settings()
           if not settings.langfuse_public_key or not settings.langfuse_secret_key:
               logger.info("langfuse_not_configured", msg="Skipping Langfuse -- no credentials in env")
               return None
           return CallbackHandler()

       - def create_langfuse_config(
             thread_id: str,
             session_id: str | None = None,
             tags: list[str] | None = None,
             user_id: str = "pipeline-v1",
         ) -> dict:
           """Create LangGraph invocation config with Langfuse tracing.
           Returns config dict with configurable thread_id and optional Langfuse callbacks.
           If Langfuse is not configured, returns config without callbacks (pipeline still runs)."""
           config: dict = {
               "configurable": {"thread_id": thread_id},
           }
           handler = create_langfuse_handler()
           if handler:
               config["callbacks"] = [handler]
               config["metadata"] = {
                   "langfuse_session_id": session_id or thread_id,
                   "langfuse_user_id": user_id,
                   "langfuse_tags": tags or [],
               }
           return config

       - def shutdown_langfuse() -> None:
           """Flush pending Langfuse traces. Call before process exit."""
           try:
               get_client().flush()
           except Exception:
               logger.warning("langfuse_flush_failed", msg="Could not flush Langfuse traces")

    3. Create src/ai_hedge_fund/observability/budget.py:
       - from __future__ import annotations
       - from ai_hedge_fund.agents.base import PipelineBudgetTracker
       - from ai_hedge_fund.config import get_settings
       - from ai_hedge_fund.observability.langfuse import create_langfuse_config

       - def create_invocation_config(
             thread_id: str,
             ticker: str,
             session_id: str | None = None,
             tags: list[str] | None = None,
         ) -> tuple[dict, PipelineBudgetTracker]:
           """Create a complete invocation config with Langfuse tracing and a fresh budget tracker.
           Returns (config_dict, budget_tracker) tuple.
           Usage:
               config, tracker = create_invocation_config("run-AAPL-001", "AAPL")
               result = await graph.ainvoke(state, config)
               # After run, update tracker with actual usage from result
           """
           settings = get_settings()
           all_tags = [f"ticker:{ticker}"] + (tags or [])
           config = create_langfuse_config(
               thread_id=thread_id,
               session_id=session_id,
               tags=all_tags,
           )
           tracker = PipelineBudgetTracker(total_limit=settings.pipeline_total_token_limit)
           return config, tracker

    4. Create tests/integration/test_observability.py:
       - Test: create_langfuse_config returns dict with "configurable" key containing "thread_id"
       - Test: create_langfuse_config without Langfuse credentials returns config without "callbacks" key
       - Test: create_langfuse_config with mock Langfuse credentials (monkeypatch env vars LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY) includes "callbacks" list with one handler
       - Test: create_invocation_config returns a (dict, PipelineBudgetTracker) tuple
       - Test: create_invocation_config tracker has total_limit from settings (default 500_000)
       - Test: create_invocation_config config includes ticker tag in metadata
       - Test: shutdown_langfuse does not raise even when Langfuse not configured

    5. Run `ruff format . && ruff check . --fix`.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund && uv run pytest tests/integration/test_observability.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/ai_hedge_fund/observability/langfuse.py contains `def create_langfuse_handler(`
    - src/ai_hedge_fund/observability/langfuse.py contains `def create_langfuse_config(`
    - src/ai_hedge_fund/observability/langfuse.py contains `def shutdown_langfuse(`
    - src/ai_hedge_fund/observability/langfuse.py contains `from langfuse.langchain import CallbackHandler`
    - src/ai_hedge_fund/observability/langfuse.py contains `from langfuse import get_client`
    - src/ai_hedge_fund/observability/langfuse.py contains `"configurable": {"thread_id": thread_id}`
    - src/ai_hedge_fund/observability/budget.py contains `def create_invocation_config(`
    - src/ai_hedge_fund/observability/budget.py contains `PipelineBudgetTracker(`
    - src/ai_hedge_fund/observability/budget.py contains `return config, tracker`
    - src/ai_hedge_fund/observability/__init__.py contains `create_langfuse_config`
    - src/ai_hedge_fund/observability/__init__.py contains `create_invocation_config`
    - tests/integration/test_observability.py contains at least 5 test functions
    - `uv run pytest tests/integration/test_observability.py -x` exits 0
    - `uv run ruff check src/ai_hedge_fund/observability/` exits 0
  </acceptance_criteria>
  <done>
    Langfuse CallbackHandler created when credentials available, gracefully skipped when not. create_langfuse_config produces proper LangGraph invocation config with thread_id and optional tracing. create_invocation_config bundles Langfuse config + PipelineBudgetTracker for complete run setup. shutdown_langfuse flushes traces safely. All observability tests pass. ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM API -> graph nodes | Untrusted LLM responses validated by PydanticAI output schemas before entering graph state |
| Langfuse cloud -> application | Observability data sent to external service; credentials loaded from env |
| PostgreSQL -> checkpointer | Graph state persisted to database; connection string from env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | observability/langfuse.py | mitigate | Langfuse credentials loaded from env (never hardcoded); graceful degradation when not configured (pipeline runs without tracing) |
| T-03-02 | Denial of Service | graph/nodes.py | mitigate | Every agent.run() call wrapped with UsageLimits; UsageLimitExceeded caught and logged as error in graph state; pipeline halts agent rather than continuing to spend |
| T-03-03 | Tampering | graph/nodes.py | mitigate | Node functions return NEW dicts (immutable pattern); never modify state parameter in place; validated by PydanticAI output schemas |
| T-03-04 | Spoofing | graph/checkpointer.py | accept | PostgreSQL connection uses local docker-compose instance with default credentials; acceptable for development; production will require proper auth |
| T-03-05 | Elevation of Privilege | graph/pipeline.py | mitigate | Graph structure is compile-time static (START -> extract -> analyze -> END); no runtime graph modification; no user-controlled node selection |
</threat_model>

<verification>
1. `uv run python -c "from ai_hedge_fund.graph import build_pipeline; g = build_pipeline(); print(type(g))"` prints CompiledGraph type
2. `uv run python -c "from ai_hedge_fund.observability import create_langfuse_config; c = create_langfuse_config('test-1'); print(c['configurable'])"` prints {"thread_id": "test-1"}
3. `uv run pytest tests/ -x -v --ignore=tests/integration/test_checkpointer.py -k "not requires"` -- all non-integration tests pass
4. `uv run ruff check src/` -- no errors across entire src/

**Full integration verification (requires ANTHROPIC_API_KEY + PostgreSQL running):**
5. `docker compose up -d` -- PostgreSQL starts
6. `uv run pytest tests/integration/ -x -v` -- all integration tests pass including checkpointing and LLM calls
7. Check Langfuse dashboard (if configured) shows trace with two steps (extract, analyze) using different models
</verification>

<success_criteria>
- LangGraph graph compiles with extract -> analyze nodes
- Node functions wrap PydanticAI agents with UsageLimits on every run() call
- UsageLimitExceeded caught and stored in graph state error field (pipeline halts)
- PostgresSaver checkpointer created via context manager, tables auto-created
- Langfuse config includes CallbackHandler when credentials available, gracefully degrades when not
- Pipeline budget tracker bundled with invocation config
- All compilation/unit tests pass without API keys or database
- Integration tests pass with API keys and PostgreSQL
- Full pipeline demonstrates dual-model routing (Haiku extract, Sonnet analyze) in traces
- ruff clean across all source files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>

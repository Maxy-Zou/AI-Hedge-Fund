# Phase 6.1: Integration Wiring - Context

**Gathered:** 2026-04-03
**Status:** Ready for planning
**Mode:** Gap closure from milestone audit

<domain>
## Phase Boundary

Fix 4 integration wiring issues found during milestone audit:
1. Wire SignalEngine into cli.py (CRITICAL — unlocks entire signal+trade pipeline)
2. Add Market table upsert in poller (HIGH — fixes DASH-01 and resolution suppression)
3. Fix _is_on_cooldown time window (MEDIUM — apply signal_cooldown_seconds config)
4. Add configure_logging() to dashboard app.py (LOW)

</domain>

<decisions>
## Implementation Decisions

### Locked (from audit findings)

1. **cli.py**: Import SignalEngine, load_signal_settings(), construct SignalEngine instance, pass as signal_engine= to make_poll_tick()
2. **poller.py**: After persisting OrmSnapshot rows, upsert Market rows (ticker, title, status, close_time) from the domain snapshots
3. **engine.py _is_on_cooldown**: Add `Signal.detected_at > (now - timedelta(seconds=self._settings.signal_cooldown_seconds))` to the filter query
4. **dashboard/app.py**: Add `from kalshi_tracker.logging import configure_logging` and call it before first logger usage

</decisions>

<code_context>
## Files to Modify

- src/kalshi_tracker/cli.py — wire SignalEngine
- src/kalshi_tracker/daemon/poller.py — add Market upsert
- src/kalshi_tracker/signals/engine.py — fix cooldown time window
- src/kalshi_tracker/dashboard/app.py — add configure_logging

</code_context>

<specifics>
## Specific Ideas

These are surgical fixes — each is 5-15 lines of code. No new files needed.

</specifics>

<deferred>
## Deferred Ideas

None.

</deferred>

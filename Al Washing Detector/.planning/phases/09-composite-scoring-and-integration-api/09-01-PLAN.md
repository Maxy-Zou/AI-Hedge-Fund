---
phase: 09-composite-scoring-and-integration-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_washer/analysis/composite_scorer.py
  - src/ai_washer/api_types.py
  - src/ai_washer/config.py
  - config/scoring.yaml
  - tests/unit/test_composite_scorer.py
  - tests/unit/test_api_types.py
autonomous: true
requirements: [SCORE-01, SCORE-03, SCORE-04]

must_haves:
  truths:
    - "Weighted average of 6 sub-scores produces a 0-100 composite score"
    - "When signals are missing, weights re-normalize and confidence reflects coverage"
    - "Risk band classification correctly maps scores to 4 bands"
    - "Zero available signals returns None (no composite)"
  artifacts:
    - path: "src/ai_washer/analysis/composite_scorer.py"
      provides: "Pure composite scoring with graceful degradation"
      exports: ["compute_composite_score", "classify_risk_band", "CompositeResult", "SIGNAL_TYPE_TO_WEIGHT_KEY", "ALL_SIGNAL_TYPES"]
    - path: "src/ai_washer/api_types.py"
      provides: "Frozen dataclasses for public API return types"
      exports: ["CompanyScore"]
    - path: "config/scoring.yaml"
      provides: "strong_short_threshold config key"
      contains: "strong_short_threshold"
  key_links:
    - from: "src/ai_washer/analysis/composite_scorer.py"
      to: "src/ai_washer/analysis/types.py"
      via: "imports SignalResult"
      pattern: "from ai_washer\\.analysis\\.types import SignalResult"
    - from: "src/ai_washer/analysis/composite_scorer.py"
      to: "src/ai_washer/config.py"
      via: "imports SignalWeights, ScoringConfig"
      pattern: "from ai_washer\\.config import"
---

<objective>
Pure composite scorer function, API type contracts, and config extension for 4-band risk classification.

Purpose: Establish the core composite scoring logic and type contracts that Plan 02 (persistence) and Plan 03 (public API) build on. All scoring logic is pure (no DB), fully testable.
Output: composite_scorer.py with compute_composite_score(), api_types.py with CompanyScore frozen dataclass, updated config with strong_short_threshold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-composite-scoring-and-integration-api/09-RESEARCH.md

@src/ai_washer/analysis/types.py
@src/ai_washer/config.py
@config/scoring.yaml

<interfaces>
<!-- Key types the executor needs -->

From src/ai_washer/analysis/types.py:
```python
class SignalResult(BaseModel):
    signal_type: str
    score: int
    evidence: dict
```

From src/ai_washer/config.py:
```python
class SignalWeights(BaseModel):
    sec_filing: float = 0.20
    patent_gap: float = 0.15
    earnings_call: float = 0.20
    job_posting: float = 0.25
    github_activity: float = 0.10
    compute_spending: float = 0.10

class ScoringConfig(BaseSettings):
    weights: SignalWeights
    high_risk_threshold: int = 60
    low_risk_threshold: int = 30
```

CRITICAL - Signal type name mismatch (Pitfall 1):
- SignalResult.signal_type="earnings_vagueness" maps to SignalWeights field "earnings_call"
- SignalResult.signal_type="job_mismatch" maps to SignalWeights field "job_posting"
- All other signal types match their weight field names exactly
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Composite scorer pure function, API types, and config extension</name>
  <files>
    src/ai_washer/analysis/composite_scorer.py,
    src/ai_washer/api_types.py,
    src/ai_washer/config.py,
    config/scoring.yaml,
    config/scoring.example.yaml,
    tests/unit/test_composite_scorer.py,
    tests/unit/test_api_types.py
  </files>
  <read_first>
    src/ai_washer/analysis/types.py,
    src/ai_washer/config.py,
    config/scoring.yaml,
    src/ai_washer/analysis/sec_filing_scorer.py (first 30 lines for pattern)
  </read_first>
  <behavior>
    test_composite_scorer.py:
    - test_all_six_signals_produces_weighted_average: 6 SignalResult inputs with known scores and default weights -> composite equals expected weighted average (0-100 int)
    - test_single_signal_produces_score_with_low_confidence: 1 signal -> composite equals that signal's score, confidence equals that signal's weight
    - test_missing_signals_renormalize_weights: 3 of 6 signals -> weights renormalized to sum 1.0, confidence equals sum of original 3 weights
    - test_empty_signals_returns_none: empty list -> returns None
    - test_risk_band_genuine: score 15 -> "genuine"
    - test_risk_band_mixed: score 45 -> "mixed"
    - test_risk_band_significant_risk: score 70 -> "significant_risk"
    - test_risk_band_strong_short: score 90 -> "strong_short"
    - test_risk_band_boundary_30: score exactly 30 -> "mixed" (exclusive lower bound)
    - test_risk_band_boundary_60: score exactly 60 -> "significant_risk"
    - test_risk_band_boundary_80: score exactly 80 -> "strong_short"
    - test_signal_type_to_weight_mapping_covers_all_six: SIGNAL_TYPE_TO_WEIGHT_KEY has exactly 6 entries
    - test_earnings_vagueness_maps_to_earnings_call: "earnings_vagueness" in SIGNAL_TYPE_TO_WEIGHT_KEY maps to "earnings_call"
    - test_job_mismatch_maps_to_job_posting: "job_mismatch" in SIGNAL_TYPE_TO_WEIGHT_KEY maps to "job_posting"
    - test_composite_score_clamped_0_100: extreme inputs produce score in [0, 100]

    test_api_types.py:
    - test_company_score_is_frozen: CompanyScore instance raises on attribute assignment
    - test_company_score_has_all_fields: CompanyScore has ticker, company_name, scored_at, composite_score, risk_band, confidence, signal_breakdown, weights_used, signals_available, signals_missing, run_id, signal_freshness
  </behavior>
  <action>
    1. Add strong_short_threshold to ScoringConfig in config.py:
       - `strong_short_threshold: int = Field(default=80, ge=0, le=100)`
       - Place after high_risk_threshold line

    2. Add strong_short_threshold to config/scoring.yaml and config/scoring.example.yaml:
       - `strong_short_threshold: 80` after `high_risk_threshold: 60`

    3. Create src/ai_washer/api_types.py with frozen dataclass CompanyScore:
       - Fields: ticker (str), company_name (str), scored_at (datetime), composite_score (int), risk_band (str), confidence (float), signal_breakdown (dict[str, int]), weights_used (dict[str, float]), signals_available (list[str]), signals_missing (list[str]), run_id (uuid.UUID), signal_freshness (dict[str, date] | None = None)
       - signal_freshness maps each signal_type to the most recent as_of_date for that signal (per INT-03 data freshness requirement). None when not populated by the query layer.
       - Use `from __future__ import annotations` and `@dataclass(frozen=True)`
       - Add module docstring explaining these are public API return types

    4. Create src/ai_washer/analysis/composite_scorer.py:
       - Module docstring explaining pure composite scoring with graceful degradation
       - SIGNAL_TYPE_TO_WEIGHT_KEY constant dict mapping all 6 signal types:
         {"sec_filing": "sec_filing", "patent_gap": "patent_gap", "earnings_vagueness": "earnings_call", "job_mismatch": "job_posting", "github_activity": "github_activity", "compute_spending": "compute_spending"}
       - ALL_SIGNAL_TYPES = list(SIGNAL_TYPE_TO_WEIGHT_KEY.keys())
       - CompositeResult frozen dataclass: score (int), confidence (float), risk_band (str), signal_breakdown (dict[str, int]), weights_used (dict[str, float]), signals_available (list[str]), signals_missing (list[str])
       - classify_risk_band(score: int, low: int = 30, high: int = 60, strong: int = 80) -> str
         Returns "genuine" for [0,30), "mixed" for [30,60), "significant_risk" for [60,80), "strong_short" for [80,100]
       - compute_composite_score(signal_results: list[SignalResult], weights: SignalWeights, low_risk_threshold: int = 30, high_risk_threshold: int = 60, strong_short_threshold: int = 80) -> CompositeResult | None
         Returns None if signal_results is empty.
         Builds available dict {signal_type: score}.
         Looks up weight for each via SIGNAL_TYPE_TO_WEIGHT_KEY.
         Sums available weights -> confidence.
         Renormalizes to sum 1.0.
         Computes weighted average, clamps to [0, 100], rounds to int.
         Classifies risk band.
         Returns CompositeResult.

    5. Export compute_composite_score from src/ai_washer/analysis/__init__.py

    6. Write tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd "/Users/maxzou/Documents/projects/AI Hedgefund/Al Washing Detector" && uv run pytest tests/unit/test_composite_scorer.py tests/unit/test_api_types.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SIGNAL_TYPE_TO_WEIGHT_KEY" src/ai_washer/analysis/composite_scorer.py
    - grep -q "earnings_vagueness.*earnings_call" src/ai_washer/analysis/composite_scorer.py
    - grep -q "job_mismatch.*job_posting" src/ai_washer/analysis/composite_scorer.py
    - grep -q "classify_risk_band" src/ai_washer/analysis/composite_scorer.py
    - grep -q "compute_composite_score" src/ai_washer/analysis/composite_scorer.py
    - grep -q "CompositeResult" src/ai_washer/analysis/composite_scorer.py
    - grep -q "frozen=True" src/ai_washer/analysis/composite_scorer.py
    - grep -q "CompanyScore" src/ai_washer/api_types.py
    - grep -q "frozen=True" src/ai_washer/api_types.py
    - grep -q "signal_freshness" src/ai_washer/api_types.py
    - grep -q "strong_short_threshold" src/ai_washer/config.py
    - grep -q "strong_short_threshold" config/scoring.yaml
    - grep -q "def test_all_six_signals" tests/unit/test_composite_scorer.py
    - grep -q "def test_missing_signals_renormalize" tests/unit/test_composite_scorer.py
    - grep -q "def test_empty_signals_returns_none" tests/unit/test_composite_scorer.py
    - grep -q "def test_risk_band_" tests/unit/test_composite_scorer.py
    - grep -q "def test_company_score_is_frozen" tests/unit/test_api_types.py
  </acceptance_criteria>
  <done>
    All 15+ tests pass. compute_composite_score produces correct weighted averages with graceful degradation. Risk bands correctly classify scores into 4 categories. CompanyScore is frozen and includes signal_freshness field. Config has strong_short_threshold=80.
  </done>
</task>

</tasks>

<verification>
- All unit tests pass: `uv run pytest tests/unit/test_composite_scorer.py tests/unit/test_api_types.py -x -q`
- Full test suite still passes: `uv run pytest tests/ -x -q`
</verification>

<success_criteria>
- compute_composite_score returns correct weighted average for all 6 signals
- Graceful degradation renormalizes weights when signals are missing
- Risk bands match spec: genuine (0-30), mixed (30-60), significant_risk (60-80), strong_short (80-100)
- CompanyScore frozen dataclass has all required fields including signal_freshness
- Config has strong_short_threshold=80
</success_criteria>

<output>
After completion, create `.planning/phases/09-composite-scoring-and-integration-api/09-01-SUMMARY.md`
</output>

---
phase: 09-composite-scoring-and-integration-api
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/ai_washer/analysis/scoring_orchestrator.py
  - src/ai_washer/cli.py
  - tests/unit/test_composite_persistence.py
autonomous: true
requirements: [SCORE-05]

must_haves:
  truths:
    - "Each daily scoring run produces new DailyScore snapshot rows, never updates existing"
    - "Running score_all twice on the same day does not create duplicate DailyScore rows"
    - "DailyScore rows contain composite_score, signal_breakdown, confidence, weights_used, and run_id"
  artifacts:
    - path: "src/ai_washer/analysis/scoring_orchestrator.py"
      provides: "persist_composite and score_all with DailyScore persistence"
      contains: "persist_composite"
    - path: "tests/unit/test_composite_persistence.py"
      provides: "Tests for idempotent DailyScore persistence"
      contains: "test_persist_composite"
  key_links:
    - from: "src/ai_washer/analysis/scoring_orchestrator.py"
      to: "src/ai_washer/analysis/composite_scorer.py"
      via: "imports compute_composite_score"
      pattern: "from ai_washer\\.analysis\\.composite_scorer import"
    - from: "src/ai_washer/analysis/scoring_orchestrator.py"
      to: "src/ai_washer/db/models.py"
      via: "writes DailyScore rows"
      pattern: "DailyScore\\("
---

<objective>
Extend ScoringOrchestrator to compute composite scores and persist immutable DailyScore rows with idempotent daily writes.

Purpose: Wire the pure composite scorer (Plan 01) into the orchestrator's score_company/score_all pipeline and persist results as immutable snapshots. Add CLI composite scoring command.
Output: Updated scoring_orchestrator.py with persist_composite method, extended score_all to write DailyScore rows, CLI `score composite` command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-composite-scoring-and-integration-api/09-RESEARCH.md
@.planning/phases/09-composite-scoring-and-integration-api/09-01-SUMMARY.md

@src/ai_washer/analysis/scoring_orchestrator.py
@src/ai_washer/db/models.py
@src/ai_washer/cli.py

<interfaces>
From src/ai_washer/analysis/composite_scorer.py (created in Plan 01):
```python
@dataclass(frozen=True)
class CompositeResult:
    score: int
    confidence: float
    risk_band: str
    signal_breakdown: dict[str, int]
    weights_used: dict[str, float]
    signals_available: list[str]
    signals_missing: list[str]

def compute_composite_score(
    signal_results: list[SignalResult],
    weights: SignalWeights,
    low_risk_threshold: int = 30,
    high_risk_threshold: int = 60,
    strong_short_threshold: int = 80,
) -> CompositeResult | None: ...
```

From src/ai_washer/db/models.py:
```python
class DailyScore(DualTimestampMixin, Base):
    __tablename__ = "daily_scores"
    id: Mapped[uuid.UUID]
    scored_at: Mapped[datetime]  # partition key, part of PK
    company_id: Mapped[uuid.UUID]
    composite_score: Mapped[int]  # SmallInteger
    signal_breakdown: Mapped[dict]  # JSONB
    confidence: Mapped[float]
    weights_used: Mapped[dict]  # JSONB
    run_id: Mapped[uuid.UUID]
    created_at: Mapped[datetime]
```

Pitfall 4: No unique constraint on (company_id, scored_at::date) -- must use EXISTS check.
Pitfall 5: scored_at is DateTime(timezone=True), not Date. Use func.date() for comparison.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScoringOrchestrator composite persistence and CLI command</name>
  <files>
    src/ai_washer/analysis/scoring_orchestrator.py,
    src/ai_washer/cli.py,
    tests/unit/test_composite_persistence.py
  </files>
  <read_first>
    src/ai_washer/analysis/scoring_orchestrator.py,
    src/ai_washer/analysis/composite_scorer.py,
    src/ai_washer/db/models.py,
    src/ai_washer/cli.py
  </read_first>
  <behavior>
    test_composite_persistence.py:
    - test_persist_composite_creates_daily_score_row: mock session, call persist_composite with CompositeResult, verify session.add called with DailyScore instance having correct composite_score, signal_breakdown, confidence, weights_used, run_id
    - test_persist_composite_idempotent_same_day: mock session to return existing row on EXISTS query, verify persist_composite returns False and session.add NOT called
    - test_persist_composite_sets_scored_at_utc: verify scored_at on the DailyScore is timezone-aware UTC datetime matching scoring_date
    - test_persist_composite_sets_as_of_date: verify as_of_date on DailyScore matches scoring_date
    - test_score_all_persists_composites: mock score_company to return 2 signals, verify persist_composite called for each company
    - test_score_all_logs_composite_results: verify structlog output includes composite_score and risk_band
  </behavior>
  <action>
    1. Add persist_composite method to ScoringOrchestrator:
       - Signature: persist_composite(self, company_id: uuid.UUID, result: CompositeResult) -> bool
       - Build scored_at as datetime(year, month, day, tzinfo=timezone.utc) from self._scoring_date
       - Idempotency check: SELECT DailyScore WHERE company_id == company_id AND func.date(DailyScore.scored_at) == self._scoring_date. If exists, log "composite_already_exists" and return False
       - Create DailyScore row with: company_id, scored_at, composite_score=result.score, signal_breakdown=result.signal_breakdown, confidence=result.confidence, weights_used=result.weights_used, run_id=self._run_id, as_of_date=self._scoring_date
       - session.add + session.flush, return True

    2. Add compute_and_persist_composite method:
       - Signature: compute_and_persist_composite(self, company_id: uuid.UUID, signal_results: list[SignalResult]) -> CompositeResult | None
       - Call compute_composite_score(signal_results, self._config.weights, self._config.low_risk_threshold, self._config.high_risk_threshold, self._config.strong_short_threshold)
       - If None, log "composite_skipped" reason="no_signals" and return None
       - Call self.persist_composite(company_id, result)
       - Log "composite_scored" with composite_score, risk_band, confidence
       - Return result

    3. Update score_all method:
       - After persist_signals for each company, call compute_and_persist_composite(company.id, results)
       - Track composite_count alongside signal_count in final log
       - Return type stays list[tuple[str, list[SignalResult]]]

    4. Add imports at top of scoring_orchestrator.py:
       - from datetime import datetime, timezone
       - from ai_washer.analysis.composite_scorer import compute_composite_score, CompositeResult
       - from ai_washer.db.models import DailyScore (add to existing import)
       - from sqlalchemy import func (add to existing import if not present)

    5. Add CLI `score composite` command to cli.py:
       - Add to existing score_app Typer group
       - @score_app.command() def composite(ticker: str = typer.Option(None), all_companies: bool = typer.Option(False, "--all"))
       - If --all: call orchestrator.score_all() and display summary table with rich
       - If ticker: look up company, call score_company + compute_and_persist_composite, display result
       - Use lazy imports for DB session, orchestrator, config (consistent with existing CLI pattern)
  </action>
  <verify>
    <automated>cd "/Users/maxzou/Documents/projects/AI Hedgefund/Al Washing Detector" && uv run pytest tests/unit/test_composite_persistence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "persist_composite" src/ai_washer/analysis/scoring_orchestrator.py
    - grep -q "compute_and_persist_composite" src/ai_washer/analysis/scoring_orchestrator.py
    - grep -q "DailyScore" src/ai_washer/analysis/scoring_orchestrator.py
    - grep -q "func.date" src/ai_washer/analysis/scoring_orchestrator.py
    - grep -q "composite_already_exists" src/ai_washer/analysis/scoring_orchestrator.py
    - grep -q "compute_composite_score" src/ai_washer/analysis/scoring_orchestrator.py
    - grep -q "def composite" src/ai_washer/cli.py
    - grep -q "test_persist_composite_creates_daily_score_row" tests/unit/test_composite_persistence.py
    - grep -q "test_persist_composite_idempotent" tests/unit/test_composite_persistence.py
  </acceptance_criteria>
  <done>
    All tests pass. ScoringOrchestrator persists DailyScore rows with correct composite_score, signal_breakdown, confidence, weights_used, run_id. Idempotent on same-day re-runs. CLI `score composite` command available.
  </done>
</task>

</tasks>

<verification>
- Unit tests pass: `uv run pytest tests/unit/test_composite_persistence.py -x -q`
- Full test suite passes: `uv run pytest tests/ -x -q`
</verification>

<success_criteria>
- DailyScore rows created with all required fields on score_all
- Same-day re-run is idempotent (no duplicates)
- scored_at is timezone-aware UTC
- CLI `score composite` command works for single ticker and --all
</success_criteria>

<output>
After completion, create `.planning/phases/09-composite-scoring-and-integration-api/09-02-SUMMARY.md`
</output>

---
phase: 06-github-signal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_washer/ingestion/github_types.py
  - src/ai_washer/db/models.py
  - src/ai_washer/db/migrations/versions/005_add_github_repos.py
  - src/ai_washer/config.py
  - tests/unit/test_github_types.py
autonomous: true
requirements: [GH-01]

must_haves:
  truths:
    - "GitHub type contracts define repo records, org snapshots, and collection results"
    - "GitHubRepo ORM model exists as append-only table with idempotent unique constraint"
    - "Alembic migration 005 creates github_repos table with correct indexes"
    - "GitHubActivityScoringConfig is available in ScoringConfig"
  artifacts:
    - path: "src/ai_washer/ingestion/github_types.py"
      provides: "GitHubRepoRecord, GitHubOrgSnapshot, GitHubCollectionResult, ML_FRAMEWORK_PATTERNS, ML_LANGUAGES, GITHUB_SIGNAL_VERSION"
      min_lines: 80
    - path: "src/ai_washer/db/models.py"
      provides: "GitHubRepo model class"
      contains: "class GitHubRepo"
    - path: "src/ai_washer/db/migrations/versions/005_add_github_repos.py"
      provides: "Alembic migration for github_repos table"
      contains: "github_repos"
    - path: "src/ai_washer/config.py"
      provides: "GitHubActivityScoringConfig added to ScoringConfig"
      contains: "class GitHubActivityScoringConfig"
    - path: "tests/unit/test_github_types.py"
      provides: "Unit tests for type contracts"
      min_lines: 60
  key_links:
    - from: "src/ai_washer/ingestion/github_types.py"
      to: "src/ai_washer/db/models.py"
      via: "GitHubRepoRecord fields map to GitHubRepo columns"
      pattern: "GitHubRepoRecord"
    - from: "src/ai_washer/config.py"
      to: "config/scoring.yaml"
      via: "GitHubActivityScoringConfig loaded via ScoringConfig"
      pattern: "github_activity.*GitHubActivityScoringConfig"
---

<objective>
Define all type contracts, database model, migration, and scoring configuration for the GitHub signal -- the foundation that Plans 02-04 build against.

Purpose: Establish the data contracts and persistence layer before any API client or scoring logic is written. This prevents the "scavenger hunt" anti-pattern where downstream plans must explore the codebase to understand contracts.
Output: github_types.py, GitHubRepo model, Alembic migration 005, GitHubActivityScoringConfig in config.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-github-signal/06-RESEARCH.md

@src/ai_washer/ingestion/patent_types.py
@src/ai_washer/db/models.py
@src/ai_washer/db/base.py
@src/ai_washer/config.py
@src/ai_washer/db/migrations/versions/004_add_patent_table.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/ai_washer/db/base.py:
```python
class Base(DeclarativeBase): ...
class DualTimestampMixin:
    as_of_date: Mapped[date]
    observed_date: Mapped[date]
class AppendOnlyMixin(DualTimestampMixin):
    id: Mapped[uuid.UUID]
    created_at: Mapped[datetime]
```

From src/ai_washer/analysis/types.py:
```python
class SignalResult(BaseModel):
    signal_type: str
    score: int
    evidence: dict
```

From src/ai_washer/ingestion/patent_types.py (pattern to follow):
```python
class PatentRecord(BaseModel): ...        # API response schema
@dataclass(frozen=True)
class PatentForScoring: ...               # Immutable scoring input
class PatentCollectionResult(BaseModel): ... # Operational tracking
PATENT_SIGNAL_VERSION: str = "0.5.0"
```

From src/ai_washer/config.py (pattern to follow):
```python
class PatentGapScoringConfig(BaseModel):
    window_years: int = Field(default=3, ge=1, le=10)
    sigmoid_midpoint: float = Field(default=1.0)
    sigmoid_steepness: float = Field(default=2.0)

class ScoringConfig(BaseSettings):
    patent_gap: PatentGapScoringConfig = Field(default_factory=PatentGapScoringConfig)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GitHub type contracts, ORM model, migration, and scoring config</name>
  <files>
    src/ai_washer/ingestion/github_types.py,
    src/ai_washer/db/models.py,
    src/ai_washer/db/migrations/versions/005_add_github_repos.py,
    src/ai_washer/config.py,
    tests/unit/test_github_types.py
  </files>
  <read_first>
    src/ai_washer/ingestion/patent_types.py,
    src/ai_washer/db/models.py,
    src/ai_washer/db/base.py,
    src/ai_washer/config.py,
    src/ai_washer/db/migrations/versions/004_add_patent_table.py
  </read_first>
  <behavior>
    - Test 1: GitHubRepoRecord validates a well-formed repo dict (repo_full_name, primary_language, language_bytes, pushed_at, is_fork, is_archived, stargazers_count, ml_frameworks)
    - Test 2: GitHubRepoRecord rejects missing required fields (repo_full_name empty string)
    - Test 3: GitHubOrgSnapshot validates with repo_count, total_ml_language_bytes, total_language_bytes, ml_language_ratio, days_since_last_push, ml_frameworks_found, repos list
    - Test 4: GitHubCollectionResult has company_cik, repo_count, skipped_count, errors fields
    - Test 5: ML_FRAMEWORK_PATTERNS dict contains tensorflow, pytorch, scikit-learn, jax, huggingface, xgboost, lightgbm keys with list[str] values
    - Test 6: ML_LANGUAGES tuple contains Python, R, Julia, C++, Rust, Jupyter Notebook
    - Test 7: GITHUB_SIGNAL_VERSION equals "0.6.0"
    - Test 8: GitHubActivityScoringConfig has max_repos_per_org (default=100), recency_decay_days (default=365), sigmoid_midpoint (default=1.0), sigmoid_steepness (default=2.0)
  </behavior>
  <action>
    1. Create src/ai_washer/ingestion/github_types.py following the patent_types.py pattern:
       - GITHUB_SIGNAL_VERSION = "0.6.0" (matching phase numbering convention)
       - ML_FRAMEWORK_PATTERNS: dict[str, list[str]] with keys: tensorflow (tensorflow, tensorflow-gpu, tf-nightly, keras), pytorch (torch, torchvision, torchaudio, pytorch-lightning), scikit-learn (scikit-learn, sklearn), jax (jax, jaxlib, flax), huggingface (transformers, datasets, tokenizers), xgboost (xgboost), lightgbm (lightgbm)
       - ML_LANGUAGES: tuple[str, ...] = ("Python", "R", "Julia", "C++", "Rust", "Jupyter Notebook") -- domain constants, not user-configurable
       - GitHubRepoRecord(BaseModel): repo_full_name (str, min_length=1), repo_name (str), primary_language (str | None), language_bytes (dict[str, int]), pushed_at (datetime | None), is_fork (bool), is_archived (bool), stargazers_count (int, ge=0), ml_frameworks (list[str])
       - GitHubOrgSnapshot(BaseModel): repo_count (int), total_ml_language_bytes (int), total_language_bytes (int), ml_language_ratio (float), days_since_last_push (int | None), ml_frameworks_found (list[str]), repos (list[GitHubRepoRecord])
       - GitHubCollectionResult(BaseModel): company_cik (str), repo_count (int=0), skipped_count (int=0), errors (list[str])

    2. Add GitHubRepo model to src/ai_washer/db/models.py following the Patent model pattern:
       - class GitHubRepo(AppendOnlyMixin, Base): __tablename__ = "github_repos"
       - Columns: company_id (ForeignKey("companies.id")), repo_full_name (String(255)), repo_name (String(255)), primary_language (String(50), nullable=True), language_bytes (JSONB, server_default="{}"), pushed_at (DateTime(timezone=True), nullable=True), is_fork (Boolean), is_archived (Boolean), stargazers_count (Integer, default=0), ml_frameworks (JSONB, server_default="[]"), collection_metadata (JSONB, server_default="{}")
       - Indexes: ix_github_repos_company_date on (company_id, observed_date), uq_github_repos_company_repo_observed on (company_id, repo_full_name, observed_date) unique=True for idempotent daily collection
       - Update module docstring to mention eight tables

    3. Create src/ai_washer/db/migrations/versions/005_add_github_repos.py:
       - revision = "005_add_github_repos", down_revision = "004_add_patents"
       - Create github_repos table with all columns matching the ORM model
       - Create both indexes (ix_github_repos_company_date and uq_github_repos_company_repo_observed)
       - downgrade drops the table

    4. Add GitHubActivityScoringConfig to src/ai_washer/config.py:
       - class GitHubActivityScoringConfig(BaseModel): max_repos_per_org (int, default=100, ge=10, le=500), recency_decay_days (int, default=365, ge=30, le=730), sigmoid_midpoint (float, default=1.0), sigmoid_steepness (float, default=2.0)
       - Add github_activity field to ScoringConfig: github_activity: GitHubActivityScoringConfig = Field(default_factory=GitHubActivityScoringConfig)

    5. Write tests/unit/test_github_types.py covering all behaviors listed above.
  </action>
  <verify>
    <automated>cd "/Users/maxzou/Documents/projects/AI Hedgefund/Al Washing Detector" && uv run pytest tests/unit/test_github_types.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class GitHubRepoRecord" src/ai_washer/ingestion/github_types.py
    - grep -q "class GitHubOrgSnapshot" src/ai_washer/ingestion/github_types.py
    - grep -q "class GitHubCollectionResult" src/ai_washer/ingestion/github_types.py
    - grep -q "ML_FRAMEWORK_PATTERNS" src/ai_washer/ingestion/github_types.py
    - grep -q "ML_LANGUAGES" src/ai_washer/ingestion/github_types.py
    - grep -q 'GITHUB_SIGNAL_VERSION.*"0.6.0"' src/ai_washer/ingestion/github_types.py
    - grep -q "class GitHubRepo" src/ai_washer/db/models.py
    - grep -q "github_repos" src/ai_washer/db/models.py
    - grep -q "005_add_github_repos" src/ai_washer/db/migrations/versions/005_add_github_repos.py
    - grep -q "class GitHubActivityScoringConfig" src/ai_washer/config.py
    - grep -q "github_activity.*GitHubActivityScoringConfig" src/ai_washer/config.py
    - test -f tests/unit/test_github_types.py
  </acceptance_criteria>
  <done>
    - github_types.py exports GitHubRepoRecord, GitHubOrgSnapshot, GitHubCollectionResult, ML_FRAMEWORK_PATTERNS, ML_LANGUAGES, GITHUB_SIGNAL_VERSION
    - GitHubRepo ORM model in models.py with append-only mixin and idempotent unique constraint
    - Alembic migration 005 creates github_repos table
    - GitHubActivityScoringConfig in ScoringConfig
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- All unit tests in test_github_types.py pass
- GitHubRepo model is importable: `uv run python -c "from ai_washer.db.models import GitHubRepo; print(GitHubRepo.__tablename__)"`
- GitHubActivityScoringConfig is loadable: `uv run python -c "from ai_washer.config import ScoringConfig; c = ScoringConfig(); print(c.github_activity.max_repos_per_org)"`
</verification>

<success_criteria>
Type contracts, ORM model, migration, and scoring config exist and all tests pass. Plans 02-04 can build against these contracts without ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/06-github-signal/06-01-SUMMARY.md`
</output>

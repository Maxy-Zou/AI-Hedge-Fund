---
phase: 10-data-quality-and-pipeline-automation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/ai_washer/pipeline/__init__.py
  - src/ai_washer/pipeline/types.py
  - src/ai_washer/db/models.py
  - src/ai_washer/db/migrations/versions/008_add_data_source_status.py
  - tests/unit/test_pipeline_types.py
autonomous: true
requirements:
  - DQ-02
must_haves:
  truths:
    - "prefect>=3.6.23 is installable and importable"
    - "DataSourceStatus ORM model exists with source_name, last_success_at, expected_cadence_hours, is_stale"
    - "Pipeline type contracts (StageResult, PipelineRunResult) are importable"
    - "Migration 008 creates data_source_status table"
  artifacts:
    - path: "src/ai_washer/pipeline/__init__.py"
      provides: "Pipeline package"
    - path: "src/ai_washer/pipeline/types.py"
      provides: "StageResult, PipelineRunResult frozen dataclasses"
      exports: ["StageResult", "PipelineRunResult"]
    - path: "src/ai_washer/db/models.py"
      provides: "DataSourceStatus model"
      contains: "class DataSourceStatus"
    - path: "src/ai_washer/db/migrations/versions/008_add_data_source_status.py"
      provides: "Migration for data_source_status table"
  key_links:
    - from: "src/ai_washer/pipeline/types.py"
      to: "src/ai_washer/db/models.py"
      via: "StageResult used to update DataSourceStatus"
      pattern: "class StageResult"
---

<objective>
Install prefect dependency, create the pipeline package with type contracts, add the DataSourceStatus ORM model for staleness tracking, and create the Alembic migration.

Purpose: Foundation layer that all other Phase 10 plans depend on -- types, models, and dependencies must exist first.
Output: Pipeline package scaffold, DataSourceStatus model, migration 008, prefect installed.
</objective>

<execution_context>
@/Users/maxzou/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxzou/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-data-quality-and-pipeline-automation/10-RESEARCH.md

@src/ai_washer/db/models.py
@src/ai_washer/db/base.py
@src/ai_washer/config.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install prefect, create pipeline types and DataSourceStatus model</name>
  <files>
    pyproject.toml,
    src/ai_washer/pipeline/__init__.py,
    src/ai_washer/pipeline/types.py,
    src/ai_washer/db/models.py,
    tests/unit/test_pipeline_types.py
  </files>
  <read_first>
    pyproject.toml,
    src/ai_washer/db/models.py,
    src/ai_washer/db/base.py,
    src/ai_washer/config.py
  </read_first>
  <behavior>
    - Test 1: StageResult is frozen dataclass with fields: stage (str), status (str), companies_processed (int, default 0), errors (list[str], default [])
    - Test 2: PipelineRunResult is frozen dataclass with fields: run_id (UUID), started_at (datetime), ended_at (datetime), status (str), stages (list[StageResult]), companies_processed (int), total_errors (int)
    - Test 3: PipelineRunResult.total_errors computes sum of all stage error counts
    - Test 4: DataSourceStatus model has correct __tablename__ = "data_source_status" and columns: id (UUID), source_name (String(50) unique), last_success_at (DateTime nullable), last_error_at (DateTime nullable), last_error_message (String(500) nullable), expected_cadence_hours (Integer), is_stale (Boolean default false), updated_at (DateTime with server_default now)
  </behavior>
  <action>
    1. Add "prefect>=3.6.23" to dependencies list in pyproject.toml. Then run `uv pip install -e ".[dev]"` to install.
    2. Create src/ai_washer/pipeline/__init__.py (empty docstring: "Pipeline orchestration for daily batch processing.").
    3. Create src/ai_washer/pipeline/types.py with:
       - `from __future__ import annotations` at top
       - `@dataclass(frozen=True)` StageResult with fields: stage (str), status (str), companies_processed (int) = 0, errors (list[str]) = field(default_factory=list)
       - `@dataclass(frozen=True)` PipelineRunResult with fields: run_id (uuid.UUID), started_at (datetime), ended_at (datetime), status (str), stages (list[StageResult]), companies_processed (int), total_errors (int)
       - PipelineRunResult uses a `__post_init__` to compute total_errors from sum of len(s.errors) for s in stages if not explicitly set. Use object.__setattr__ since frozen.
    4. Add DataSourceStatus to src/ai_washer/db/models.py:
       - Class inherits from Base (NOT a mixin -- operational table like PipelineRun)
       - __tablename__ = "data_source_status"
       - id: Mapped[uuid.UUID] with primary_key=True, default=uuid.uuid4
       - source_name: Mapped[str] with String(50), unique=True, nullable=False
       - last_success_at: Mapped[datetime | None] with DateTime(timezone=True), nullable=True
       - last_error_at: Mapped[datetime | None] with DateTime(timezone=True), nullable=True
       - last_error_message: Mapped[str | None] with String(500), nullable=True
       - expected_cadence_hours: Mapped[int] with Integer, nullable=False
       - is_stale: Mapped[bool] with Boolean, server_default=sa.text("false")
       - updated_at: Mapped[datetime] with DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    5. Write tests in tests/unit/test_pipeline_types.py verifying all behaviors above.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_pipeline_types.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "prefect>=3.6.23" pyproject.toml
    - grep -q "class StageResult" src/ai_washer/pipeline/types.py
    - grep -q "class PipelineRunResult" src/ai_washer/pipeline/types.py
    - grep -q "class DataSourceStatus" src/ai_washer/db/models.py
    - grep -q "data_source_status" src/ai_washer/db/models.py
  </acceptance_criteria>
  <done>prefect installed, StageResult and PipelineRunResult importable, DataSourceStatus model in models.py, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration 008 for data_source_status table</name>
  <files>
    src/ai_washer/db/migrations/versions/008_add_data_source_status.py
  </files>
  <read_first>
    src/ai_washer/db/migrations/versions/007_add_job_postings.py,
    src/ai_washer/db/models.py
  </read_first>
  <action>
    Create a manual Alembic migration (following existing pattern from 007):
    - revision = "008"
    - down_revision = "007"
    - Create table "data_source_status" with columns matching DataSourceStatus model:
      - id UUID primary key with gen_random_uuid() default
      - source_name VARCHAR(50) NOT NULL UNIQUE
      - last_success_at TIMESTAMPTZ nullable
      - last_error_at TIMESTAMPTZ nullable
      - last_error_message VARCHAR(500) nullable
      - expected_cadence_hours INTEGER NOT NULL
      - is_stale BOOLEAN NOT NULL DEFAULT false
      - updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - Pre-seed the 6 data sources with expected cadences:
      - sec_filings: 24 hours
      - patents: 168 hours (7 days)
      - github: 24 hours
      - earnings: 2160 hours (90 days)
      - job_postings: 24 hours
      - xbrl_facts: 24 hours
    - downgrade: drop table data_source_status
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_pipeline_types.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "data_source_status" src/ai_washer/db/migrations/versions/008_add_data_source_status.py
    - grep -q "down_revision.*007" src/ai_washer/db/migrations/versions/008_add_data_source_status.py
    - grep -q "sec_filings" src/ai_washer/db/migrations/versions/008_add_data_source_status.py
    - grep -q "expected_cadence_hours" src/ai_washer/db/migrations/versions/008_add_data_source_status.py
  </acceptance_criteria>
  <done>Migration 008 exists with data_source_status table creation and 6 pre-seeded sources</done>
</task>

</tasks>

<verification>
- `uv run python -c "from ai_washer.pipeline.types import StageResult, PipelineRunResult; print('OK')"` prints OK
- `uv run python -c "from ai_washer.db.models import DataSourceStatus; print('OK')"` prints OK
- `uv run python -c "import prefect; print(prefect.__version__)"` prints version >= 3.6.23
- `uv run pytest tests/unit/test_pipeline_types.py -x -q` passes
</verification>

<success_criteria>
- prefect is installed and importable
- Pipeline types module exists with StageResult and PipelineRunResult frozen dataclasses
- DataSourceStatus ORM model exists in models.py
- Migration 008 creates data_source_status with pre-seeded sources
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-quality-and-pipeline-automation/10-01-SUMMARY.md`
</output>

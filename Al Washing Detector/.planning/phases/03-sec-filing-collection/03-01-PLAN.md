---
phase: 03-sec-filing-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_washer/ingestion/types.py
  - src/ai_washer/db/models.py
  - src/ai_washer/db/migrations/versions/003_add_filing_tables.py
  - src/ai_washer/config.py
  - tests/unit/test_filing_types.py
  - tests/unit/test_filing_models.py
autonomous: true
requirements: [SEC-01, SEC-03, SEC-05]

must_haves:
  truths:
    - "Filing and XBRLFact Pydantic schemas validate ingested SEC data at system boundary"
    - "Filing and XBRLFact ORM models store SEC data with dual timestamps and append-only semantics"
    - "Alembic migration 003 creates sec_filings and xbrl_facts tables with correct indexes"
    - "FilingCollectionSettings provides configurable form types, section char limits, and max 8-K count"
  artifacts:
    - path: "src/ai_washer/ingestion/types.py"
      provides: "FilingData, FilingSections, XBRLFactRecord, XBRLTagGroup, CollectionResult Pydantic models"
      contains: "class FilingData"
    - path: "src/ai_washer/db/models.py"
      provides: "Filing and XBRLFact ORM models with AppendOnlyMixin"
      contains: "class Filing"
    - path: "src/ai_washer/db/migrations/versions/003_add_filing_tables.py"
      provides: "Alembic migration for sec_filings and xbrl_facts tables"
      contains: "op.create_table"
    - path: "src/ai_washer/config.py"
      provides: "FilingCollectionSettings Pydantic model"
      contains: "class FilingCollectionSettings"
    - path: "tests/unit/test_filing_types.py"
      provides: "Unit tests for Pydantic type contracts"
      min_lines: 80
    - path: "tests/unit/test_filing_models.py"
      provides: "Unit tests for Filing and XBRLFact ORM models"
      min_lines: 60
  key_links:
    - from: "src/ai_washer/db/models.py"
      to: "src/ai_washer/db/base.py"
      via: "AppendOnlyMixin inheritance"
      pattern: "class Filing\\(AppendOnlyMixin"
    - from: "src/ai_washer/db/models.py"
      to: "src/ai_washer/ingestion/types.py"
      via: "XBRL_TAG_GROUPS constant shared between types and extractor"
      pattern: "XBRL_TAG_GROUPS"
---

<objective>
Define the data contracts and storage layer for SEC filing collection -- Pydantic type schemas, ORM models, Alembic migration, and collection settings.

Purpose: Every downstream plan (filing client, XBRL extractor, collector orchestrator) depends on these type definitions and database tables. This plan establishes the contracts before implementation begins.
Output: Pydantic types, ORM models, migration script, and config settings -- all tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sec-filing-collection/03-RESEARCH.md

@src/ai_washer/db/base.py
@src/ai_washer/db/models.py
@src/ai_washer/db/session.py
@src/ai_washer/config.py
@src/ai_washer/universe/types.py
@src/ai_washer/entity/types.py
@src/ai_washer/ingestion/__init__.py

<interfaces>
<!-- Existing patterns the executor must follow exactly -->

From src/ai_washer/db/base.py:
```python
class Base(DeclarativeBase): ...
class DualTimestampMixin:
    as_of_date: Mapped[date] = mapped_column(Date, nullable=False)
    observed_date: Mapped[date] = mapped_column(Date, nullable=False, server_default=func.current_date())
class AppendOnlyMixin(DualTimestampMixin):
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
```

From src/ai_washer/db/models.py (existing models pattern):
```python
class Company(Base):
    __tablename__ = "companies"
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    cik: Mapped[str | None] = mapped_column(String(10), nullable=True)
    aliases: Mapped[dict] = mapped_column(JSONB, nullable=False, server_default="{}")
    # ...
class SignalDetail(AppendOnlyMixin, Base):
    __tablename__ = "signal_details"
    __table_args__ = (Index("ix_signal_details_company_type_date", "company_id", "signal_type", "as_of_date"),)
    company_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("companies.id"), nullable=False)
    # ...
```

From src/ai_washer/universe/types.py (Pydantic model pattern):
```python
class EFTSHit(BaseModel):
    accession_no: str
    form_type: str
    # ...
class MarketCapRange(BaseModel):
    min_cents: int = Field(gt=0)
    max_cents: int = Field(gt=0)
    @model_validator(mode="after")
    def _check_min_lt_max(self) -> MarketCapRange: ...
```

From src/ai_washer/config.py (settings pattern):
```python
class UniverseSettings(BaseModel):
    efts_page_size: int = Field(default=50, ge=10, le=100)
    market_cap_min_cents: int = Field(default=150_000_000_000)
    # ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Pydantic type contracts and FilingCollectionSettings</name>
  <files>src/ai_washer/ingestion/types.py, src/ai_washer/config.py, tests/unit/test_filing_types.py</files>
  <read_first>
    - src/ai_washer/ingestion/types.py (will be extended -- currently only has universe types... actually this file is at src/ai_washer/universe/types.py; ingestion/types.py does not exist yet)
    - src/ai_washer/universe/types.py (pattern reference for Pydantic models)
    - src/ai_washer/entity/types.py (pattern reference for Pydantic models)
    - src/ai_washer/config.py (will be extended with FilingCollectionSettings)
    - .planning/phases/03-sec-filing-collection/03-RESEARCH.md (XBRL_TAG_GROUPS, Filing model schema, section names)
  </read_first>
  <behavior>
    - Test 1: FilingData validates required fields (accession_no, form_type, filing_date) and rejects missing required fields
    - Test 2: FilingSections validates section text truncation at max_chars limit
    - Test 3: XBRLFactRecord validates required fields (tag, concept, end_date, value_cents, fiscal_year, fiscal_period)
    - Test 4: XBRLTagGroup iterates fallback tags in order
    - Test 5: XBRL_TAG_GROUPS constant contains rd_expense, capex, revenue keys with correct tag lists
    - Test 6: CollectionResult tracks filing_count, xbrl_fact_count, skipped_count
    - Test 7: FilingCollectionSettings defaults: form_types=["10-K","10-Q","8-K"], section_max_chars=50000, max_8k_filings=10
    - Test 8: FilingCollectionSettings validates section_max_chars >= 1000
  </behavior>
  <action>
Create `src/ai_washer/ingestion/types.py` with filing-specific Pydantic models. This file is NEW (the existing types.py is at `src/ai_washer/universe/types.py`).

Define these models:

1. `XBRL_TAG_GROUPS: dict[str, list[str]]` -- constant mapping concept names to ordered fallback XBRL tag lists:
   - `"rd_expense"`: `["ResearchAndDevelopmentExpense", "ResearchAndDevelopmentExpenseExcludingAcquiredInProcessCost", "ResearchAndDevelopmentExpenseSoftwareExcludingAcquiredInProcessCost"]`
   - `"capex"`: `["PaymentsToAcquirePropertyPlantAndEquipment", "PaymentsToAcquireProductiveAssets", "CapitalExpenditureDiscontinuedOperations"]`
   - `"revenue"`: `["RevenueFromContractWithCustomerExcludingAssessedTax", "Revenues", "RevenueFromContractWithCustomerIncludingAssessedTax", "SalesRevenueNet", "SalesRevenueGoodsNet"]`

2. `FilingSections(BaseModel)` -- section text excerpts from a filing:
   - `business: str | None = None` (Item 1)
   - `risk_factors: str | None = None` (Item 1A)
   - `mda: str | None = None` (Item 7 -- Management Discussion & Analysis)
   - `financial_statements: str | None = None` (Item 8)
   - `full_text_excerpt: str | None = None` (fallback when section parsing fails)

3. `FilingData(BaseModel)` -- parsed filing data from edgartools:
   - `accession_no: str`
   - `form_type: str` (constrained to "10-K", "10-Q", "8-K" via Literal)
   - `filing_date: date`
   - `period_of_report: date | None = None`
   - `sections: FilingSections = Field(default_factory=FilingSections)`
   - `content_hash: str | None = None`
   - `entity_name: str | None = None`

4. `XBRLFactRecord(BaseModel)` -- single extracted XBRL fact:
   - `tag: str` (the actual XBRL tag matched)
   - `concept: str` (normalized concept name, e.g. "rd_expense")
   - `end_date: date`
   - `value_cents: int` (value in cents -- convert from dollars)
   - `fiscal_year: int`
   - `fiscal_period: str` (e.g. "FY", "Q1", "Q2", "Q3", "Q4")
   - `form_type: str`
   - `filed_date: date`
   - `accession_no: str`

5. `CollectionResult(BaseModel)` -- result of collecting filings for one company:
   - `company_cik: str`
   - `filing_count: int = 0`
   - `xbrl_fact_count: int = 0`
   - `skipped_count: int = 0`
   - `errors: list[str] = Field(default_factory=list)`

Then extend `src/ai_washer/config.py` with `FilingCollectionSettings(BaseModel)`:
   - `form_types: list[str] = Field(default_factory=lambda: ["10-K", "10-Q", "8-K"])`
   - `section_max_chars: int = Field(default=50_000, ge=1000)`
   - `max_8k_filings: int = Field(default=10, ge=1, le=50)`
   - `max_annual_filings: int = Field(default=5, ge=1, le=20)`
   - `max_quarterly_filings: int = Field(default=8, ge=1, le=40)`

Write tests FIRST in `tests/unit/test_filing_types.py`, then implement.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/Al\ Washing\ Detector && .venv/bin/python -m pytest tests/unit/test_filing_types.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class FilingData" src/ai_washer/ingestion/types.py
    - grep -q "class FilingSections" src/ai_washer/ingestion/types.py
    - grep -q "class XBRLFactRecord" src/ai_washer/ingestion/types.py
    - grep -q "class CollectionResult" src/ai_washer/ingestion/types.py
    - grep -q "XBRL_TAG_GROUPS" src/ai_washer/ingestion/types.py
    - grep -q "ResearchAndDevelopmentExpense" src/ai_washer/ingestion/types.py
    - grep -q "PaymentsToAcquirePropertyPlantAndEquipment" src/ai_washer/ingestion/types.py
    - grep -q "RevenueFromContractWithCustomerExcludingAssessedTax" src/ai_washer/ingestion/types.py
    - grep -q "class FilingCollectionSettings" src/ai_washer/config.py
    - grep -q "section_max_chars" src/ai_washer/config.py
    - grep -q "max_8k_filings" src/ai_washer/config.py
    - test -f tests/unit/test_filing_types.py
    - .venv/bin/python -m pytest tests/unit/test_filing_types.py -x exits 0
  </acceptance_criteria>
  <done>All Pydantic type contracts for filing data, XBRL facts, collection results, and filing collection settings are defined, tested, and importable. XBRL_TAG_GROUPS constant contains the correct fallback tag lists for rd_expense, capex, and revenue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Filing and XBRLFact ORM models with Alembic migration 003</name>
  <files>src/ai_washer/db/models.py, src/ai_washer/db/migrations/versions/003_add_filing_tables.py, tests/unit/test_filing_models.py</files>
  <read_first>
    - src/ai_washer/db/models.py (will be extended -- read current state)
    - src/ai_washer/db/base.py (AppendOnlyMixin pattern)
    - src/ai_washer/db/migrations/versions/002_add_company_active_fields.py (migration pattern)
    - src/ai_washer/db/migrations/versions/001_initial_schema.py (migration pattern)
    - .planning/phases/03-sec-filing-collection/03-RESEARCH.md (Filing model schema from Pattern 4 section)
  </read_first>
  <behavior>
    - Test 1: Filing model has __tablename__ == "sec_filings"
    - Test 2: Filing model inherits AppendOnlyMixin (has id, as_of_date, observed_date, created_at)
    - Test 3: Filing model has company_id (FK to companies.id), form_type, accession_no, filing_date, period_of_report, sections (JSONB), content_hash, collection_metadata (JSONB)
    - Test 4: Filing model has unique index on (company_id, form_type, accession_no) for idempotency
    - Test 5: Filing model has index on (company_id, form_type, observed_date)
    - Test 6: XBRLFact model has __tablename__ == "xbrl_facts"
    - Test 7: XBRLFact model inherits AppendOnlyMixin
    - Test 8: XBRLFact model has company_id, concept, tag, value_cents (BigInteger), fiscal_year, fiscal_period, form_type, filed_date, end_date, accession_no
    - Test 9: XBRLFact model has index on (company_id, concept, end_date)
    - Test 10: XBRLFact model has unique index on (company_id, concept, end_date, fiscal_period) for dedup
  </behavior>
  <action>
Extend `src/ai_washer/db/models.py` with two new ORM models following the existing patterns (AppendOnlyMixin, JSONB, Index).

1. `Filing(AppendOnlyMixin, Base)`:
   - `__tablename__ = "sec_filings"`
   - `__table_args__` with two indexes:
     - `Index("ix_sec_filings_company_form_observed", "company_id", "form_type", "observed_date")`
     - `Index("uq_sec_filings_company_form_accession", "company_id", "form_type", "accession_no", unique=True)`
   - Columns:
     - `company_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("companies.id"), nullable=False)`
     - `form_type: Mapped[str] = mapped_column(String(10), nullable=False)`
     - `accession_no: Mapped[str] = mapped_column(String(25), nullable=False)`
     - `filing_date: Mapped[date] = mapped_column(Date, nullable=False)`
     - `period_of_report: Mapped[date | None] = mapped_column(Date, nullable=True)`
     - `sections: Mapped[dict] = mapped_column(JSONB, nullable=False, server_default="{}")`
     - `content_hash: Mapped[str | None] = mapped_column(String(64), nullable=True)`
     - `collection_metadata: Mapped[dict] = mapped_column(JSONB, nullable=False, server_default="{}")`

2. `XBRLFact(AppendOnlyMixin, Base)`:
   - `__tablename__ = "xbrl_facts"`
   - `__table_args__` with two indexes:
     - `Index("ix_xbrl_facts_company_concept_end", "company_id", "concept", "end_date")`
     - `Index("uq_xbrl_facts_company_concept_end_fp", "company_id", "concept", "end_date", "fiscal_period", unique=True)`
   - Columns:
     - `company_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("companies.id"), nullable=False)`
     - `concept: Mapped[str] = mapped_column(String(50), nullable=False)` (e.g. "rd_expense", "capex", "revenue")
     - `tag: Mapped[str] = mapped_column(String(100), nullable=False)` (actual XBRL tag matched)
     - `value_cents: Mapped[int] = mapped_column(BigInteger, nullable=False)` (monetary value in cents)
     - `fiscal_year: Mapped[int] = mapped_column(nullable=False)`
     - `fiscal_period: Mapped[str] = mapped_column(String(5), nullable=False)` ("FY", "Q1", etc.)
     - `form_type: Mapped[str] = mapped_column(String(10), nullable=False)`
     - `filed_date: Mapped[date] = mapped_column(Date, nullable=False)`
     - `end_date: Mapped[date] = mapped_column(Date, nullable=False)` (fiscal period end)
     - `accession_no: Mapped[str] = mapped_column(String(25), nullable=False)`

Then create `src/ai_washer/db/migrations/versions/003_add_filing_tables.py`:
   - Manual Alembic migration (not autogenerate -- per Phase 1 pattern)
   - `revision = "003"`
   - `down_revision = "002"`
   - `upgrade()`: create `sec_filings` table, create `xbrl_facts` table, create all indexes
   - `downgrade()`: drop both tables

Write tests FIRST in `tests/unit/test_filing_models.py` using the same pattern as `tests/unit/test_models.py` (inspect ORM metadata -- columns, table names, indexes), then implement.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/Al\ Washing\ Detector && .venv/bin/python -m pytest tests/unit/test_filing_models.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class Filing" src/ai_washer/db/models.py
    - grep -q "class XBRLFact" src/ai_washer/db/models.py
    - grep -q 'sec_filings' src/ai_washer/db/models.py
    - grep -q 'xbrl_facts' src/ai_washer/db/models.py
    - grep -q "AppendOnlyMixin" src/ai_washer/db/models.py | grep -c "AppendOnlyMixin" shows at least 3 (SignalDetail + Filing + XBRLFact)
    - grep -q "uq_sec_filings_company_form_accession" src/ai_washer/db/models.py
    - grep -q "uq_xbrl_facts_company_concept_end_fp" src/ai_washer/db/models.py
    - grep -q "BigInteger" src/ai_washer/db/models.py (for value_cents)
    - test -f src/ai_washer/db/migrations/versions/003_add_filing_tables.py
    - grep -q 'down_revision = "002"' src/ai_washer/db/migrations/versions/003_add_filing_tables.py
    - grep -q "op.create_table" src/ai_washer/db/migrations/versions/003_add_filing_tables.py
    - .venv/bin/python -m pytest tests/unit/test_filing_models.py -x exits 0
  </acceptance_criteria>
  <done>Filing and XBRLFact ORM models exist with AppendOnlyMixin, dual timestamps, correct indexes (including unique constraints for idempotency), and Alembic migration 003 creates both tables. All existing tests still pass.</done>
</task>

</tasks>

<verification>
Run the full unit test suite to confirm no regressions:
```
cd /Users/maxzou/Documents/projects/AI\ Hedgefund/Al\ Washing\ Detector && .venv/bin/python -m pytest tests/unit/ -x -q
```
All pre-existing tests (260+) plus new tests must pass.
</verification>

<success_criteria>
1. `src/ai_washer/ingestion/types.py` exists with FilingData, FilingSections, XBRLFactRecord, CollectionResult, and XBRL_TAG_GROUPS
2. `src/ai_washer/db/models.py` contains Filing and XBRLFact models with AppendOnlyMixin
3. Alembic migration 003 creates sec_filings and xbrl_facts tables
4. `src/ai_washer/config.py` contains FilingCollectionSettings
5. All unit tests pass including new test_filing_types.py and test_filing_models.py
</success_criteria>

<output>
After completion, create `.planning/phases/03-sec-filing-collection/03-01-SUMMARY.md`
</output>

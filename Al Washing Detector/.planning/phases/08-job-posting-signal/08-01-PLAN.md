---
phase: 08-job-posting-signal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/ai_washer/ingestion/job_types.py
  - src/ai_washer/db/models.py
  - src/ai_washer/db/migrations/versions/007_add_job_postings.py
  - src/ai_washer/config.py
  - config/scoring.yaml
  - config/scoring.example.yaml
  - tests/unit/test_job_types.py
autonomous: true
requirements:
  - JOB-02
  - JOB-03

must_haves:
  truths:
    - "python-jobspy is installed and importable"
    - "Role classifier distinguishes engineering, marketing, and ambiguous roles"
    - "Dedup hash produces deterministic SHA-256 from company+title+location"
    - "JobPosting ORM model has lifecycle columns (first_seen, last_seen, is_active)"
    - "Migration 007 creates job_postings table with unique dedup_hash constraint"
    - "JobMismatchScoringConfig is part of ScoringConfig"
  artifacts:
    - path: "src/ai_washer/ingestion/job_types.py"
      provides: "Type contracts, constants, role classifier, dedup hash"
      exports: ["JobRecord", "JobCollectionResult", "JOB_SIGNAL_VERSION", "ENGINEERING_KEYWORDS", "MARKETING_KEYWORDS", "classify_role", "compute_job_hash"]
    - path: "src/ai_washer/db/models.py"
      provides: "JobPosting ORM model"
      contains: "class JobPosting"
    - path: "src/ai_washer/db/migrations/versions/007_add_job_postings.py"
      provides: "Alembic migration for job_postings table"
    - path: "src/ai_washer/config.py"
      provides: "JobMismatchScoringConfig in ScoringConfig"
      contains: "class JobMismatchScoringConfig"
    - path: "tests/unit/test_job_types.py"
      provides: "Tests for role classifier, dedup hash, type validation"
  key_links:
    - from: "src/ai_washer/ingestion/job_types.py"
      to: "src/ai_washer/db/models.py"
      via: "JobRecord fields map to JobPosting columns"
    - from: "src/ai_washer/config.py"
      to: "config/scoring.yaml"
      via: "job_mismatch key loaded into JobMismatchScoringConfig"
---

<objective>
Install python-jobspy dependency, define job posting type contracts (Pydantic models, role classifier, dedup hash), create JobPosting ORM model with lifecycle tracking, add Alembic migration 007, and extend ScoringConfig with JobMismatchScoringConfig.

Purpose: Establishes the foundation types, database schema, and configuration that all subsequent job signal plans depend on.
Output: Importable types, working migration, scoring config, passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-job-posting-signal/08-RESEARCH.md

@src/ai_washer/ingestion/github_types.py
@src/ai_washer/db/models.py
@src/ai_washer/db/base.py
@src/ai_washer/config.py
@config/scoring.yaml
@src/ai_washer/db/migrations/versions/006_add_earnings_transcripts.py

<interfaces>
<!-- Existing patterns the executor needs -->

From src/ai_washer/db/base.py:
```python
class Base(DeclarativeBase): ...
class AppendOnlyMixin: ...   # NOT used for JobPosting -- lifecycle tracking needs updates
class DualTimestampMixin: ... # NOT used -- JobPosting has custom timestamps
```

From src/ai_washer/analysis/types.py:
```python
class SignalResult(BaseModel):
    signal_type: str
    score: int
    evidence: dict
```

From src/ai_washer/config.py:
```python
class ScoringConfig(BaseSettings):
    weights: SignalWeights
    sec_filing: SecFilingScoringConfig
    compute_spending: ComputeSpendingScoringConfig
    patent_gap: PatentGapScoringConfig
    github_activity: GitHubActivityScoringConfig
    earnings_vagueness: EarningsVaguenessScoringConfig
    # Add: job_mismatch: JobMismatchScoringConfig
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install python-jobspy, create job_types.py with type contracts, role classifier, and dedup hash</name>
  <files>pyproject.toml, uv.lock, src/ai_washer/ingestion/job_types.py, tests/unit/test_job_types.py</files>
  <read_first>
    - src/ai_washer/ingestion/github_types.py (pattern for signal types module)
    - src/ai_washer/ingestion/patent_types.py (pattern for collection result)
    - pyproject.toml (current dependencies)
  </read_first>
  <behavior>
    - classify_role("ML Engineer", "Build PyTorch models for NLP pipeline") returns "engineering"
    - classify_role("AI Strategy Director", "Lead digital transformation AI initiatives") returns "marketing"
    - classify_role("Software Engineer", "Build web applications") returns "ambiguous"
    - compute_job_hash("Apple Inc", "ML Engineer", "Cupertino, CA") == compute_job_hash("apple inc", " ML Engineer ", "cupertino, ca") (case/whitespace insensitive)
    - compute_job_hash("Apple", "ML Engineer", "Cupertino") != compute_job_hash("Google", "ML Engineer", "Cupertino") (different company = different hash)
    - JobRecord validates with all required fields, rejects missing title
    - JOB_SIGNAL_VERSION == "0.8.0"
  </behavior>
  <action>
1. Run `uv add python-jobspy` to install the dependency.

2. Create `src/ai_washer/ingestion/job_types.py` following github_types.py pattern:
   - `JOB_SIGNAL_VERSION = "0.8.0"` (Phase 8 numbering convention)
   - `ENGINEERING_KEYWORDS`: list of ~15 engineering terms: "machine learning engineer", "ml engineer", "data scientist", "deep learning", "nlp engineer", "computer vision", "pytorch", "tensorflow", "mlops", "ml infrastructure", "research scientist", "applied scientist", "ml platform", "model training", "feature engineering"
   - `MARKETING_KEYWORDS`: list of ~10 marketing terms: "ai strategy", "ai-powered", "digital transformation", "ai evangelist", "innovation lead", "thought leader", "ai product marketing", "ai partnership", "ai consultant", "ai advisor"
   - `classify_role(title: str, description: str) -> str`: Returns "engineering", "marketing", or "ambiguous" by counting keyword hits in lowercased `f"{title} {description}"`. Engineering wins ties.
   - `compute_job_hash(company: str, title: str, location: str) -> str`: SHA-256 of `f"{company.lower().strip()}|{title.lower().strip()}|{location.lower().strip()}"`. Return hex digest (64 chars).
   - `JobRecord(BaseModel)`: Fields: title (str, min_length=1), company_name_raw (str), location (str | None), description (str | None), job_url (str | None), source_site (str), role_classification (str), dedup_hash (str, min_length=64, max_length=64)
   - `JobCollectionResult(BaseModel)`: Fields: company_cik (str), jobs_found (int=0), jobs_new (int=0), jobs_updated (int=0), errors (list[str]=Field(default_factory=list))

3. Create `tests/unit/test_job_types.py` with tests per behavior block above. Use `uv run pytest tests/unit/test_job_types.py -x` to verify.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_job_types.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "python-jobspy" pyproject.toml
    - grep -q 'JOB_SIGNAL_VERSION.*=.*"0.8.0"' src/ai_washer/ingestion/job_types.py
    - grep -q "def classify_role" src/ai_washer/ingestion/job_types.py
    - grep -q "def compute_job_hash" src/ai_washer/ingestion/job_types.py
    - grep -q "class JobRecord" src/ai_washer/ingestion/job_types.py
    - grep -q "class JobCollectionResult" src/ai_washer/ingestion/job_types.py
    - grep -q "ENGINEERING_KEYWORDS" src/ai_washer/ingestion/job_types.py
    - grep -q "MARKETING_KEYWORDS" src/ai_washer/ingestion/job_types.py
    - grep -q "test_classify_role" tests/unit/test_job_types.py
    - grep -q "test_compute_job_hash" tests/unit/test_job_types.py
  </acceptance_criteria>
  <done>python-jobspy installed, job_types.py exports all types/functions/constants, all unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: JobPosting ORM model, Alembic migration 007, JobMismatchScoringConfig, scoring.yaml update</name>
  <files>src/ai_washer/db/models.py, src/ai_washer/db/migrations/versions/007_add_job_postings.py, src/ai_washer/config.py, config/scoring.yaml, config/scoring.example.yaml</files>
  <read_first>
    - src/ai_washer/db/models.py (existing models, import pattern)
    - src/ai_washer/db/base.py (Base class, mixins)
    - src/ai_washer/db/migrations/versions/006_add_earnings_transcripts.py (migration pattern)
    - src/ai_washer/config.py (ScoringConfig class, existing config fields)
    - config/scoring.yaml (current YAML structure)
    - config/scoring.example.yaml (example config)
  </read_first>
  <action>
1. Add `JobPosting` model to `src/ai_washer/db/models.py`:
   - NOT append-only. Lifecycle tracking requires updates to last_seen and is_active.
   - Table name: "job_postings"
   - Columns: id (UUID pk, default uuid4), company_id (UUID FK to companies.id, not nullable), title (String(500), not nullable), company_name_raw (String(255), not nullable), location (String(255), nullable), description (sa.Text, nullable), job_url (String(1000), nullable), source_site (String(50), not nullable), role_classification (String(20), not nullable), dedup_hash (String(64), not nullable), first_seen (Date, not nullable), last_seen (Date, not nullable), is_active (Boolean, not nullable, server_default=sa.text("true")), collection_metadata (JSONB, not nullable, server_default=sa.text("'{}'")), created_at (DateTime(timezone=True), not nullable, server_default=func.now()), updated_at (DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
   - Indexes: Index("ix_job_postings_company_date", "company_id", "first_seen"), Index("uq_job_postings_dedup_hash", "dedup_hash", unique=True)
   - Update module docstring to mention job_postings table (tenth table)
   - Add JobPosting to any __all__ or imports if the pattern exists

2. Create `src/ai_washer/db/migrations/versions/007_add_job_postings.py`:
   - Follow 006 migration pattern exactly
   - revision id: generate a short unique hex string
   - down_revision: must match 006's revision id (read from 006 file)
   - Create job_postings table with all columns and indexes above
   - Include downgrade that drops the table

3. Add `JobMismatchScoringConfig` to `src/ai_washer/config.py`:
   ```python
   class JobMismatchScoringConfig(BaseModel):
       ghost_days_threshold: int = Field(default=90, ge=30, le=365)
       ghost_penalty_weight: float = Field(default=0.20, ge=0.0, le=1.0)
       engineering_weight: float = Field(default=0.40, ge=0.0, le=1.0)
       hiring_intensity_weight: float = Field(default=0.40, ge=0.0, le=1.0)
       sigmoid_midpoint: float = Field(default=1.0)
       sigmoid_steepness: float = Field(default=2.0)
   ```
   Add `job_mismatch: JobMismatchScoringConfig = Field(default_factory=JobMismatchScoringConfig)` to ScoringConfig.

4. Update `config/scoring.yaml` and `config/scoring.example.yaml` to include:
   ```yaml
   job_mismatch:
     ghost_days_threshold: 90
     ghost_penalty_weight: 0.20
     engineering_weight: 0.40
     hiring_intensity_weight: 0.40
     sigmoid_midpoint: 1.0
     sigmoid_steepness: 2.0
   ```
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config.py tests/unit/test_job_types.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class JobPosting" src/ai_washer/db/models.py
    - grep -q "job_postings" src/ai_washer/db/models.py
    - grep -q "dedup_hash" src/ai_washer/db/models.py
    - grep -q "first_seen" src/ai_washer/db/models.py
    - grep -q "last_seen" src/ai_washer/db/models.py
    - grep -q "is_active" src/ai_washer/db/models.py
    - test -f src/ai_washer/db/migrations/versions/007_add_job_postings.py
    - grep -q "class JobMismatchScoringConfig" src/ai_washer/config.py
    - grep -q "ghost_days_threshold" src/ai_washer/config.py
    - grep -q "job_mismatch" src/ai_washer/config.py
    - grep -q "job_mismatch" config/scoring.yaml
    - grep -q "job_mismatch" config/scoring.example.yaml
  </acceptance_criteria>
  <done>JobPosting model in models.py, migration 007 created, JobMismatchScoringConfig added to ScoringConfig, scoring YAML updated</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_job_types.py tests/unit/test_config.py -x -v` all pass
- `uv run python -c "from ai_washer.ingestion.job_types import JobRecord, classify_role, compute_job_hash; print('OK')"` succeeds
- `uv run python -c "from ai_washer.db.models import JobPosting; print('OK')"` succeeds
- `uv run python -c "from ai_washer.config import ScoringConfig; c = ScoringConfig(); print(c.job_mismatch.ghost_days_threshold)"` prints 90
</verification>

<success_criteria>
- python-jobspy is installed and importable
- job_types.py exports JOB_SIGNAL_VERSION, ENGINEERING_KEYWORDS, MARKETING_KEYWORDS, classify_role, compute_job_hash, JobRecord, JobCollectionResult
- JobPosting ORM model has lifecycle columns (first_seen, last_seen, is_active, updated_at)
- Migration 007 creates job_postings table with unique dedup_hash constraint
- JobMismatchScoringConfig is part of ScoringConfig with configurable ghost_days_threshold
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-job-posting-signal/08-01-SUMMARY.md`
</output>

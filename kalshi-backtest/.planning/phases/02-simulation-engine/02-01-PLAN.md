---
phase: 02-simulation-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/kalshi_backtest/simulation/__init__.py
  - src/kalshi_backtest/simulation/protocol.py
  - src/kalshi_backtest/simulation/snapshot.py
  - tests/test_simulation_types.py
autonomous: true
requirements:
  - SIM-01
  - SIM-02

must_haves:
  truths:
    - "Any class with a generate_signals() method satisfies Strategy without importing engine internals"
    - "isinstance(strategy, Strategy) returns True for runtime-checkable validation in tests"
    - "MarketSnapshot.result is always None for any bar where ts < close_time"
    - "MarketSnapshot is frozen — mutation raises a ValidationError"
    - "Signal and Position carry all fields needed by FillEngine and PositionTracker"
  artifacts:
    - path: "src/kalshi_backtest/simulation/__init__.py"
      provides: "Public exports: Strategy, Signal, Position, MarketSnapshot"
      exports: ["Strategy", "Signal", "Position", "MarketSnapshot"]
    - path: "src/kalshi_backtest/simulation/protocol.py"
      provides: "Strategy Protocol, Signal dataclass, Position dataclass"
      min_lines: 60
    - path: "src/kalshi_backtest/simulation/snapshot.py"
      provides: "MarketSnapshot frozen Pydantic model"
      min_lines: 40
    - path: "tests/test_simulation_types.py"
      provides: "TDD tests for Protocol, Signal, Position, MarketSnapshot"
      min_lines: 80
  key_links:
    - from: "simulation/protocol.py"
      to: "simulation/snapshot.py"
      via: "Strategy.generate_signals() accepts MarketSnapshot"
      pattern: "MarketSnapshot"
    - from: "simulation/__init__.py"
      to: "simulation/protocol.py and simulation/snapshot.py"
      via: "re-exports all public types"
      pattern: "from kalshi_backtest.simulation.protocol import"
---

<objective>
Define the immutable data contracts that every other simulation module depends on: the Strategy Protocol plugin interface, Signal and Position dataclasses, and the MarketSnapshot look-ahead firewall model.

Purpose: All downstream modules (BarIterator, FillEngine, BacktestRunner) consume these types. Defining them first prevents the executor from making ad-hoc decisions about field names and semantics mid-implementation.

Output: `simulation/` package with three files: `protocol.py` (Strategy + Signal + Position), `snapshot.py` (MarketSnapshot), `__init__.py` (public re-exports). Full TDD test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@kalshi-backtest/.planning/PROJECT.md
@kalshi-backtest/.planning/ROADMAP.md
@kalshi-backtest/.planning/STATE.md
@kalshi-backtest/src/kalshi_backtest/ingestion/types.py
@kalshi-backtest/src/kalshi_backtest/config.py

<interfaces>
<!-- Existing types the simulation layer builds on. No imports needed from these in simulation/protocol.py — types are self-contained. -->

From src/kalshi_backtest/ingestion/types.py:
```python
class MarketRecord(BaseModel):
    model_config = {"frozen": True}
    ticker: str
    event_ticker: str
    series_ticker: str
    subtitle: str | None = None
    open_time: datetime
    close_time: datetime
    expiration_time: datetime | None = None
    status: str
    result: str | None = None  # 'yes'|'no' — None until settled

class CandlestickRecord(BaseModel):
    model_config = {"frozen": True}
    ticker: str
    ts: datetime
    open_price: int | None = None
    high_price: int | None = None
    low_price: int | None = None
    close_price: int  # yes price in cents (0-100), required
    volume: int | None = None
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Strategy Protocol, Signal, and Position contracts</name>
  <files>
    src/kalshi_backtest/simulation/protocol.py,
    tests/test_simulation_types.py (partial — Protocol + Signal + Position tests)
  </files>
  <read_first>
    - kalshi-backtest/.planning/phases/02-simulation-engine/02-RESEARCH.md (Pattern 1: Strategy Protocol section)
    - kalshi-backtest/src/kalshi_backtest/ingestion/types.py (frozen Pydantic model pattern to match)
  </read_first>
  <behavior>
    RED tests first:
    - test_strategy_protocol_satisfied_without_inheritance: a class with generate_signals() passes isinstance(obj, Strategy)
    - test_strategy_protocol_not_satisfied_missing_method: a class without generate_signals() fails isinstance()
    - test_signal_is_frozen: Signal(ticker="X", direction="yes", contracts=1, limit_price=55, reason="test") then mutation raises AttributeError or ValidationError
    - test_signal_direction_validation: Signal with direction="invalid" raises ValidationError
    - test_position_is_frozen: Position mutation raises
    - test_position_fields_present: Position has ticker, direction, contracts, entry_price, entry_ts, fill_id

    GREEN implementation:
    - Strategy: typing.Protocol with @runtime_checkable; generate_signals(snapshot: MarketSnapshot, open_positions: list[Position]) -> list[Signal]
    - Signal: frozen Pydantic BaseModel — fields: ticker: str, direction: Literal["yes", "no"], contracts: int (>=1), limit_price: int (0-100), reason: str
    - Position: frozen Pydantic BaseModel — fields: ticker: str, direction: Literal["yes", "no"], contracts: int (>=1), entry_price: int (0-100), entry_ts: datetime, fill_id: str
    - Use `from __future__ import annotations` and Literal from typing
    - Model config: frozen=True on Signal and Position
    - Validate contracts >= 1 using field_validator; validate limit_price and entry_price in [0, 100]
  </behavior>
  <action>
    Create `src/kalshi_backtest/simulation/` directory with `__init__.py` (empty stub for now — populated in Task 3).

    Create `src/kalshi_backtest/simulation/protocol.py`:
    - Module docstring explaining plugin interface purpose and structural subtyping rationale
    - `from __future__ import annotations`
    - Imports: typing.Protocol, runtime_checkable, TYPE_CHECKING; if TYPE_CHECKING: import MarketSnapshot from snapshot
    - `@runtime_checkable class Strategy(Protocol)` with `generate_signals(self, snapshot: MarketSnapshot, open_positions: list[Position]) -> list[Signal]: ...`
    - `class Signal(BaseModel)` frozen, fields as above, with field validators for direction ("yes"|"no"), contracts (>= 1), limit_price (0-100)
    - `class Position(BaseModel)` frozen, fields as above, with same validators for direction, contracts, entry_price (0-100)
    - All classes have Google-style docstrings
    - File stays under 120 lines
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/kalshi-backtest && uv run pytest tests/test_simulation_types.py -k "protocol or signal or position" -x -q 2>&1 | head -40</automated>
  </verify>
  <done>
    All Protocol/Signal/Position tests pass. isinstance(stub_strategy, Strategy) returns True. Mutation of Signal/Position raises. Direction "invalid" raises ValidationError.
  </done>
  <acceptance_criteria>
    - `from kalshi_backtest.simulation.protocol import Strategy, Signal, Position` succeeds
    - Strategy is @runtime_checkable — isinstance check works without inheritance
    - Signal.direction only accepts "yes" or "no" — ValueError on anything else
    - Signal.contracts and Position.contracts reject values < 1
    - Signal.limit_price and Position.entry_price reject values outside [0, 100]
    - All fields on both models are immutable (frozen=True)
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: MarketSnapshot look-ahead firewall model</name>
  <files>
    src/kalshi_backtest/simulation/snapshot.py,
    tests/test_simulation_types.py (partial — MarketSnapshot tests appended)
  </files>
  <read_first>
    - kalshi-backtest/.planning/phases/02-simulation-engine/02-RESEARCH.md (Pattern 2: MarketSnapshot section)
    - kalshi-backtest/src/kalshi_backtest/ingestion/types.py (_to_naive_utc pattern to reuse)
  </read_first>
  <behavior>
    RED tests first:
    - test_snapshot_result_none_before_close_time: build a snapshot with ts = close_time - 1 day; assert snapshot.result is None
    - test_snapshot_result_visible_at_close_time: build snapshot with ts = close_time; result='yes' stored; assert snapshot.result == 'yes'
    - test_snapshot_result_visible_after_close_time: ts = close_time + 1 day; assert result exposed
    - test_snapshot_is_frozen: mutation raises
    - test_snapshot_close_price_range: close_price outside [0,100] raises ValidationError
    - test_snapshot_build_from_market_and_candle: build_snapshot(market_dict, candle_dict) produces correct MarketSnapshot

    GREEN implementation:
    - MarketSnapshot frozen Pydantic model with fields: ticker, event_ticker, series_ticker, ts (datetime), close_price (int), open_price (int|None), high_price (int|None), low_price (int|None), volume (int|None), close_time (datetime), result (str|None), subtitle (str|None)
    - NOTE: MarketSnapshot does NOT enforce the result=None rule itself — that is BarIterator's job. The model is a passive container. Tests above are for BarIterator behavior, previewed here to confirm the interface design.
    - build_snapshot(market: dict, candle: dict, *, suppress_result: bool) -> MarketSnapshot: factory function that handles the result suppression logic (suppress_result=True → result=None regardless of DB value)
    - Reuse _to_naive_utc pattern from ingestion/types.py for datetime normalization (copy the function, do not import from ingestion layer to avoid coupling)
  </action>
  <action>
    Create `src/kalshi_backtest/simulation/snapshot.py`:
    - Module docstring explaining look-ahead firewall role and that BarIterator controls suppress_result
    - `from __future__ import annotations`
    - `_to_naive_utc()` helper (same logic as ingestion/types.py — duplicated intentionally to avoid coupling)
    - `class MarketSnapshot(BaseModel)` frozen, all fields above
    - field_validators: normalize ts and close_time to naive UTC; validate close_price in [0,100]; validate open/high/low prices in [0,100] if not None
    - `def build_snapshot(market: dict, candle: dict, *, suppress_result: bool = False) -> MarketSnapshot:` — extracts fields from dicts, applies suppress_result logic, returns frozen model
    - Google-style docstrings on class and factory function
    - File stays under 120 lines
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/kalshi-backtest && uv run pytest tests/test_simulation_types.py -x -q 2>&1 | head -40</automated>
  </verify>
  <done>
    All simulation type tests pass. build_snapshot() correctly suppresses result when suppress_result=True. MarketSnapshot is frozen.
  </done>
  <acceptance_criteria>
    - `from kalshi_backtest.simulation.snapshot import MarketSnapshot, build_snapshot` succeeds
    - build_snapshot(market, candle, suppress_result=True) → result is None regardless of DB value
    - build_snapshot(market, candle, suppress_result=False) with result='yes' → snapshot.result == 'yes'
    - close_price outside [0, 100] raises ValidationError
    - MarketSnapshot mutation raises
    - All datetime fields stored as naive UTC
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire simulation/__init__.py public exports</name>
  <files>
    src/kalshi_backtest/simulation/__init__.py
  </files>
  <read_first>
    - kalshi-backtest/src/kalshi_backtest/ingestion/__init__.py (existing export pattern — note: may be empty to avoid circular imports)
    - kalshi-backtest/src/kalshi_backtest/db/__init__.py (another export example)
  </read_first>
  <action>
    Write `src/kalshi_backtest/simulation/__init__.py` that re-exports the public API:

    ```python
    """Simulation engine public API.

    Import from this module to access the Strategy Protocol and core data contracts:
        from kalshi_backtest.simulation import Strategy, Signal, Position, MarketSnapshot, build_snapshot
    """
    from kalshi_backtest.simulation.protocol import Position, Signal, Strategy
    from kalshi_backtest.simulation.snapshot import MarketSnapshot, build_snapshot

    __all__ = ["Strategy", "Signal", "Position", "MarketSnapshot", "build_snapshot"]
    ```

    Confirm no circular imports by running: `python -c "from kalshi_backtest.simulation import Strategy, Signal, Position, MarketSnapshot, build_snapshot; print('ok')"`
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/kalshi-backtest && uv run python -c "from kalshi_backtest.simulation import Strategy, Signal, Position, MarketSnapshot, build_snapshot; print('ok')" && uv run pytest tests/test_simulation_types.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    `from kalshi_backtest.simulation import Strategy, Signal, Position, MarketSnapshot, build_snapshot` succeeds with no import errors. All tests pass.
  </done>
  <acceptance_criteria>
    - All 5 names importable from `kalshi_backtest.simulation` in a single import statement
    - No circular import errors
    - `__all__` is defined
    - All tests in test_simulation_types.py pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
cd /Users/maxzou/Documents/projects/AI\ Hedgefund/kalshi-backtest && uv run pytest tests/test_simulation_types.py -v --tb=short 2>&1 | tail -20

Expected: All tests pass. No import errors. Coverage on simulation/protocol.py and simulation/snapshot.py >= 90%.
</verification>

<success_criteria>
- Strategy Protocol with @runtime_checkable works via structural subtyping (no inheritance required)
- Signal and Position are frozen Pydantic models with validated fields
- MarketSnapshot has a build_snapshot() factory that controls result suppression
- All public types importable from `kalshi_backtest.simulation`
- TDD: all tests written first (RED), then implementation (GREEN)
- File sizes: protocol.py < 120 lines, snapshot.py < 120 lines
</success_criteria>

<output>
After completion, create `.planning/phases/02-simulation-engine/02-01-SUMMARY.md` using the summary template.
</output>

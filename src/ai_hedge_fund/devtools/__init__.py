"""Development-workflow tooling used by the ``.claude/skills/phase-*`` skills.

Not imported by the research pipeline. Each module is a small, deterministic
check that turns a workflow rule into something a script enforces rather than
something a reviewer has to remember.
"""

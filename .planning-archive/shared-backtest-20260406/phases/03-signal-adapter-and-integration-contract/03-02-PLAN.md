---
phase: 03-signal-adapter-and-integration-contract
plan: 02
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - backtest/src/fund_backtest/signal/adapter.py
  - backtest/tests/unit/test_signal_adapter.py
autonomous: true
requirements:
  - BT-01
  - BT-05

must_haves:
  truths:
    - "SignalAdapter.adapt() accepts a valid SignalFrame and returns a WeightFrame with all values in [-1.0, +1.0]"
    - "A signal spike on date T produces zero weight on date T and non-zero weight on date T+1 (look-ahead bias guard)"
    - "Rows where non-NaN ticker count < min_coverage are zeroed out (not dropped, not raised)"
    - "All-NaN columns are silently dropped with a structlog warning before processing"
    - "Invalid signal input raises SignalValidationError (not a silent failure)"
    - "Input SignalFrame is never mutated — all operations return new DataFrames"
  artifacts:
    - path: "backtest/src/fund_backtest/signal/adapter.py"
      provides: "SignalAdapter class with adapt() method"
      exports: ["SignalAdapter"]
    - path: "backtest/tests/unit/test_signal_adapter.py"
      provides: "Full behavioral test suite for SignalAdapter"
  key_links:
    - from: "signal/adapter.py"
      to: "signal/validator.py"
      via: "validate_signal_frame(signal) called at start of adapt()"
      pattern: "validate_signal_frame"
    - from: "signal/adapter.py"
      to: "config.py SignalAdapterConfig"
      via: "SignalAdapterConfig injected via __init__ default"
      pattern: "SignalAdapterConfig"
    - from: "WeightFrame output"
      to: "Phase 4 Portfolio Simulator"
      via: "shift(1) already applied — Phase 4 must NOT shift again"
      pattern: "shift\\(1\\)"
---

<objective>
Build the SignalAdapter class using TDD: first write tests capturing all behavioral contracts (normalization, look-ahead bias guard, min_coverage, gross exposure scaling, mutation safety), then implement adapter.py to pass them all.

Purpose: The SignalAdapter is the Phase 3 deliverable — it is the boundary between raw strategy scores and the portfolio simulation engine.

Output: signal/adapter.py and passing test_signal_adapter.py. Phase 3 is complete when both plans are green.
</objective>

<execution_context>
@$HOME/.claire/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/maxzou/Documents/projects/AI Hedgefund/.planning/ROADMAP.md
@/Users/maxzou/Documents/projects/AI Hedgefund/.planning/phases/03-signal-adapter-and-integration-contract/03-RESEARCH.md
@/Users/maxzou/Documents/projects/AI Hedgefund/.planning/phases/03-signal-adapter-and-integration-contract/03-01-SUMMARY.md

<interfaces>
<!-- Types produced by Plan 01 that the executor MUST use. -->

From backtest/src/fund_backtest/signal/types.py (created in Plan 01):
```python
SignalFrame = pd.DataFrame
"""
index:   pd.DatetimeIndex, daily
columns: list[str] — ticker symbols
values:  float — raw scores (e.g. 0-100)
NaN:     allowed
"""

WeightFrame = pd.DataFrame
"""
values:  float in [-1.0, +1.0]
index:   DatetimeIndex — already shifted(1)
         First row is always NaN (no prior signal).
"""
```

From backtest/src/fund_backtest/signal/validator.py (created in Plan 01):
```python
class SignalValidationError(ValueError): ...
def validate_signal_frame(signal: pd.DataFrame) -> None: ...
```

From backtest/src/fund_backtest/config.py (created in Plan 01):
```python
class SignalAdapterConfig(BaseModel):
    min_coverage: int = 5
    gross_exposure_limit: float = 1.0
```

CRITICAL architecture note from RESEARCH.md:
- shift(1) lives HERE in SignalAdapter — Phase 4 must NOT shift again
- After shift(1), WeightFrame.iloc[0] is always NaN — this is correct by design
- All operations must return new DataFrames — never mutate input signal

Working directory for test runs: backtest/
Run command: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_adapter.py -x -q
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for SignalAdapter behavioral contracts</name>
  <files>backtest/tests/unit/test_signal_adapter.py</files>
  <read_first>
    - backtest/src/fund_backtest/signal/types.py (SignalFrame, WeightFrame aliases)
    - backtest/src/fund_backtest/config.py (SignalAdapterConfig)
    - backtest/src/fund_backtest/signal/validator.py (SignalValidationError)
    - backtest/tests/unit/test_signal_types.py (test structure to follow)
  </read_first>
  <behavior>
    TDD: Write tests BEFORE implementation. All tests must FAIL (ImportError on adapter.py).

    FIXTURE: Build a reusable 5-row, 4-ticker SignalFrame helper:
      dates = pd.date_range("2024-01-02", periods=5, freq="B")
      df = pd.DataFrame({
          "AAPL": [10.0, 20.0, 99.0, 30.0, 40.0],   # spike on index 2
          "NVDA": [90.0, 80.0,  1.0, 70.0, 60.0],
          "MSFT": [50.0, 50.0, 50.0, 50.0, 50.0],
          "GOOG": [25.0, 25.0, 25.0, 25.0, 25.0],
      }, index=dates)

    Tests:

    test_adapt_returns_dataframe:
      - adapter = SignalAdapter()
      - result = adapter.adapt(df)
      - assert isinstance(result, pd.DataFrame)

    test_weight_values_in_minus_one_to_one:
      - result = SignalAdapter().adapt(df)
      - assert result.stack().dropna().abs().max() <= 1.0 + 1e-9

    test_look_ahead_bias_guard:
      - result = SignalAdapter().adapt(df)
      - spike_day = 2   # index 2 has AAPL=99, NVDA=1 (extreme spread)
      # On the spike day itself, weights come from T-1 (not T) — shift(1)
      # AAPL had value 20 on T-1 and NVDA had 80 — NVDA should outrank AAPL
      - assert result.iloc[spike_day]["NVDA"] > result.iloc[spike_day]["AAPL"]
      # On T+1, the spike is reflected — AAPL (99 on T) should now outrank NVDA (1 on T)
      - assert result.iloc[spike_day + 1]["AAPL"] > result.iloc[spike_day + 1]["NVDA"]

    test_first_row_is_nan_after_shift:
      - result = SignalAdapter().adapt(df)
      - assert result.iloc[0].isna().all()

    test_low_coverage_row_is_zeroed:
      - config = SignalAdapterConfig(min_coverage=10)   # impossible to meet
      - result = SignalAdapter(config).adapt(df)
      # All rows should be zeroed (non-NaN count < 10 always)
      - assert (result.fillna(0).abs().sum(axis=1) == 0).all()

    test_all_nan_column_dropped_silently:
      - df_with_nan_col = df.copy()
      - df_with_nan_col["DEAD"] = float("nan")
      - result = SignalAdapter().adapt(df_with_nan_col)
      - assert "DEAD" not in result.columns  # dropped silently

    test_invalid_input_raises_validation_error:
      - bad_df = pd.DataFrame({"AAPL": [90.0]}, index=[0])  # int index
      - pytest.raises(SignalValidationError)

    test_input_not_mutated:
      - original = df.copy()
      - SignalAdapter().adapt(df)
      - pd.testing.assert_frame_equal(df, original)  # df unchanged

    test_gross_exposure_scaling:
      - config = SignalAdapterConfig(gross_exposure_limit=0.5)
      - result = SignalAdapter(config).adapt(df)
      # abs sum per row should not exceed 0.5 + floating point tolerance
      - assert (result.fillna(0).abs().sum(axis=1) <= 0.5 + 1e-9).all()

    Import target (does not exist yet — causes ImportError = RED):
      from fund_backtest.signal.adapter import SignalAdapter
  </behavior>
  <action>
    Create backtest/tests/unit/test_signal_adapter.py with all tests above.
    Run: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_adapter.py -x -q
    Confirm: Tests FAIL with ImportError for fund_backtest.signal.adapter (RED state).
    Do NOT implement adapter.py yet.
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/test_signal_adapter.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backtest/tests/unit/test_signal_adapter.py exists with 9+ test functions
    - Running pytest shows collection errors or ImportError (not "passed")
    - grep "test_look_ahead_bias_guard" backtest/tests/unit/test_signal_adapter.py succeeds
    - grep "test_input_not_mutated" backtest/tests/unit/test_signal_adapter.py succeeds
  </acceptance_criteria>
  <done>All test_signal_adapter.py tests collected and failing (RED state confirmed)</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — Implement SignalAdapter to pass all tests</name>
  <files>backtest/src/fund_backtest/signal/adapter.py</files>
  <read_first>
    - backtest/tests/unit/test_signal_adapter.py (tests just written — implement exactly to these)
    - backtest/src/fund_backtest/signal/validator.py (validate_signal_frame to call)
    - backtest/src/fund_backtest/config.py (SignalAdapterConfig to import)
    - backtest/src/fund_backtest/signal/types.py (SignalFrame, WeightFrame type aliases)
  </read_first>
  <behavior>
    Implement signal/adapter.py with the SignalAdapter class.

    Module structure (follow price/validator.py pattern):
    - from __future__ import annotations
    - Module docstring explaining purpose
    - import structlog; logger = structlog.get_logger(__name__)
    - Import SignalFrame, WeightFrame from .types
    - Import SignalValidationError, validate_signal_frame from .validator
    - Import SignalAdapterConfig from fund_backtest.config

    class SignalAdapter:
      """Normalizes raw strategy scores into portfolio-ready WeightFrame.

      Pipeline steps (in order):
        1. validate_signal_frame(signal) — raises SignalValidationError on bad input
        2. Drop all-NaN columns with structlog warning
        3. Cross-sectional rank: signal.rank(axis=1, pct=True) -> [0, 1]
        4. Map to weights: 2 * ranks - 1 -> (-1, +1]
        5. Clip to [-1.0, +1.0] (handles float edge cases)
        6. Zero out rows where non-NaN count < config.min_coverage
        7. Scale rows where abs().sum() > config.gross_exposure_limit (proportional)
        8. Apply shift(1) — WeightFrame row T carries T-1 signal (look-ahead bias guard)

      CRITICAL: Phase 4 Portfolio Simulator must NOT apply an additional shift.
      The WeightFrame returned is already temporally safe.
      """

      def __init__(self, config: SignalAdapterConfig | None = None) -> None:
          self._config = config or SignalAdapterConfig()
          self._log = logger.bind(component="signal_adapter")

      def adapt(self, signal: SignalFrame) -> WeightFrame:
          """Convert raw SignalFrame to portfolio-ready WeightFrame.

          Returns a new DataFrame. Input is never modified.
          First row of returned WeightFrame is always NaN (shift artifact).
          """

    PRIVATE HELPER METHODS (keep adapt() < 50 lines by extracting):
    - _drop_all_nan_columns(df: pd.DataFrame) -> pd.DataFrame
        Find all-NaN columns, log warning for each, return df.drop(those_cols)
    - _compute_weights(ranks: pd.DataFrame) -> pd.DataFrame
        2 * ranks - 1, then clip(-1.0, 1.0)
    - _zero_low_coverage_rows(weights: pd.DataFrame) -> pd.DataFrame
        non_nan_count = weights.notna().sum(axis=1)
        weights = weights.copy()
        weights.loc[non_nan_count < self._config.min_coverage] = 0.0
        return weights
    - _scale_gross_exposure(weights: pd.DataFrame) -> pd.DataFrame
        abs_sum = weights.abs().sum(axis=1)
        needs_scaling = abs_sum > self._config.gross_exposure_limit
        scale_factor = self._config.gross_exposure_limit / abs_sum.where(needs_scaling, 1.0)
        return weights.mul(scale_factor, axis=0)

    IMMUTABILITY: Never use .fillna(inplace=True), .drop(inplace=True), or any in-place pandas ops.
    Always: result = df.copy() before any modification, return result.

    Run: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_adapter.py -v
    Run: cd backtest && .venv/bin/python -m pytest tests/unit/ -q  (full unit suite)
  </behavior>
  <action>
    Create backtest/src/fund_backtest/signal/adapter.py with SignalAdapter class and private helpers.
    Run ruff check: cd backtest && .venv/bin/ruff check src/fund_backtest/signal/adapter.py --fix
    Run ruff format: cd backtest && .venv/bin/ruff format src/fund_backtest/signal/adapter.py
    Run targeted tests: cd backtest && .venv/bin/python -m pytest tests/unit/test_signal_adapter.py -v
    Run full unit suite: cd backtest && .venv/bin/python -m pytest tests/unit/ -q
  </action>
  <verify>
    <automated>cd /Users/maxzou/Documents/projects/AI\ Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/ -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "class SignalAdapter" backtest/src/fund_backtest/signal/adapter.py succeeds
    - grep "def adapt" backtest/src/fund_backtest/signal/adapter.py succeeds
    - grep "shift(1)" backtest/src/fund_backtest/signal/adapter.py succeeds
    - grep "validate_signal_frame" backtest/src/fund_backtest/signal/adapter.py succeeds
    - pytest tests/unit/test_signal_adapter.py shows all 9+ tests passed
    - pytest tests/unit/ shows 0 failures (no regressions to Plan 01 tests)
    - ruff check src/fund_backtest/signal/ shows no errors
  </acceptance_criteria>
  <done>All test_signal_adapter.py tests pass, full unit suite green, ruff clean</done>
</task>

</tasks>

<verification>
Phase 3 is complete when all unit tests pass and ruff is clean:
cd /Users/maxzou/Documents/projects/AI Hedgefund/backtest && .venv/bin/python -m pytest tests/unit/ -v 2>&1 | tail -20
cd /Users/maxzou/Documents/projects/AI Hedgefund/backtest && .venv/bin/ruff check src/fund_backtest/signal/

Observable: 19+ tests passing (10 from Plan 01 + 9 from Plan 02).
</verification>

<success_criteria>
- signal/adapter.py exists with SignalAdapter class
- adapt() method applies: validation → drop NaN cols → rank → weight map → clip → min_coverage zero-out → gross exposure scale → shift(1)
- test_look_ahead_bias_guard passes: spike on T → zero weight on T, nonzero on T+1
- Full unit suite green with no regressions
- No import of ai_washer anywhere in signal/ (fund-backtest is independent)
</success_criteria>

<output>
After completion, create `/Users/maxzou/Documents/projects/AI Hedgefund/.planning/phases/03-signal-adapter-and-integration-contract/03-02-SUMMARY.md`
</output>

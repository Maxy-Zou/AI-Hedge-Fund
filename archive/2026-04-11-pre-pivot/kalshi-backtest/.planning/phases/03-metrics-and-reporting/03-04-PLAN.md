---
phase: 03-metrics-and-reporting
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - kalshi-backtest/src/kalshi_backtest/cli.py
  - kalshi-backtest/tests/test_cli.py
autonomous: false
requirements:
  - MET-05

must_haves:
  truths:
    - "kalshi-backtest run produces a metrics summary table in the terminal after each run"
    - "kalshi-backtest run --output-dir DIR writes trade_log.csv and dashboard.html to DIR"
    - "kalshi-backtest compare runs two named stub strategies and prints a side-by-side Rich table in the terminal"
    - "test_cli.py::test_compare_command passes"
  artifacts:
    - path: "kalshi-backtest/src/kalshi_backtest/cli.py"
      provides: "Enriched run command with --output-dir; new compare command"
      contains: "compare"
  key_links:
    - from: "cli.py run command"
      to: "MetricsCalculator.compute"
      via: "call after runner_obj.run()"
      pattern: "MetricsCalculator"
    - from: "cli.py run command"
      to: "DashboardBuilder.write_html"
      via: "conditional on --output-dir"
      pattern: "DashboardBuilder"
    - from: "cli.py compare command"
      to: "print_comparison_table"
      via: "call after running both strategies"
      pattern: "print_comparison_table"
---

<objective>
Wire the metrics module into the existing `run` CLI command (add `--output-dir` for report output) and add a new `compare` command that runs two stub strategies side-by-side and prints a Rich comparison table. Turn test_cli.py::test_compare_command GREEN.

Purpose: MET-05 requires the compare workflow to be accessible via CLI. The run command must auto-generate the investor-useful summary after every backtest.
Output: Updated `cli.py` with enriched `run` and new `compare` command; `print_comparison_table` helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@kalshi-backtest/.planning/ROADMAP.md
@kalshi-backtest/.planning/phases/03-metrics-and-reporting/03-CONTEXT.md
@kalshi-backtest/.planning/phases/03-metrics-and-reporting/03-RESEARCH.md
@kalshi-backtest/.planning/phases/03-metrics-and-reporting/03-03-SUMMARY.md
</context>

<interfaces>
<!-- Types and patterns this plan consumes. -->

BacktestMetrics (from kalshi_backtest.metrics.calculator):
```python
# All scalar fields: total_return_pct, cagr_pct, sharpe, sortino,
#                    max_drawdown_pct, win_rate_pct, profit_factor,
#                    total_trades, settled_markets, sample_size_warning
# strategy_name: str
```

Rich Table pattern (from RESEARCH.md Pattern 4):
```python
from rich.table import Table
from rich.console import Console

def print_comparison_table(results: list[BacktestMetrics], console: Console) -> None:
    table = Table(title="Strategy Comparison", show_header=True)
    table.add_column("Metric", style="cyan")
    for m in results:
        table.add_column(m.strategy_name, justify="right")
    # rows: label + formatter lambda per metric
    console.print(table)
```

Existing CLI structure (from cli.py):
```python
app = typer.Typer(name="kalshi-backtest", ...)
# Commands: ingest(), run()
# _console = Console() — shared console instance
```

BacktestRunner.run() signature:
```python
def run(self, strategy, start_date, end_date, series_tickers=None) -> BacktestResult
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich run command and add compare command to cli.py</name>
  <files>
    kalshi-backtest/src/kalshi_backtest/cli.py
    kalshi-backtest/tests/test_cli.py
  </files>
  <behavior>
    Enrichments to existing `run` command:
    - Add `--output-dir` option (type: Path, default: None). If provided, create the directory and write:
        * `{output_dir}/trade_log.csv` via export_trade_log
        * `{output_dir}/dashboard.html` via DashboardBuilder.write_html
    - After runner_obj.run() returns, call MetricsCalculator().compute(result) to get BacktestMetrics.
    - Print a Rich metrics summary table to _console (NOT the compare table — just a two-column "Metric / Value" table listing all scalar metrics).
    - If result has sample_size_warning=True, print a bold orange warning line: "[bold yellow]Warning: N={metrics.settled_markets} settled markets — results may be statistically unreliable (N<30)[/bold yellow]"
    - Keep existing final summary line (Settled contracts, Total P&L, etc.) — the metrics table is additional output above it.

    New `compare` command:
    - `@app.command()` named `compare`.
    - Options: `--lookback-days` (int, default 365), `--series` (list[str], default []), `--log-level` (str, default "INFO"), `--output-dir` (Path, default None), `--dry-run` (bool, default False).
    - Dry-run path: print the comparison table with zero-valued BacktestMetrics for each strategy ("StrategyA", "StrategyB") and exit 0 — no DB, no credentials loaded.
    - Live path: runs TWO stub strategies (both _PassThroughStrategy, named "StrategyA" and "StrategyB"), calls MetricsCalculator().compute() on each result, then calls print_comparison_table.
    - If --output-dir given, write `dashboard_StrategyA.html` and `dashboard_StrategyB.html`.

    `print_comparison_table(results: list[BacktestMetrics], console: Console) -> None`:
    - Defined as a module-level function in cli.py (not inside a command).
    - Uses Rich Table with: Metric column + one column per strategy.
    - Rows: Total Return %, CAGR %, Sharpe, Sortino, Max Drawdown %, Win Rate %, Profit Factor, Total Trades, Settled Markets.
    - If any result has sample_size_warning=True, print a warning line below the table.

    test_cli.py::test_compare_command:
    - Uses typer.testing.CliRunner to invoke `compare --dry-run`.
    - Assert exit_code == 0.
    - Assert "Strategy Comparison" appears in the captured output.
  </behavior>
  <action>
    Read the current cli.py (already in context above). Plan changes carefully before writing:

    1. Add lazy imports inside run() for new metrics modules:
       ```python
       from kalshi_backtest.metrics import MetricsCalculator, export_trade_log, DashboardBuilder
       ```

    2. Add `print_comparison_table` function before the first @app.command() decorator.

    3. Modify the `run` command:
       - Add `output_dir: Path | None = typer.Option(None, "--output-dir", ...)` parameter.
       - After `result = runner_obj.run(...)`, add metrics computation and display.
       - If output_dir is not None: `output_dir.mkdir(parents=True, exist_ok=True)`, then write CSV and HTML.

    4. Add `compare` command with dry-run support as described in behavior section.

    5. Write test_cli.py updates: add `test_compare_command` that invokes `compare --dry-run` via CliRunner and asserts exit_code == 0 and "Strategy Comparison" in output.

    Keep the file under 400 lines total. Extract print_comparison_table and any other helpers cleanly to avoid deep nesting.

    After writing, run:
    `cd "/Users/maxzou/Documents/projects/AI Hedgefund/kalshi-backtest" && uv run pytest tests/test_cli.py -q`

    Then run the full test suite:
    `cd "/Users/maxzou/Documents/projects/AI Hedgefund/kalshi-backtest" && uv run pytest -q`
  </action>
  <verify>
    <automated>cd "/Users/maxzou/Documents/projects/AI Hedgefund/kalshi-backtest" && uv run pytest tests/test_cli.py -q && uv run pytest -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>test_cli.py::test_compare_command passes. Full test suite passes (or only pre-existing failures remain). `kalshi-backtest compare --dry-run` exits 0 and prints "Strategy Comparison".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 3 reporting end-to-end</name>
  <files>kalshi-backtest/src/kalshi_backtest/cli.py</files>
  <action>Human verification step — no automated action. Confirm the full Phase 3 reporting layer works correctly via the steps in how-to-verify.</action>
  <verify>
    <automated>cd "/Users/maxzou/Documents/projects/AI Hedgefund/kalshi-backtest" && uv run kalshi-backtest compare --dry-run 2>&1 | grep -c "Strategy Comparison"</automated>
  </verify>
  <done>User confirms all verification steps pass and types "approved".</done>
  <what-built>
    Full Phase 3 reporting layer:
    - MetricsCalculator computing Sharpe, Sortino, CAGR, max drawdown, win rate, total return, avg trade P&L from BacktestResult
    - export_trade_log writing CSV with pnl_usd and fee_usd columns
    - build_dashboard producing 4-panel Plotly HTML with equity curve, drawdown, monthly P&L, per-category breakdown
    - Sample size warning (N lt 30 distinct settled markets) surfaced in BacktestMetrics, dashboard annotation, and CLI output
    - CLI run command prints metrics summary table and optionally writes trade_log.csv + dashboard.html
    - CLI compare command prints side-by-side Rich comparison table for two strategies
  </what-built>
  <how-to-verify>
    1. Run: `cd kalshi-backtest && uv run pytest -q --tb=short`
       Expected: all tests pass (or only pre-existing failures from Phases 1-2)

    2. Run: `uv run kalshi-backtest compare --dry-run`
       Expected: prints "Strategy Comparison" table in terminal with two columns (StrategyA, StrategyB), exits 0

    3. Run: `uv run kalshi-backtest run --dry-run`
       Expected: prints dry-run plan as before, exits 0 (metrics not computed in dry-run — this is correct)

    4. Inspect generated test files (optional): open a Python REPL and manually exercise MetricsCalculator with a minimal BacktestResult to verify no runtime errors.

    5. Check file sizes: dashboard.py should be 120-300 lines, calculator.py 120-250 lines, cli.py under 400 lines.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_cli.py -q` — test_compare_command PASSED
2. `uv run pytest -q` — full suite shows no new failures
3. `uv run kalshi-backtest compare --dry-run` exits 0 with "Strategy Comparison" in output
4. Ruff clean: `uv run ruff check src/kalshi_backtest/cli.py --fix`
5. cli.py is under 400 lines
</verification>

<success_criteria>
- MET-05: compare command prints side-by-side Rich table for multiple strategies
- run command calls MetricsCalculator after every non-dry-run execution
- --output-dir flag on run command writes trade_log.csv and dashboard.html
- compare --dry-run exits 0 without credentials or DB access
- All 6 phase requirements (MET-01 through MET-07 excluding MET-04) covered across Plans 01-04
</success_criteria>

<output>
After completion, create `kalshi-backtest/.planning/phases/03-metrics-and-reporting/03-04-SUMMARY.md`
</output>

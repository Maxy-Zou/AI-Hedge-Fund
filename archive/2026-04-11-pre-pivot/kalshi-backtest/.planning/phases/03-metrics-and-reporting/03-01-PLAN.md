---
phase: 03-metrics-and-reporting
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - kalshi-backtest/pyproject.toml
  - kalshi-backtest/tests/test_metrics_calculator.py
  - kalshi-backtest/tests/test_trade_log.py
  - kalshi-backtest/tests/test_dashboard.py
autonomous: true
requirements:
  - MET-01
  - MET-02
  - MET-03
  - MET-06
  - MET-07

must_haves:
  truths:
    - "quantstats and plotly are declared in pyproject.toml and installable via uv"
    - "test_metrics_calculator.py exists with failing tests for core metrics, empty result guard, and sample size warning"
    - "test_trade_log.py exists with failing tests for CSV export column names and dollar conversion"
    - "test_dashboard.py exists with failing tests for equity curve trace presence and HTML file write"
  artifacts:
    - path: "kalshi-backtest/pyproject.toml"
      provides: "quantstats and plotly dependency declarations"
      contains: "quantstats"
    - path: "kalshi-backtest/tests/test_metrics_calculator.py"
      provides: "RED tests for MET-01 and MET-07"
      min_lines: 60
    - path: "kalshi-backtest/tests/test_trade_log.py"
      provides: "RED tests for MET-02"
      min_lines: 30
    - path: "kalshi-backtest/tests/test_dashboard.py"
      provides: "RED tests for MET-03 and MET-06"
      min_lines: 40
  key_links:
    - from: "tests/test_metrics_calculator.py"
      to: "kalshi_backtest.metrics.calculator"
      via: "import"
      pattern: "from kalshi_backtest.metrics.calculator import"
    - from: "tests/test_trade_log.py"
      to: "kalshi_backtest.metrics.trade_log"
      via: "import"
      pattern: "from kalshi_backtest.metrics.trade_log import"
    - from: "tests/test_dashboard.py"
      to: "kalshi_backtest.metrics.dashboard"
      via: "import"
      pattern: "from kalshi_backtest.metrics.dashboard import"
---

<objective>
Add quantstats and plotly as declared dependencies, then write the complete RED test scaffold for all Phase 3 metrics requirements. Tests must import from modules that do not yet exist — they must fail with ImportError or assertion failures, never skip.

Purpose: Establish the TDD baseline. Every subsequent plan in this phase runs against these tests and must leave them GREEN.
Output: Updated pyproject.toml with two new dependencies; three new test files with failing tests covering MET-01, MET-02, MET-03, MET-06, MET-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@kalshi-backtest/.planning/PROJECT.md
@kalshi-backtest/.planning/ROADMAP.md
@kalshi-backtest/.planning/STATE.md
@kalshi-backtest/.planning/phases/03-metrics-and-reporting/03-CONTEXT.md
@kalshi-backtest/.planning/phases/03-metrics-and-reporting/03-RESEARCH.md
</context>

<interfaces>
<!-- Key types from Phase 2 that tests must use for fixture construction. -->
<!-- Read directly from source — do not explore further. -->

From kalshi-backtest/src/kalshi_backtest/simulation/runner.py:
```python
@dataclass(frozen=True)
class BacktestResult:
    run_id: str
    strategy_name: str
    start_date: datetime
    end_date: datetime
    trade_log: pd.DataFrame      # columns: ticker, direction, contracts, entry_price,
                                  #          entry_ts, exit_price, exit_ts, pnl_cents,
                                  #          fee_cents, exit_reason
    daily_pnl: pd.Series         # date index → integer pnl_cents
    total_pnl_cents: int
    total_fees_cents: int
    settled_contracts: int        # sum of contracts where exit_reason == 'settlement'
    open_contracts: int
```

From kalshi-backtest/src/kalshi_backtest/simulation/position_tracker.py:
```python
# to_trade_log() columns (in order):
# "ticker", "direction", "contracts", "entry_price", "entry_ts",
# "exit_price", "exit_ts", "pnl_cents", "fee_cents", "exit_reason"
#
# exit_reason values: "settlement", "position_close"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add quantstats and plotly to pyproject.toml</name>
  <files>kalshi-backtest/pyproject.toml</files>
  <action>
    Read the current pyproject.toml. Add two new entries to the `[project] dependencies` list:
      - `"quantstats>=0.0.81"`
      - `"plotly>=5.0"`

    Then run `uv add quantstats plotly` from within the `kalshi-backtest/` directory to update uv.lock and install both packages into the virtual environment.

    Both packages must be resolvable without conflict against the existing dependency set (pandas 3.x, numpy 2.x are already pinned).

    Do NOT change any other lines in pyproject.toml.
  </action>
  <verify>
    <automated>cd "/Users/maxzou/Documents/projects/AI Hedgefund/kalshi-backtest" && uv run python -c "import quantstats; import plotly; print('ok')"</automated>
  </verify>
  <done>Both `import quantstats` and `import plotly` succeed inside the uv venv. quantstats and plotly appear in pyproject.toml dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test scaffolds — metrics, trade log, and dashboard</name>
  <files>
    kalshi-backtest/tests/test_metrics_calculator.py
    kalshi-backtest/tests/test_trade_log.py
    kalshi-backtest/tests/test_dashboard.py
  </files>
  <behavior>
    Shared fixture (add to each test file or conftest):
    - `minimal_backtest_result()` — a BacktestResult with:
        * 3 closed trades: two profitable settlements (pnl_cents 200, 150) and one losing settlement (pnl_cents -80)
        * daily_pnl: pd.Series({date(2024,1,10): 350, date(2024,1,12): -80}, dtype=int)
        * start_date=datetime(2024,1,1), end_date=datetime(2024,1,31)
        * total_pnl_cents=270, total_fees_cents=15, settled_contracts=3, open_contracts=0
        * trade_log has exit_reason='settlement' for all rows, tickers 'KXBTC-A', 'KXBTC-B', 'KXETH-A'

    test_metrics_calculator.py tests:
    - test_sharpe_is_finite: MetricsCalculator().compute(result) → BacktestMetrics with sharpe being a finite float (not NaN, not inf)
    - test_total_return_positive: metrics.total_return_pct > 0 for the profitable minimal result
    - test_sample_warning_false_when_enough: result with 30 distinct settled tickers → sample_size_warning is False
    - test_sample_warning_true_when_few: minimal_backtest_result (3 distinct tickers) → sample_size_warning is True
    - test_empty_result_no_error: BacktestResult with empty trade_log and empty daily_pnl → MetricsCalculator().compute() returns BacktestMetrics with total_return_pct == 0.0 and sample_size_warning == True, no exception raised
    - test_win_rate_range: metrics.win_rate_pct is between 0.0 and 100.0

    test_trade_log.py tests:
    - test_export_columns: export_trade_log(result, path) writes CSV with columns including 'pnl_usd', 'fee_usd', 'ticker', 'direction', 'entry_ts', 'exit_ts'
    - test_dollar_conversion: pnl_usd column values equal pnl_cents / 100 for each row
    - test_empty_trade_log_no_file: export_trade_log with empty trade_log does not create the file (or creates it empty — either is valid)

    test_dashboard.py tests:
    - test_equity_curve_trace_present: build_dashboard(metrics, result.trade_log) returns a plotly Figure with at least one trace named 'Equity' or containing equity curve data
    - test_write_html_creates_file: DashboardBuilder(metrics, result.trade_log).write_html(tmp_path/"dashboard.html") creates a non-empty file ending in .html
    - test_category_breakdown_groups: compute_category_breakdown(trade_log) returns DataFrame with a 'KXBTC' row and a 'KXETH' row based on ticker prefixes

    ALL tests must import from modules that do not yet exist:
      `from kalshi_backtest.metrics.calculator import MetricsCalculator, BacktestMetrics`
      `from kalshi_backtest.metrics.trade_log import export_trade_log`
      `from kalshi_backtest.metrics.dashboard import build_dashboard, compute_category_breakdown`

    Tests MUST fail with ImportError when run before Plan 02 creates those modules.
  </behavior>
  <action>
    Create three test files. Each begins with `from __future__ import annotations`.

    In test_metrics_calculator.py: define the `minimal_backtest_result` pytest fixture at module level (not in conftest.py — keep it local). Build a BacktestResult using the exact field names from the interface block above. Create a 30-ticker version for the sample_warning=False test by constructing a trade_log DataFrame with 30 distinct ticker values all with exit_reason='settlement'.

    In test_trade_log.py: reuse a simpler version of the fixture inline. Use `tmp_path` pytest fixture for file output paths.

    In test_dashboard.py: import `BacktestMetrics` from `kalshi_backtest.metrics.calculator` (will fail at import time — this is correct RED behavior). Use `tmp_path` for HTML output.

    Do NOT create `kalshi-backtest/src/kalshi_backtest/metrics/` yet — the module must be absent so tests fail with ImportError, confirming RED state.

    After writing all three files, run the tests to confirm they all fail:
    `cd "/Users/maxzou/Documents/projects/AI Hedgefund/kalshi-backtest" && uv run pytest tests/test_metrics_calculator.py tests/test_trade_log.py tests/test_dashboard.py -x -q 2>&1 | head -20`

    The output should show ImportError or ModuleNotFoundError — not skip or pass.
  </action>
  <verify>
    <automated>cd "/Users/maxzou/Documents/projects/AI Hedgefund/kalshi-backtest" && uv run pytest tests/test_metrics_calculator.py tests/test_trade_log.py tests/test_dashboard.py -q 2>&1 | grep -E "ERROR|ImportError|ModuleNotFoundError|error"</automated>
  </verify>
  <done>All three test files exist, contain the specified tests, and fail with ImportError when run. The test count is at least 12 failing tests across the three files.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "import quantstats; import plotly"` exits 0
2. `uv run pytest tests/test_metrics_calculator.py tests/test_trade_log.py tests/test_dashboard.py -q` shows ERRORS (not PASSED, not SKIPPED)
3. Each test file is at least 30 lines long
4. pyproject.toml contains `quantstats>=0.0.81` and `plotly>=5.0`
</verification>

<success_criteria>
- quantstats and plotly importable in the uv venv
- 12+ failing tests across three test files, all failing with ImportError from non-existent metrics modules
- No test passes or skips at this stage
</success_criteria>

<output>
After completion, create `kalshi-backtest/.planning/phases/03-metrics-and-reporting/03-01-SUMMARY.md`
</output>
